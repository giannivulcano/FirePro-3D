# TODO — Completed (archive)

> Append-only archive of finished tasks (moved here from `todo_open.md` on completion, with their `[done:]` stamp and build notes). Not scanned for task selection.

## 🎯 Current Focus — MVP: AHJ Package (decided 2026-06-23 grill; full rationale in `DOCS-REVIEW.md`)

### A. Drift / correctness fixes (do FIRST — they keep the AI's specs trustworthy; cheap)

- [x] Fix broken fresh install — `requirements.txt` omits `pyvista`/`pyvistaqt`/`vtk` (hard-imported by `view_3d.py` at startup, so a clean clone crashes on launch); document ODA File Converter setup for DWG. Added the 3D stack to `requirements.txt` (dropped vestigial `vispy`); documented deps + ODA setup + launch-verify in `getting-started.md`; fixed `index.md` "DXF export" claim. `requirements.txt`, `docs/getting-started.md` [type:Bug] [P1] [subject:Documentation] [done:2026-06-23]

- [x] Sweep removed layer system from docs — purged `user_layer` from `architecture/{display-system,io,entities,overview,level-system}.md` + `contributing/{guide,adding-entities}.md`; renamed `DEFAULT_USER_LAYER → DEFAULT_ANNOTATION_GROUP` (constants.py + annotations.py use; dropped dead imports in model_space.py + test); also fixed CLAUDE.md stale layer/tech-stack lines. (Spec-level `user_layer` refs in grid-system/underlay-workflow/pipe-placement/wall-room left for lazy-backfill on next touch.) [type:Task] [P1] [subject:Documentation] [done:2026-06-23]

- [x] Reconcile Z-order to one source of truth — `docs/specs/view-relationships.md §7.3` + `constants.py` are the home (also fixed stale `_Z_SCALE`→`Z_ELEV_SCALE` in §7.3); replaced the wrong Z tables in `display-system.md` + `guide.md` with LINKs (Rule A) and added the runtime elevation-based ordering. `overview.md` had no Z table to fix. [type:Task] [P1] [subject:Documentation] [done:2026-06-23]

- [x] Fix fictional APIs in contributor how-tos — `_snap_or_raw`→`get_effective_position(event.scenePos())`; `_geom_to_item()`→`_on_dxf_finished`/`_append_geom_to_path` (incl. `pdf_import_worker.py` docstring); `SnapEngine.snap()`→`find()`; `apply_for_level`→`apply_to_scene`; `Model_Space.py`→`model_space.py`. `contributing/*`, `architecture/{overview,io,level-system}.md` [type:Task] [P1] [subject:Documentation] [done:2026-06-23]

- [x] Fix `architecture/io.md` — `.fp3d`→`.fpd`; cache version 3→4; replaced fabricated version-gated migration with the real key-presence backward-compat behavior (SAVE_VERSION recorded but not branched on; inch→mm by key); cross-linked `underlay-workflow.md`. [type:Task] [P1] [subject:Documentation] [done:2026-06-23]

- [x] **Install `pytest-timeout` so a hung test fails instead of stalling the suite** — `test_print_passes_template_and_project_info` blocked forever from 2026-08-06 to 2026-08-19 on an unpatched modal (fixed `41d80a3`), and because it **hung rather than failed** with nothing to time it out, *no* full-suite run could complete for twelve days and it emitted no output at all to say so. That silently made the "run the full suite before claiming done" rule unenforceable; the moment it was fixed, the suite immediately caught a real regression targeted runs had missed. A per-test timeout turns the whole failure class from invisible into a normal red test. Shipped: `pytest-timeout>=2.3` in `requirements.txt` (installed 2.4.0); `pytest.ini` gets `timeout = 300` + `timeout_method = thread` (Windows has no SIGALRM, so thread is the only method). Verified the header reports `timeout: 300.0s` and a deliberate 30s-sleep test is killed at a 3s override with a `Timeout` traceback naming the test+line and a nonzero exit. `requirements.txt`, `pytest.ini` [type:Task] [P1] [subject:Testing] [done:2026-08-21]

### B. Paper-space AHJ output (the MVP blocker — most machinery already exists)

- [x] Paper-space PLOT step (sheet → PDF/print) — **the #1 MVP blocker.** Shipped **single-sheet** vector PDF export (`export_pdf`) + system-dialog print (`print_sheets`) in new `paper_export.py`, surfaced via Draft ribbon → **Plot** group. Renders a transient off-screen `PaperScene` (reuses the `paper_display.py` B&W/line-weight override pipeline verbatim — no second render path). Core takes `list[Sheet]` so batch/multi-page is a thin add once multi-sheet management lands. 12 tests (`tests/test_paper_export.py`). `paper_export.py`, `paper_space.py`, `main.py` [ref:paper-space§7] [type:Task] [P1] [subject:Architecture] [done:2026-06-25]

- [x] Sheet-space annotations — free-placed **text blocks** on `PaperScene` + a unified, session-only **paper-space undo/redo** stack (covers text **and** viewport ops). New `TextAnnotationData`/`TextAnnotationItem` (paper-fixed **cap-height** sizing via `setPixelSize`+`setScale` — never point-size / `ItemIgnoresTransformations`; wrap-resize grip; inline edit; opaque-bg; z=15 above the title block), `TextAnnotationPropertiesDialog`, new `paper_commands.py` (`QUndoStack` + per-op commands, Ctrl+Z/Y/Shift+Z routing, reset on load/new). Prereq: collapsed the duplicate `PaperSpaceWidget` so one canonical scene drives the tab. Export + persistence come **free** via the data-driven `_setup` rebuild (zero `scene_io`/`paper_export` changes). Smoke-test fixes: undo (ShortcutOverride suppressed the QAction), Esc-revert (window `QShortcut("Escape")` stole it), zero-height `ZeroDivisionError`, imperial height precision. ~50 tests. `paper_space.py`, `paper_commands.py`, `constants.py`, `main.py`, `docs/specs/paper-space.md §9/§17` [ref:paper-space§9,§17] [type:Task] [P1] [subject:Architecture] [done:2026-07-02]
  - **Follow-ups (from the 2026-07-02 build + smoke test):**
    - [x] Replace the text **Properties dialog** with the right-side **property panel** + a persisted **Add-Text template** (pipe/sprinkler pattern; QSettings `template/text`). `TextAnnotationItem` joined the duck-typed `get_properties`/`set_property` protocol with commits routed through the paper `QUndoStack` (multi-select = one macro = one Ctrl+Z; Word-like `_MixedStateCheckBox` for mixed values); panel grew `bool`/`font` types + `dimension` parser/minimum/formatter passthrough (`DimensionEdit` D4 resolved); **height displays/parses Word-style pt** (storage stays cap-height mm); paper/panel scrollbars hidden; `TextAnnotationPropertiesDialog` deleted. Prereq: forged `docs/specs/property-panel.md` (orphan gate). ~35 new tests. `property_manager.py`, `dimension_edit.py`, `paper_space.py`, `theme.py`, `main.py` [ref:paper-space§9.5-9.6, property-panel] [type:Task] [P2] [subject:Architecture] [done:2026-07-09]
      - **Follow-ups (from the 2026-07-09 build + smoke test):**
        - [x] **Word-style Font ribbon group** for sheet text — shipped with the Draft-tab migration on `feat/ribbon-text-ux` (2026-07-16 grill; 4 smoke rounds). New `font_group.py` `FontGroupController` on the Draft tab: family combo, editable pt dropdown (Word ladder `FONT_SIZE_LADDER_PT`, panel-identical parsing), grow/shrink (ladder-snap), B/I/**U** (underline = new `TextAnnotationData` field end-to-end incl. template seed + QSettings), font color (themed swatch), L/C/R. Enable-on-context (paper tab + selection, or Add-Text mode → edits template); all writes ride `FormatTextCommand`; multi-select = one macro; **no empty macros on no-op gestures**; Word-style mixed display; panel↔ribbon co-exist (panel keeps template view during add-mode undo churn). `RibbonGroup.add_widget` primitive (ribbon spec D3 resolved). Scope decided in grill: sheet text only (NoteAnnotation absorption filed), no opaque-bg button (panel-only), colored highlight deferred. `font_group.py`, `ribbon_bar.py`, `main.py`, `paper_space.py`, `constants.py` [ref:ribbon-bar-spec, paper-space§9.6] [type:Task] [P2] [subject:UX] [done:2026-07-20]
        - [x] **Viewport properties in the panel** — shipped 2026-08-27 (paper-space bug bundle). Selecting a `SheetViewport` populates the panel via a new `ViewportProperties` adapter (Title/Scale/Show Border/Position X-Y/read-only derived W-H), routed through the undo `ChangeViewportPropertiesCommand`; the double-click dialog and the panel now share `PaperScene.commit_viewport_edit` (one home for the crop×scale derivation). Ribbon contextual Viewport tab (the "both" other half) deferred — see follow-up below. `paper_space.py`, `main.py` [ref:paper-space §19.4, property-panel §3.1] [type:Task] [P2] [subject:Architecture] [done:2026-08-27]
        - [x] **Paper-space viewport/detail-crop cluster** — shipped on `feat/paper-space-crop-cluster` (2026-08-21 grill→brainstorm→plan→subagent build, spec `docs/superpowers/specs/2026-08-21-paper-space-viewport-crop-cluster-design.md`, folded into `paper-space.md`/`view-relationships.md`). **Delivered:** (5) viewport on-paper size = `crop_rect × scale` (Revit crop model; grips crop at fixed scale, details inert; scale change + dialog no longer fight the derived size — smoke bug #5 fixed); (2) elevation markers (`ViewMarkerArrow`+`SharedCropBox`) never plot via `PAPER_EXCLUDED` hard rule; (3) detail boundary boxes plot by default, self-hide inside their own viewport, hideable per-sheet via right-click (undoable). (4) title block non-selectable; Rev/Date/Revisions moved to the Sheet Properties panel. New `SheetViewData.crop_rect`/`hidden_detail_ids` (dual-serialized + migration). Full suite green (2857). **Concern 1 (crop bleed) was initially deferred as PARTIAL (ITT hypothesis); resolved 2026-08-22 in the follow-up below — the real cause was section-cut fill under `ReplaceClip`, not ITT.** `paper_space.py`, `paper_display.py`, `view_marker.py`, `paper_commands.py`, `main.py` [ref:paper-space §5.2/§6.2/§6.4/§19.4] [type:Task] [P1] [subject:Architecture] [done:2026-08-21]
        - [x] **Per-viewport view isolation + robust crop clip (P1 follow-up to the crop cluster)** — shipped on `feat/paper-space-crop-cluster` (2026-08-21/22 grill→brainstorm→plan→subagent build + systematic-debugging, spec `docs/superpowers/specs/2026-08-21-per-viewport-isolation-robust-clip-design.md`, folded into `paper-space.md §6.6`/`view-relationships.md §6.5`). **Isolation (#3/#4/#2):** `ViewResolver.resolve_level_context(view_type,view_name)→(level,vh,vd)` (plan→PlanView, detail→marker w/ plan cut-plane inheritance, elevation→None); `SheetViewport.paint` snapshots `active_level`/`active_view_key`, applies the viewport's context via `LevelManager.apply_to_scene` **before** the override pass, and restores **synchronously** in `finally` (export-safe — no event loop); no-op skip when the viewport ctx == on-screen ctx; echo-suppression window widened; `main._apply_plan_level`/`_apply_detail_level` dedup onto the resolver. **Clip (#1) — the smoke "wall bleed" was NOT the ITT hypothesis:** two adversarial real-item spikes proved `setClipRect` clips ITT items/underlays/fittings/details/PDF correctly; the real bleed was **section-cut walls** — `draw_section_hatch` drew the solid fill/hatch under `setClipPath(ReplaceClip)` (discarded the viewport clip) → fixed to `IntersectClip` (`displayable_item.py`, vector-preserving, also correct on the model canvas). **Construction geometry not plotting (#2):** rect/circle/arc default to pen white + read `self.pen()` directly + had no paper category → white-on-white; added a paper-only **"Construction" category** (`paper_display.py`, black in BW) that sets the pen colour AND normalises width to true on-paper mm (`lw_mm/paper_scale`, else sub-pixel hairline). All 4 smoke issues user-confirmed on a real PDF. Gaming-proof tests: REAL WallSegment/GridlineItem/RectangleItem + rendered-pixel/PDF assertions, all red-verified. Full suite green (2875) + targeted sweep 991. `paper_space.py`, `paper_display.py`, `displayable_item.py`, `main.py`, `detail_view.py`, `construction_geometry.py` (net-0, debug only) [ref:paper-space §6.2/§6.6, view-relationships §6.5] [type:Task] [P1] [subject:Architecture] [done:2026-08-22]
          - **Follow-ups (from the 2026-08-21/22 build + smoke tests):**
            - [x] **`plan_view_mgr` leaks on new project** — `new_file()` clears the detail manager (fixed this session) but not `plan_view_mgr._views`, so stale per-level cut-plane `PlanView`s from the previous project accumulate (masked because `create()` reuses by name). Fixed 2026-09-02: added `PlanViewManager.clear()` (mirrors `DetailViewManager.clear()`) + call it in `new_file()` alongside the detail-manager reset. Regression tests (functional + unit, red-verified) added to `tests/test_detail_new_project_leak.py`. `main.py`, `level_manager.py` [type:Bug] [P3] [subject:Architecture] [done:2026-09-02]
            - [x] **Model-side Construction Display-Manager category** — done via the 2D-geometry level+fill task (2026-08-22): new "2D Geometry" category in `display_manager` (mirrors Design Area), construction items read `_display_color` via `DisplayableItemMixin` + `_apply_geo2d`. `display_manager.py`, `construction_geometry.py` [type:Task] [P3] [subject:UX] [done:2026-08-22]
        - [x] **Paper dirty-flag** — shipped on `feat/paper-persistence` (2026-07-20 grill: latch-until-save, undo/redo dirty, rule = "dirties iff it changes bytes `Sheet.to_dict()` would emit"). New `PaperScene.sheetModified` signal: relay from `undo_stack.indexChanged` (suppressed during `update_from_sheet` rebuilds so load/new stay clean), paper-size setter emits on real change only (no-op sets now skip the rebuild too), title-block dialog emits on actual field change (cancel/no-op silent); `MainWindow._on_paper_modified` (no 3D-refresh debounce). Add-Text template edits stay clean (QSettings-only). Autosave now captures paper-only edits. 13 tests (`tests/test_paper_persistence.py`, red-verified 10/13 bite pre-fix). `paper_space.py`, `main.py` [ref:paper-space§17.7] [type:Bug] [P2] [subject:Architecture] [done:2026-07-20]
        - [x] **Crash-recovery skips the paper scene** — shipped on `feat/paper-persistence`. `_check_recovery` now runs **full open parity** via the extracted shared `_apply_loaded_file()` (scene, levels, display settings, elevation markers, detail browser, active plan view, unit prefs, sheet + resolver rebind), deviating only in `_current_file=None` (first Save → Save-As), `_modified=True`, no recent-files entry. Recovered annotations visible; save-after-recovery no longer overwrites them with the stale default sheet. Bonus fix discovered en route: `PaperSpaceWidget._sheet` was never rebound on load, so post-load **title-block edits wrote into a detached dict** and were silently lost — new `PaperSpaceWidget.set_sheet(sheet, resolver)` keeps widget/scene/resolver in lockstep on every load path (open/recovery/new). `main.py`, `paper_space.py` [ref:paper-space§17.7] [type:Bug] [P2] [subject:Architecture] [done:2026-07-20]
        - [x] **`_load_project` resolver rebind** — fixed with the recovery-parity refactor: `_apply_loaded_file` builds the `ViewResolver` *before* the paper rebind and pushes it into the live scene via `PaperScene.set_resolver` (viewports capture it at construction). Identity asserted by test after open + recovery. `main.py`, `paper_space.py` [type:Bug] [P3] [subject:Architecture] [done:2026-07-20]
    - [x] Move the paper toolbar / **Add Text** into the ribbon **Draft tab** — shipped on `feat/ribbon-text-ux`. PaperSpaceWidget toolbar **retired entirely** (public command API: `change_paper`/`set_add_text_mode`/`refresh_viewport`/`fit_sheet`; Refresh+Fit → Draft small buttons; Paper Size/Title Block deduped to their existing ribbon homes). Add Text = checkable Draft button: auto-switches to the paper tab, unchecks on placement/mode-exit/tab-leave, and **Esc** (new ShortcutOverride+keyPressEvent path — no Esc-cancel existed). Draw+Draft merge evaluated in grill → **no merge**; target taxonomy recorded in ribbon-bar spec (Draft = documentation tab; Annotations move filed below). Smoke rounds also landed: Esc **commits** text (placement + existing blocks; undo recovers), sheet-text **box model** (`box_height_mm`) + viewport-parity **8-handle grips** + shared `SELECTION_*` style constants + theming.md base-style guide, grip-halo boundingRect fix (stale drag trails), 1mm text-box margin, grab-anywhere hit area (`shape`+`contains`). `main.py`, `ribbon_bar.py`, `paper_space.py`, `paper_commands.py`, `font_group.py`, `constants.py` [ref:ribbon-bar-spec, paper-space§9] [type:Task] [P2] [subject:UX] [done:2026-07-20]
    - [x] **Four-corner resize grips** for sheet text — superseded-and-shipped as the viewport-parity **8-handle** model (corners + midpoints, box model with stored `box_height_mm`) during the 2026-07-20 smoke rounds; see the Draft-tab item above. [type:Task] [P3] [subject:CAD] [done:2026-07-20]

### C. Hydraulic calc deliverable

- [x] Hydraulic report → 3-tab + Node Summary — see **"Consolidate report to 3 tabs"** under _Hydraulic Solver Follow-Ups_ (bumped to P1). [ref:hydraulic-spec§9,D7] [type:Task] [P1] [subject:Hydraulic Calculator] [done:2026-07-10]

- [x] **Design-area definition/creation polish** — shipped 2026-07-13 (`feat/design-area-polish`; spec §11 rewritten as-built; 4 smoke-test rounds). Calc: `As = min(NFPA S×L, listed Coverage Area)`, warn+cap surfacing atop the HC report. Drawing: tessellating tiles (half-gap / **ray-cast** wall stop at any angle / √cov÷2 open sides) clipped to the containing room polygon, ~10mm-inflated union; editing = fill + tile edges below geometry, confirmed = red dashed **traced outline** above geometry+gridlines (`Z_DESIGN_AREA_CONFIRMED`). Pick mode: sprinkler-centre-only snap w/ marker, non-sprinkler items inert, zoom-aware px radius, per-sprinkler rings, live count+area tally, sceneModified → browser/dirty. **Multi-area flow**: confirm stays in mode + clears working area; member-click resumes. Level **derived read-only** from member sprinklers (fixed latent hardwired-Level-1 wall lookup). Display Manager "Design Area" category. Both serialization paths (scene_io + undo) persist level w/ backfill and recompute tiles after walls/rooms load. Bonus: root-caused+fixed one contributor of the "environmental" full-suite 0xC0000409 (View3D.rebuild after cleanup — see Testing item below). 54 tests (`tests/test_design_area.py`). `design_area.py`, `model_space.py`, `scene_io.py`, `level_manager.py`, `display_manager.py`, `constants.py` [ref:sprinkler-spec§11] [type:Task] [P2] [subject:Sprinkler Design] [done:2026-07-13]

- [x] **Design-area tiles break on non-uniform arrangements near diagonal walls** (user smoke-test screenshot 2026-07-13, Room 2) — root cause (evidence: 8-scenario reconstruction sweep): flat per-side ray extents can't express a slanted boundary, and the room-polygon clip — the only defense — silently no-ops whenever the detected room boundary doesn't carry the diagonal edge (room snapshot predates the wall, unconnected/dead-end diagonal, or no room). Room containment never failed; perp rays Did hit. Fix: `_clip_tile_at_walls` — each nearby wall segment casts a line-of-sight **shadow quad** from the sprinkler's viewpoint, subtracted from the tile (connected piece containing the sprinkler kept), so tiles stop at physical walls regardless of room state; coverage survives past open wall ends only where sight lines exist. Also retires the spec §11.5 no-room known limitation. 6 regression tests (red-green verified). `design_area.py` [ref:sprinkler-spec§11.5] [type:Bug] [P2] [subject:Sprinkler Design] [done:2026-07-13]

- [x] **Design-Area Criteria System** (the badge + panel/inheritance pair, expanded in the 2026-07-14 grill) — shipped on `feat/design-area-criteria` (~20 commits, 3 smoke rounds, subagent-driven with two-stage reviews). **Room:** panel reorganized into Room Info / Geometry / Protection Criteria sections (new panel `header` type); Protection Criteria = Hazard Class (Low-Piled Storage added; Fill Color row removed — Display Manager owns appearance) + Occupancy (free text) + System Type (Wet/Dry) + **design point picked on the auto-populate curve graph** (`design_point_dialog.py`; defaults to curve minimum; resets on hazard change; N/A for storage). **Inheritance:** `DesignArea.effective_criteria()` — single room = read-only inherited (+ "Criteria From"/"Rooms" rows); multi-room = most demanding governs + warning (Dry > Wet); storage/unknown hazard = disengage editable + warning; mixed = rooms govern + note; no rooms = editable fallback; write-back keeps last effective on disengage. **Dry check:** required = base ×1.3; drawn < required → warning in panel (new full-width wrapped `warning` panel type, bottom row) + top of HC report. **Report:** Design-criteria section = Hazard / System Type / Design Point / Required Area (+30% dry note) / Drawn Area; density = design-point density. **Badge:** mockup-gated DESIGN CRITERIA table (filleted corners, blank footer band, SYSTEM ID label) — grip-draggable (scene has no native item drag; room-label pattern), auto-centred until moved, confirmed-only + Show Badge toggle, dual-path persisted, TBD hydraulic cells fill from a `run_hydraulics` snapshot, computed dry system capacity (Σ 0.0408·ID²·L), K→orifice map, numeric-deduped K/coverage lists. **Also:** new `nfpa_curves.py` (curves/interpolators moved out of the dialog), `units-and-formatting.md` governing spec forged + `format_area_sqft`/`format_density` on ScaleManager ("sq ft" convention, project-units aware), WaterSupply Domestic Water Allowance (informational), auto-populate seeds from room design point, legacy panel Level-row suppression, panel min-width fixes (combo `minimumContentsLength`, button shrink). ~90 new tests; full suite green (chunked). `design_area.py`, `room.py`, `nfpa_curves.py`, `design_point_dialog.py`, `property_manager.py`, `scale_manager.py`, `water_supply.py`, `model_space.py`, `scene_io.py`, `hydraulic_report.py`, `auto_populate_dialog.py`, `constants.py` [ref:sprinkler-spec§11, wall-spec§9, hydraulic-spec§9.1, property-panel, units-and-formatting] [type:Task] [P2] [subject:Sprinkler Design] [done:2026-07-14]
  - **Follow-ups (from the 2026-07-14 grill + build):**

## Tasks

- [x] **Import Underlay dialog — first-principles redesign** (user, 2026-08-31→09-01; **SHIPPED & smoke-tested** on `feat/import-dialog-redesign`; [done:2026-09-01]). Rebuilt `UnderlayImportDialog` (`dxf_preview_dialog.py`) from `IMPORT_DIALOG_SPEC.md` into a **frameless step-rail / preview / contextual-panel / commit-sentence** shell reusing all existing loaders/calibrate/crop/scale/layer/PDF machinery. **Data layer:** `Underlay.scale_verified` (persisted), `ImportParams.levels`+`scale_verified`, `underlay_mru.RecentSources`, `_record_levels` threading, Modify authors levels+verified (reverses §10.3 level-picker removal). **Shell:** `_StepRow` rail (number chips/highlight), `QStackedWidget` per-step panel (flat overline sections), `_ToggleSwitch` Position, `_SwitchBar`, centred empty-state overlay, outside-dim crop scrim, calibrate auto-verifies, commit sentence, side-arrow filmstrip. **Chrome:** Win11 DWM rounded corners + thin edge, three-dot window controls, footer-style header, accent hover, new `underlay_import_icon.svg`/`underlay_manager_icon.svg`. **House conventions established** (`theming.md`): Arial UI / Consolas values + 5 type roles, switch-vs-checkbox, windows/dialogs, scrollbars-off. Full token sweep of the dialog. 154 targeted tests green (full suite blocked only by the pre-existing offscreen-VTK `view_3d` segfault). `dxf_preview_dialog.py`, `underlay.py`, `underlay_mru.py` (new), `model_space.py`, `main.py`, `dimension_edit.py`, `theme.py`, `graphics/Ribbon/*`, `docs/{specs/underlay-workflow.md §10, architecture/theming.md, specs/icon-style-guide.md}` [ref:underlay-workflow §10] [type:Task] [P1] [subject:Architecture] [done:2026-09-01]
  - **Follow-ups (deferred from the 2026-09-01 build):**
    - [x] **Underlay Manager chrome-match + import polish** ("import manager unification"; user, 2026-09-01; **SHIPPED, smoke-tested iteratively** on `feat/underlay-manager-chrome-match`; grill→plan→subagent-driven (12 tasks + two-stage reviews + whole-branch holistic review) then a long live-smoke arc; ~26 commits, HEAD `cfcb6d6` + docs `db36958`; [done:2026-09-01]). **(A) Rename** `dxf_preview_dialog.py`→`underlay_import_dialog.py` (drops the `DxfPreviewDialog` alias; ~14 importers + doc-gen + hexguard updated). **(B) `FramelessShellMixin`** (`frameless_shell.py`, new) extracted from the import dialog — frameless window, min/max/close (`_WinDot`), header drag, double-click-maximize, **pixel-exact resize edges** (QRect inclusive-edge fix), DWM rounded corners, optional titlebar icon; inherited by BOTH `UnderlayImportDialog` (modal, keeps own header) and `UnderlayManagerDialog` (modeless, resizable). **(C) Manager chrome** = frameless house shell + generic `shellHeader`; both dialogs **open maximized**. **(D) Tokens/roles/QSS** single-homed in `theme.py`: `on_accent` (white primary-btn text, kills a hardcoded `#ffffff`), `role="title"`/`role="name"`, unified **accent hover** (was grey; convention now in `theming.md`), VIS/SNAP/TYPE centered, tree selector `QTableView→QTreeView` (whole block was inert → restored themed bg/hover/selection), **branch chevron images** (`chevron_right/down.svg`) restoring disclosure arrows after the confined-highlight fix. **(E) Manager model:** master-visibility hides all layers + remembers per-layer state (root cause: `_apply_visibility` read `scene.level_mgr` but real scene = `_level_manager` → silent no-op), default collapsed + expansion preserved across model resets (snapshot by `id(record)`), **partial-visibility parent eye glyph** (some layers hidden), **column-width persistence** (QSettings). **(F) Modify = lossless round-trip** (restore page/layers/crop/scale/base; scale root cause = custom-scale edit left hidden by blocked signals) + **3-way insertion-position control** (Reuse existing / Pick new on canvas via `begin_replace_underlay_placement` carrying management fields / Insert at origin; `replace_underlay(position=…)`); Modify header retitles; page switches **preserve** scale/base/levels/layers-by-name/crop-by-bounds; **Escape exits pick mode** (not the dialog); **deterministic preview fit** (`_content_rect()` excludes origin overlay markers that ballooned `itemsBoundingRect`). **(G) insert-at-origin+rotation** pivot fixed (`_apply_underlay_display` pivots vector groups about base-point (0,0) matching the preview; raster keeps centroid; existing rotated underlays shift once on reload — corrected). **(H) combo `QFont::setPointSize<=0`** warning fixed (dialog-scoped QComboBox pt font; app-wide root = `theme.py` `QWidget{font-size:13px}` pixel rule). `underlay_import_dialog.py`, `frameless_shell.py` (new), `underlay_manager.py`/`_model.py`/`_delegates.py`, `model_space.py`, `theme.py`, `main.py`, `graphics/{Ribbon/underlay_manager_icon.svg, chevron_*.svg}`, `docs/{specs/underlay-workflow.md §10.7–10.10, architecture/theming.md, specs/icon-style-guide.md, specs/SPEC-INDEX.md}` [ref:underlay-workflow §10.7] [type:Task] [P2] [subject:UX] [done:2026-09-01]
      - **Follow-ups (from the 2026-09-01 build):**
        - [x] **`.testhome/` gitignore** — subagent pytest runs create an untracked `.testhome/`; add to `.gitignore`. Already present (`.gitignore` lines 45-46, "Test-run scratch"); verified during the 2026-09-02 quick-wins sweep. [type:Task] [P3] [subject:Code Quality] [done:2026-09-02]
    - _Permanently dropped (2026-09-01): URL loader, match-two-points placement, section-stack level picker, relative-path checkbox._ (User-editable Name field **un-dropped 2026-09-01** — see Import Dialog Polish v2 cluster below.)

- [x] **Import Dialog Polish v2 (cluster)** — **SHIPPED & smoke-tested** on `feat/import-dialog-polish-v2` (2026-09-02; /todo grill→plan→subagent-driven with two-stage reviews per task + whole-branch seams review + live-smoke iteration; ~20 commits; governing spec `underlay-workflow.md §10.11–10.13` reconciled + stamped `ea6c1ea`; 155 targeted tests green; [done:2026-09-02]). 2026-09-01 grill-locked (planning only; deferred until the merged chrome-match branch settled). Governing spec: `underlay-workflow.md §10` (update in place) + `architecture/theming.md`. **Tier: Large.** Build order is risk-ranked (loading overlay first, zoom last). Folds in five already-filed open items (annotated `→ folded` at their homes below). All snap/render/overlay changes need **live smoke** (house rule: live-render + interaction bugs dodge headless — `project_live_render_bugs_dodge_headless`, `feedback_synthetic_item_render_test_blindspot`). `underlay_import_dialog.py` + as noted per item.
  - [x] **(1) Loading overlay — replace the inline bottom `LoadingBar`** *(highest risk)*. Centred **overlay card over the preview** (port `d:\Custom Code\FPD Design\underlay_manager_pyqt6\loading.py`: staged checklist, facts-left-behind, worker-thread + Cancel seam), house-themed. **PDF extraction moves onto a worker thread** so its staged progress + Cancel work like DXF/DWG (was synchronous). Stages/facts: DWG = Convert(ODA)→DXF · Read(size) · Scan(N entities) · Extract(M geoms · L layers, determinate) · [paper] Clip/Annotations; DXF = Read · Detect units/layouts · Extract; PDF = Open(pages · sheet size) · Render(vector paths / raster @dpi). Error → **inline failed stage + Close**, returns to Source untouched; Cancel → Source untouched (no partial geoms). ⚠️ Re-verify the `550bb5d` **Modify-PDF sync-block reconcile** path and the `processEvents` re-entrancy history (`project_processevents_reentrancy_crash`, `project_dxf_import_async_gotcha`) against the new async-PDF path — the batch's dominant risk. `underlay_import_dialog.py`, `loading_bar.py`→new `loading.py`, `pdf_import_worker.py` [type:Task] [P2] [subject:UX]
  - [x] **(2) Underlay Name field** *(biggest plumbing; un-drops the 2026-09-01 drop)*. Editable Name in the Source step; blank → filename basename (no regression); duplicates allowed (label, not ID); **import + Modify only** (Manager inline-rename is a separate follow-up). New `Underlay.name` (+ to/from_dict round-trip), `ImportParams.name`, `get_import_params`, ~4 `model_space` commit sites, Modify prefill, `_record_levels`/refresh preservation. Manager `Col.NAME` **and** the model-browser tree node both read `record.name or basename` (`underlay_manager.py:139` `_name`). AC: named import shows in Manager + browser, survives save/load + Modify, blank falls back to filename. `underlay.py`, `underlay_import_dialog.py`, `model_space.py`, `underlay_manager.py`, `main.py` [type:Task] [P2] [subject:CAD]
  - [x] **(3) DXF/DWG `$INSUNITS` unit fix** *(shrunk from "add arch presets")*. **Decision (grill 2026-09-01):** arch/eng plot-scale presets stay **PDF-only** — they're semantically wrong for real-units DXF/DWG model space (user calibrates scaleless DXF views). **Fix the latent 25.4× bug:** `_DXF_INSUNITS` factors are "to-inches" but the app scale is `real_mm/source_units`, so inch/foot/mm DXFs with `$INSUNITS` set auto-import 25.4× too small (`_detect_dxf_units`, `underlay_import_dialog.py:2152`). Correct the table to source-unit→mm; optional units label/selector pre-filled from the corrected code. Unitless DXF unchanged (calibrate). AC: an inch/foot/mm DXF auto-imports at correct real size. **→ folds line 457 (was P2 Bug).** `underlay_import_dialog.py` [type:Bug] [P3] [subject:CAD]
  - [x] **(4) Custom scale → two-field `[paper] = [real]` ratio** (PDF-only). **Replaces** the raw mm-per-point factor field (nobody thinks in mm-per-point); unit-system-aware — imperial `N in = 1 ft`, metric `1:N` — reusing `DimensionEdit`/`format_length`; **calibrate fills both fields**. DXF "custom" after calibrate stays a derived factor under the existing read-only ratio label. AC: no raw factor visible; calibrate populates the two fields; round-trips to the same `import_scale`. **→ folds line 456 (was P3).** `underlay_import_dialog.py` [type:Task] [P3] [subject:UX]
  - [x] **(5) Shared snap renderer (DRY, full parity)**. Extract `paint_snap_indicator(painter, view, snap_result)` into `snap_engine.py`; both `model_view.drawForeground` and `_PreviewView.drawForeground` call it. Preview **gains** the main scene's richer visuals (`source_item2` + filled face indicators) — detection is already unified via `SnapEngine`, so the fields are present. Live-smoke the preview indicators. **→ folds line 454 (P2, "ready to build").** `snap_engine.py`, `model_view.py`, `underlay_import_dialog.py` [type:Task] [P2] [subject:CAD]
  - [x] **(6) De-dup `_append_geom_to_path`**. Extract the geom→path builder (duplicated in `model_space` + the dialog) into one shared helper; pure refactor, guarded by a render-equivalence test on **real** geoms. **→ folds line 458 (P3); discharges the remaining half of the §17.4 de-dup follow-up.** `model_space.py`, `underlay_import_dialog.py` [type:Task] [P3] [subject:Code Quality]
  - [x] **(7) Preview placeholder on layout switch** → `"Loading <layout>…"` during extraction (currently stays "Select a layout to preview."), driven off the new overlay stage. **→ folds line 465 (P3 Bug).** `underlay_import_dialog.py` [type:Bug] [P3] [subject:CAD]
  - [x] **(8) Zoom cap** — `_PreviewView._ZOOM_MAX 12.0 → 20.0` (2000% of fit; `underlay_import_dialog.py:201`). One line. `underlay_import_dialog.py` [type:Task] [P3] [subject:UX]
  - **Bonus fixes (live-smoke, 2026-09-02):** overlay pre-lists the full stage checklist up front (`3347efb`); **calibration snaps** a measured factor to the nearest standard scale within 2% — named arch/eng preset or metric `1:N` — + added `1/32"`/`1/16"`/`3/32"` arch presets (`d964495`); PDF **Modify reload filters render by `selected_layers`** on reuse/origin paths (`910d07f`, pre-existing bug from `935905a`); Modify **preserves the saved base point** (`reset_base=False`, `29ba95a`); **PDF Cancel** honored mid-extraction via a `should_cancel` poll (`ea6c1ea`).
  - **Follow-ups (from the 2026-09-02 build + smoke):**

- [x] **Underlay zoom/pan performance** (user, 2026-08-29→30, Sleeman boiler room FPD; **SHIPPED & smoke-tested** on `feat/underlay-freeze-blit` — grill→plan→subagent build, then a live-instrumented systematic-debugging arc that overturned the original diagnosis twice; [done:2026-08-30]). **Final root causes (BOTH found only by instrumenting the LIVE app — headless probes cache-missed and painted 20× less geometry, understating everything):** (1) ⭐ **Cosmetic pen width > 1.0 px falls off Qt's fast stroker** — `QCosmeticStroker` only takes widths ≤ 1.0; `UNDERLAY_LINE_WIDTH_PX = 1.5` (and the "Light" hint's 1.08) routed every underlay stroke through the generic stroke pipeline: measured **22 ms vs 1 ms** for the same 94k-point drawing (user's clue: the import-dialog preview uses width-0 pens and zoomed lag-free). Fixed `a62f543`: default → 1.0, hints ≤ `UNDERLAY_FAST_PATH_SNAP_PX` (1.25) snap to 1.0, heavier user-chosen weights keep their look. Live paints 300–760 ms → **5.7–15.8 ms at every zoom**. (2) **Gesture freeze-blit** (`underlay_freeze.py`, spec §18): during wheel/pan the batched `_UnderlayPathItem`s skip paint while a transient viewport-sized pixmap blits (paint-only suppression — never setVisible; ~9 abort hooks guarantee instant Manager edits + vector-only plots, red-verified); tuned to real cadence (settle 100→**450 ms** — deviates from the grill's ≤150 ms crisp-restore, deliberate: 100 ms thrashed per-tick at the measured 0.3–3 s wheel gaps; pad 0.25, aliased capture → capture 71 ms). Gesture frames **2–5 ms**; 30-repaint gesture 3.6 s → **208 ms**. (3) **Latent multi-page bug exposed & fixed** (`46e37d6`): the interactive placement commit never persisted `params.pdf_page/pdf_dpi` into the record (two sibling sites did) — the Sleeman sheet-2 import recorded page 0, masked for months by cache hits; a mid-session cache invalidation (v5 bump, **reverted** `a2e2797`) re-extracted the cover sheet = "you broke the underlay". User's FPD repaired (page 0→1, backup `.FPD.bak-page0`). **Wrong theories buried by evidence:** poisoned-cache spatial filter (cache bbox == import_bounds exactly; the bounds-filter parity fixes `f0b1775` kept as hardening), per-layer item count (merging to 5 paths was *slower*), lossy extractor (it faithfully extracted the wrong page). **Kept from the original plan:** freeze module + 28 behavioral tests + vector-plot/snap/theme hard-constraint gates + `tools/perf_probe_underlay.py` (FPD arg + synthetic fallback). DeviceCoordinateCache and AA-toggling were correctly rejected at design time. `underlay_freeze.py` (new), `model_space.py`, `model_view.py`, `level_manager.py`, `paper_space.py`, `scene_io.py`, `constants.py`, `tools/perf_probe_underlay.py` [ref:underlay-workflow §16.3/§18] [type:Bug] [P1] [subject:CAD] [done:2026-08-30]

- [x] **PDF underlay text renders wrong on canvas (correct in import-dialog preview)** — shipped on `feat/pdf-underlay-text-fix` (2026-08-30, `/todo` grounded → TDD → smoke-confirmed). **The two `_append_geom_to_path` copies were NOT the drift** (verified byte-identical). Root cause: the import-transform loop bakes `import_scale` into a text span's `x/y/size` but **left `twidth` raw** (the source span width the substitute font is x-scaled to), so `sx = twidth/nat_w` broke relative to the vertical scale → text horizontally distorted by the import-scale factor at architectural scales; the preview renders raw geometry (no bake) so it stayed self-consistent. The transform loop was inlined **byte-identically in FOUR `model_space` sites** (`_commit_place_import`, `_on_dxf_finished`, `_import_pdf_vectors`, `_load_underlay_from_cache`), all with the same omission. Extracted one pure `apply_import_transform()` in `dwg_converter.py` (beside the other geom-dict operators), fixed `twidth` there once, routed all four sites through it. Tests: `tests/test_import_transform.py` — pure-function ground truth + a render-level guard (`apply_import_transform`→`_append_geom_to_path`) asserting placed text scales isotropically; both red-verified. **§17.4 de-dup follow-up partially discharged** (transform loop 4→1; the two `_append_geom_to_path` copies remain, still a filed follow-up). `dwg_converter.py`, `model_space.py` [ref:underlay-workflow §17.4] [type:Bug] [P1] [subject:CAD] [done:2026-08-30]

- [x] **PDF reload/refresh paths must be regression-tested against multi-page imports** — shipped on `feat/pdf-underlay-text-fix` (2026-08-30). `tests/test_underlay_page_persist.py`: a real 2-page fitz PDF (pages distinguished by geometry **Y-extent** — robust to PyMuPDF stroke merging) drives `_commit_place_import` with `params.pdf_page=1`/`pdf_dpi=300` and asserts (a) the `Underlay` record carries the page+dpi, (b) the geometry cache key is page-sensitive (page 0 ≠ page 1), (c) end-to-end the persisted `record.page` routes a cache-miss re-extraction back to the correct page, not the cover sheet. Red-verified: forcing the record back to page 0 fails commit + reload. `model_space.py`, `tests/test_underlay_page_persist.py` [ref:underlay-workflow §17.2] [type:Task] [P1] [subject:Testing] [done:2026-08-30]

- [x] **PDF import geometry inflation — PDF bézier flatten tolerance** (filed 2026-08-29 from the underlay-perf session; **SHIPPED on `feat/pdf-bezier-flatten-coarsen`, 2026-08-30**, grill→plan→TDD→live visual gate). `_flatten_bezier` (`pdf_import_worker.py`) over-tessellates béziers (Sleeman: 4,695 paths → ~94k points). **Outcome flipped mid-task:** instead of hardcoding a coarser value, exposed the tolerance as a live **Preferences → Import & Conversion → "Curve flatten tolerance"** knob (spinbox 0.25–4.0 pt) — `current_pdf_flatten_tol()` reads `import/pdf_bezier_flatten_tol` and feeds BOTH extraction AND the **PDF cache key** (`compute_cache_key(flatten_tol=…)`), so a change re-extracts on load/Manager-refresh. The planned global `_CACHE_VERSION` bump was **reverted** (tolerance-in-key invalidates PDF only; DXF caches stay valid). **Default kept at the original fine 0.5** — the Sleeman visual gate showed coarser defaults (2.0/1.5) facet when zoomed past plot scale, so no fidelity regression; coarsening is opt-in. Endpoints (p0/p3) always exact. `constants.py`, `pdf_import_worker.py`, `underlay_cache.py`, `underlay.py`, `model_space.py`, `preferences_dialog.py`, tests. [ref:underlay-workflow §18.5] [type:Task] [P1] [subject:CAD] [done:2026-08-30]

- [x] **Unify accent colour on the theme token + style-guide-align the status chrome** — shipped on `feat/unify-accent-theme-token` (2026-08-30, `/todo` grounded → grill → TDD → smoke-confirmed both themes). **(A)** `icons.token_map()` now derives the accent from the active theme variant (`theme.LIGHT.accent`/`theme.DARK.accent`); `ACCENT_GREEN`/`ACCENT_BLUE`/`_TOKENS` deleted, primary ink kept in a small `_PRIMARY` table. The SNAP/ALIGN pills, SNAP toolbar `:checked` border, and mode badge all read `theme.detect().accent` (shared `_pill_style`/`_mode_badge_style` helpers in `main.py`; the four rogue hexes — `#44FF88`, `#004CFF` icon-accent, and both `#44aaff` — retired). Light accent → `#2f9e63`, dark → `#63BE8B` (neon `#44FF88` gone). **(B)** scoped down at the mockup gate — user chose a **text-only** mode badge, so `select_icon.svg` was **not** authored (both candidates discarded); the badge is just retinted to the theme accent. Smoke fix: SNAP/ALIGN pills dropped their `#1a3a24` green fill to match the badge (transparent, accent text+border). Docs: `icon-style-guide.md §4.2` rewritten + frontmatter stamped; `ribbon-bar.md §accent` Rule-A drift fixed. Tests: `test_status_chrome_accent.py` (new) + `token_map` derivation/ground-truth/legacy-removed guards in `test_icon_theming.py`. Full-suite green-count masked by the pre-existing environmental print/VTK segfault (diff proven clean: no test observes the changed strings but the passing ones). `icons.py`, `main.py`, `docs/specs/icon-style-guide.md`, `docs/specs/ribbon-bar.md` [ref:icon-style-guide §4.2] [type:Task] [P2] [subject:UX] [done:2026-08-30]
  - **(shipped, for reference — original two-part scope):**
  - **(A) One accent source of truth.** `icons.py` hard-codes `ACCENT_GREEN=#44FF88` / `ACCENT_BLUE=#004CFF` for the two-token ribbon-icon recolour, and the SNAP/ALIGN status-bar pills import `ACCENT_GREEN` (`main.py`) — so pills + icon accents form one accent system NOT tied to the theme's `accent`/`selection` tokens (dark `#63BE8B`/`#8FE3B4`, light `#2f9e63`/`#1f7a49`, which drive ribbon-button selection, grip handles, selection rendering). Make **`theme.accent` the single source of truth**: delete `ACCENT_GREEN`/`ACCENT_BLUE`, have the icon recolour map (`ACCENT_SENTINEL` → display value) and the pills derive the accent from the active `HouseTheme` via `theme.detect()`, and rewrite `icon-style-guide.md §accent` (currently documents blue/green as canonical). **SVGs untouched** — recolour stays keyed on the `#004CFF` authoring sentinel; only the per-theme *display* value changes. **Visible consequence to gate:** light-theme icon/pill accents change from blue `#004CFF` → theme green `#2f9e63` (confirm at the visual gate; house rule: plotted/visual sign-off).
  - **(B) Bottom-left mode badge — add the Select icon + retire its rogue colour.** The active-mode badge (`mode_name_label`, `main.py:679`, defaults to text `"Select"`) is styled with a THIRD hardcoded off-palette colour `#44aaff`. Show the **active mode's icon** in that badge (the **Select / pointer icon** in Select mode — none exists yet, so **author `select_icon.svg`** per `icon-style-guide.md`, mockup-gated) and restyle the badge to the theme accent so it matches (A). Generalises to per-mode icons; Select is the default case.
  - Ripples: `icons.py`, `main.py` (pills + mode badge), `theme.py`, `firepro3d/graphics/Ribbon/select_icon.svg` (new), `docs/specs/icon-style-guide.md §accent + checklist`. [ref:icon-style-guide, theming] [type:Task] [P1] [subject:UX]

- [x] **Revise floor placement workflow (mirror wall) + floor elevation/thickness reference model** (user, 2026-08-27; **SHIPPED & smoke-tested** on `feat/floor-workflow-elevation-model`, grill→brainstorm→plan→subagent-driven build: 9 tasks + consolidated cleanup + whole-branch holistic review, per-task two-stage reviews, [done:2026-08-28]). **As-built:** two-boundary Z model (top ∈ {level+offset, absolute}, bottom ∈ {level+offset, absolute, thickness-below-top}; pure `_resolve_boundary_z`; ordered `z_range_mm`; `MIN_FLOOR_THICKNESS_MM` degenerate guard + allow-and-warn inversion). **Owning `.level` KILLED** (was going to be "derived owning level" — grill decided it's obsolete): floors use pure z-range visibility (`_visibility_by_zrange` carve-out in `_set_level_vis`); rename remaps `_top_level`/`_bottom_level`. **View-range upper bound** now derives from the actual floor above (`compute_view_height`) + `PlanView.view_height_explicit` user-override flag (folded-in "elegant fix"; scoped to on-screen plan — paper-viewport parity filed). **Holistic review caught the CRITICAL blocker** the green suite hid: `elevation_scene._project_floor_slabs` still used the retired single-datum model → fixed to resolve via `z_range_mm` (per-task reviews were blind to it). Placement mirrors wall (unified `floor` mode, ←/→ Corner/Center-Rect + Polygon, rect rotate step, `F`, one checkable button, HUD live via polygon `publish_placement_state`, template name seeds instances uniquified "Slab/Slab 1/Slab 2"). Panel = mode-conditional rows, no Colour (→ Display-Manager "Floor" category + new reusable **Graphic Override** contextual group), inversion warning. Template persists (QSettings). Icon C wired + **accent token reversed** (light=blue `#004CFF` / dark=green `#44FF88`, pill-synced). Full suite green (~3466 pass; only the pre-existing L72 3 failures). Bring `FloorSlab` placement up to the unified 2D-geometry dispatch the way `feat/wall-placement-workflow` did for walls, and redesign the floor's Z definition. **In scope (WHAT to be nailed in grill):** (1) **Workflow** — collapse the 2-item Floor dropdown → one checkable button; register `floor` into the schema-driven HUD dispatch (`_SCHEMA_FOR_MODE`/`_APPLIER_FOR_MODE`/`_PLACEMENT_VARIANTS`) with a `_floor_primitive` variant + `_press_floor_router`/`_move_floor_router` (mirrors `_press_wall_router`); **←/→ primitive cycle = Corner-Rect → Center-Rect → Polygon** (closed-polyline); typed dimensions via the HUD; `F` tool shortcut (verify free); ALIGN already enabled. (2) **Elevation/thickness reference model (the real design work, GAP)** — top of floor = **(reference level + offset) OR (absolute height)**; bottom of floor assembly = **(reference level + offset) OR (absolute thickness below top)**. New `FloorSlab` fields replacing the fixed `level`+`_level_offset_mm`+`_thickness_mm` single-level model; migration/back-compat for existing slabs; ripples to `z_range_mm`, `get_3d_mesh`, section-cut + occlusion z-range, and the property panel (per-boundary mode selector). Grill must settle field semantics, panel UX, and migration. **Folded in:** L256 backlog "Spec session: Floor elements (Z-range top/bottom)" (**superseded**); L105 floor part (wire `_build_placement_group` into a Floor contextual tab); L35 floor part (persist the floor template across sessions via QSettings, pipe/sprinkler/text pattern); **L121 floor icon** (author the Floor ribbon icon per `icon-style-guide.md`, mockup-gated → render candidates, user picks; L121 shrinks by one). **Approach:** grill (WHAT) → brainstorm/spec (elevation model + workflow) → plan → subagent build; heavy parity testing (posted-event, ground-truth, byte-identical serialization diffs) like the wall task. Governing spec `wall-room-floor-system.md §11` (current, no orphan gate); also touches `2d-geometry.md §4`, `ribbon-bar.md`, `view-relationships.md` (Z-model), `property-panel.md`. `floor_slab.py`, `model_space.py`, `level_manager.py`, `model_browser.py`, `property_manager.py`, `scene_io.py`, `main.py`, `model_view.py`, `elevation_scene.py`, `view_range_dialog.py`, `level_widget.py`, `icons.py`, `constants.py` [ref:wall-room-floor-system §11, 2d-geometry §4, ribbon-bar, property-panel, view-relationships, icon-style-guide] [type:Task] [P2] [subject:Architecture] [done:2026-08-28]
  - **Follow-ups (filed from the 2026-08-27/28 build + smoke test):**

- [x] **Paper-space bug bundle** (user, 2026-08-27; grill-lite via AskUserQuestion → per-bug TDD on `fix/paper-space-bugs`, each red-verified). Six fixes: **(1) paper-widget crash** — `_on_tab_close_requested` unconditionally `deleteLater()`'d the closed tab, destroying the persistent singleton `paper_space_widget` (re-added on demand, like the protected 3D tab) → next paper access `indexOf(deleted)` raised `RuntimeError: wrapped C/C++ object … deleted`, making paper space unreachable until restart; now guarded (removed-not-deleted). **(2) Drawn By / Checked By** surfaced as per-sheet Sheet Properties (mirror Rev/Date, undoable `SetSheetFieldCommand`; the `@[Drawn By]`/`@[Checked By]` title-block tokens already existed). **(3) rooms plot boundary + tag only** — `apply_paper_overrides` flags `Room` `_paper_no_fill`, `Room.paint` skips the fill in viewports (model canvas untouched). **(4) viewport properties in the panel** — `ViewportProperties` adapter (Title/Scale/Show Border/Position/read-only W-H) via shared `PaperScene.commit_viewport_edit`; supersedes `SheetViewPropertiesDialog` for editing (ribbon contextual tab = "both" other half, filed). **(5) model browser delete** — Delete key + context-menu action delegating to the canonical `delete_selected_items()` (one undo, underlay rows excluded); forged `docs/specs/model-browser.md` (orphan gate). **(6) stale view tabs on load/new** — `_apply_loaded_file`/`new_file` never dropped the prior project's Plan/Elevation/Detail tabs (a `Plan: Level 1` lingered opening a project with no Level 1) → new `_close_stale_view_tabs()`. Specs stamped: `paper-space.md` (§6.2/§19.4/§8.4), `property-panel.md` (§3.1 adapters), `model-browser.md` (new). `main.py`, `paper_space.py`, `paper_display.py`, `room.py`, `model_browser.py` [ref:paper-space §6.2/§19.4, property-panel §3.1, model-browser] [type:Bug] [P1] [subject:CAD] [done:2026-08-27]

- [x] **Update stale `test_ribbon_restructure.py::test_open_preferences_has_five_tabs`** (found 2026-08-30 during the underlay-perf full-suite verify; **pre-existing — branch touched neither preferences nor the test**). The Preferences → UI tab (System/Light/Dark theme selector, `7d92e6a` on the design-token branch) made it SIX tabs; the test still asserts five. Renamed → `test_open_preferences_has_six_tabs`, asserts `count() == 6` + `"UI"` present; also fixed the stale "all 5 panes" docstring in `main.py:_build_preferences_dialog`. `tests/test_ribbon_restructure.py`, `main.py` [type:Bug] [P3] [subject:Testing] [done:2026-09-02]

- [x] **ALIGN — acquire-and-track alignment (AutoCAD OTRACK-style), replaces inference/GUIDES** (user, 2026-08-26; grill→brainstorm→plan→subagent-driven build on `feat/align-tracking`, per-task + whole-branch holistic review, [done:2026-08-26]). Shipped the **A**cquire→**L**ock→**I**nfer→**G**uide→**N**avigate model: **acquire** (hover-dwell→green `+`, cap/release re-hover, per-placement reset); **tracking** = H/V + extension (any angle) + perpendicular rays; transient rays feed `SnapEngine.find(align_paths=…)` at ALIGN priority 20 (`align_intersection`) / 30 (`align_path`) into the **one** existing priority-band picker (below real snaps, own aperture); path×path & path×geometry **intersection snaps**; **distance-along-path** typing via the `track` HUD schema (field labelled "L"); the active anchor **auto-acquires** and inherits its object's direction (extend end-to-end at an angle). **5 Preferences SNAP-pane knobs** — path-tol (`ALIGN_PATH_TOL_PX=20`) / dwell / max-acquired-points / per-direction toggles (incl. Perpendicular) / master **F11** — with full rename inference→ALIGN + F11 shortcut + `align/*` QSettings migration. New modules `align_engine.py` (pure geometry, no Qt) / `align_controller.py` (acquire state machine). Holistic review caught the **universal-scope blocker** (ALIGN was dead in 8 of 12 placement modes → `_ALIGN_PLACEMENT_MODES`). **Direction-Parallel defaulted OFF** (`ALIGN_DIR_PARALLEL_DEFAULT=False`) pending the perpendicular-OFFSET follow-up. Governing spec `docs/specs/align-placement.md`. `align_engine.py, align_controller.py, snap_engine.py, dynamic_input.py, model_space.py, model_view.py, preferences_dialog.py, main.py, constants.py, gridline.py, wall.py` [ref:align-placement] [type:Task] [P1] [subject:CAD]

- [x] **SNAP refinement — pixel-constant aperture across zoom + tunable settings + OSNAP→"SNAP" rename** (user, 2026-08-25→26; **SHIPPED & smoke-tested** on `feat/snap-refinement`, grill→plan→subagent-driven build, per-task two-stage + whole-branch holistic review; [done:2026-08-26]). **⭐ ROOT CAUSE found during smoke-testing (dominated the session, pre-existing):** the model scene has **N+1 views** — a vestigial, never-shown `MainWindow.view` frozen at m11=1.0 (always `views()[0]`) plus one `Model_View` per open plan tab (only the active tab visible). Every zoom-dependent tolerance read `self.views()[0].transform().m11()` = the frozen 1.0, so the snap aperture (and design-area pick, inference band, array/offset/dimension/rotate previews) collapsed to a **fixed SCENE distance regardless of zoom** — the entire pixel-aperture design was silently neutralized; snapping was world-unit ("huge jumps zoomed in, insensitive zoomed out") the whole time. **Found by instrumenting the LIVE app** (code-reasoning kept disagreeing with the user's ruler until a debug readout showed `find-transform m11=1.0` while the visible view's m11 climbed with zoom). Fixed `a4affd8`: `Model_Space._snap_view()`/`_active_view_scale()` read the **visible** plan view (fallback = last-attached, never the vestigial index-0); routed all 14 view-reads through them. Shipped aperture default **15px** (user-tuned live). **Reported symptom:** snap aperture feels huge when zoomed in and dead when zoomed out. **Diagnosis (verified in code — CORRECTED mid-grill):** the ONLY *live* snap path is `SnapEngine.find`; both symptoms trace to it alone — (1) it **already** converts `SNAP_TOLERANCE_PX=40 / view.m11()` per mouse-move (pixel-constant intent, spec §3.1), but the `SNAP_MAX_SCENE_TOL=200mm` cap clips the aperture at architectural zoom-out (<~0.2 px/mm → ~4px effective) = **"dead when zoomed out"**; and (2) the 40px default is 4× AutoCAD's ~10px so a fixed grab lunges during precise work = **"huge jumps when zoomed in"** (`_SnapCtx.check` judges in scene units — equivalent to px only while the conversion holds). The underlay `find_snap_point` (`:3876`) has a fixed world-unit tol and IS reachable — driven by the **"Snap to Underlay"** ribbon toggle (`main.py:1441`, sets `_snap_to_underlay`, default OFF); when a DXF-over drafter enables it, the world-unit bug goes live (a real second contributor to "huge jumps"). (Earlier "dead code" read was wrong — the UI lambda lives in `main.py` at repo root, outside a `firepro3d/`-scoped grep.) **Decision:** REROUTE the "Snap to Underlay" toggle through `SnapEngine` restricted to underlay sources (pixel-correct + inherits hysteresis), delete the world-unit `find_snap_point` — preserve the feature, one brain. **Contract (FINAL as-built):** **constant-pixel aperture** (15px default, tunable) — NOT zoom-adaptive. Mid-smoke-test we briefly explored a zoom-adaptive `clamp(K/scale, MIN=20, MAX=200)` model (user initially thought they wanted ~200px zoomed out) and a partial adaptive build even landed in the working tree before being reverted; but the user reasoned back to constant-pixel (a *bigger* aperture zoomed out worsens candidate crowding and breaks the zoom-in-to-disambiguate escape hatch — the real zoomed-out pain is disambiguation, not aperture size). Then the true culprit surfaced (the vestigial-view bug above): the aperture had never actually *been* pixel-constant in the running app, so nothing felt right regardless of the model. Once the visible-view read was fixed, constant-15px felt correct at every zoom. Adaptive was **not** shipped. (Disambiguation follow-ups — in-aperture candidate cycling + tooltip — filed separately.) **In scope (decided 2026-08-25):** (a) **fix the cap** — search rect derived from the pixel aperture, perf ceiling raised far past the working range (only clamps below the zoom where features are distinguishable; phase-4 256-seg bail unchanged), and **judge acceptance in true pixels** (`d_scene × scale ≤ APERTURE_PX`); (b) **one object-snap brain** — reroute the "Snap to Underlay" toggle (`main.py:1441`, `_snap_to_underlay`) through `SnapEngine` restricted to underlay sources + delete the world-unit `find_snap_point` (preserve the feature, pixel-correct); **route design-area pick through `SnapEngine`** via a new candidate-whitelist API (`only_types={'center'}` + item filter) so it inherits the aperture+hysteresis (existing 54 design-area tests stay green, pick stays sprinkler-centers-only); **one shared pixel→scene conversion helper** consumed by engine + design-area + inference (kills copy-pasted `X_PX/scale`); inference stays its own subsystem (composition filed as OTRACK follow-up); (c) **hysteresis** — tunable ~3px margin, hold current snap until another beats it by margin OR cursor leaves aperture, **higher-priority always breaks the hold** (recall-first, spec §1.2), strict WYSIWYG (marker==commit), reset on commit/mode-change/Esc/miss; (d) **default aperture 40→20px**, tunable; (e) **settings** in Preferences → SNAP pane (REUSE `SnappingPane`): relabel Snap-radius=aperture + new **hysteresis px** spin (cap stays advanced/non-UI), net-new **"Reset to defaults"** button (resets whole SNAP pane to factory + live-applies), **Apply** button for the tuning loop (not full non-modal), **all knobs persist to `snap/*` + restore on startup** (persistence REQUIRED — extends existing `SNAP_TOLERANCE_PX` persist); (f) **full rename OSNAP→"SNAP"** — UI labels + code identifiers (`osnapToggled`→`snapToggled`, `toggle_osnap`→`toggle_snap`, `_osnap_enabled`, ~58 refs/7 files) + docs; **QSettings keys already `snap/*` → NO migration**; tooltip reads **"Select Nearest Anchor Point (SNAP)"**; **preserve comparative AutoCAD "OSNAP" references in spec prose**; rename spec file `osnap-toolbar.md`→`snap-toolbar.md` + fix SPEC-INDEX/`[ref:]`. **Already correct (no work):** priority-before-distance (`SNAP_PRIORITY` + band). **N/A:** adaptive grid snap (gridlines are objects, spec §2.3); no real-PDF gate (on-screen interaction, not print). **Acceptance:** headless `SnapEngine.find` zoom-invariance test at scale 0.02/1.0/10.0 (caught within N px, missed just beyond, identically) is the HARD gate; stateful hysteresis-sequence tests; per-field settings tests (each knob applies live + round-trips QSettings + Reset restores factory — the 1-of-6 lesson); parity-review the retired code; **user interactive smoke over a real imported DXF at zoomed-in+out is the feel sign-off**. Opportunistic: §13.3 near-miss logging hook if cheap. Governing specs: `snapping-engine.md` (§3.1/§6.1), `snap-toolbar.md` (renamed); Preferences dialog orphan (temp-governed by `ribbon-bar.md §3.4`). `snap_engine.py`, `model_space.py`, `design_area.py`, `preferences_dialog.py`, `main.py`, `ribbon_bar.py`, `constants.py`, `inference_engine.py`, `docs/specs/snapping-engine.md`, `docs/specs/snap-toolbar.md` [ref:snapping-engine §3.1/§6.1, snap-toolbar] [type:Task] [P2] [subject:CAD]

- [x] **Inference refinement — right-size the alignment/OTRACK guide band + make it Preferences-tunable** **SUBSUMED by ALIGN [done:2026-08-26]:** the band right-size shipped as `ALIGN_PATH_TOL_PX=20` (was 65) with its own Preferences knob + the visible-view pixel read; the whole subsystem was superseded by the ALIGN acquire-and-track redesign. (user, 2026-08-26 SNAP smoke test). The inference/OTRACK alignment-guide tolerance feels **too large during Move** (and other placements): `INFERENCE_TOL_PX = 65` (`constants.py`) was sized when the snap aperture was 40px, but the vestigial-view fix (`a4affd8`, 2026-08-26) made inference **correctly pixel-based** — it too had been reading the frozen `views()[0]` (m11=1.0) so it was silently scene-based — and the snap aperture default is now **15px**, making 65px ~4× the aperture and grabby. **In scope:** (1) **right-size the default** (~30px — still wider than snap so alignment is easy to catch, but not grabby); (2) **add editable Preferences (SNAP pane) settings to dial it in** — a tunable + persisted **"Alignment guide tolerance"** spin mirroring the aperture/hysteresis knobs (live-apply + `snap/*` persistence + covered by Reset-to-Defaults); group any future inference knobs (guide types, acquire/tracking) here too. Relates to the filed OTRACK follow-ups (*compose inference guides as transient SNAP sources*; *inference at range: acquire-based tracking + band queries*). `constants.py`, `inference_engine.py`, `model_space.py`, `preferences_dialog.py`, `main.py`, `docs/specs/inferred-dimension-driven-placement.md` [type:Task] [P1] [subject:CAD]

- [x] **Revise wall placement workflow — unify with 2D-geometry drawing + true centerline** (user, 2026-08-24 grill→brainstorm→plan→subagent-driven build; **SHIPPED & smoke-tested** on `feat/wall-placement-workflow`, ~15 commits, per-task two-stage review + whole-branch holistic review; governing specs reconciled + stamped `verified-commit eead762` 2026-08-25). **As-built extras from the smoke tests (beyond the original locked scope):** Rectangle got the **full** workflow — Corner **and** Center variants + a **rotate step** (←/→ now cycles all four: Line→Polyline→Corner Rect→Center Rect; Q11's "keep Wall-Rect simple" deferral was pulled into scope); **Ctrl** angle-snap fixed to work on **either** endpoint (was pt1-only); **HUD rect-angle now live-seeds** during the rotate step (root cause: `_transform_seed_values` read the 2D-geo `_draw_rect_pivot`, `None` for walls → frozen 0° — added a `wall` branch); inference-at-range (OTRACK acquire-tracking + band-queries) filed as a P2 follow-up. Holistic review caught the `get_placement_anchor` rect-anchor blocker (HUD couldn't open for typed rect) that per-task reviews couldn't see. [done:2026-08-25] Make wall placement a first-class client of the unified 2D-geometry placement dispatch (`2d-geometry.md §4`) so future drawn-primitive updates flow into walls; add a true wall centerline so hosted features stop referencing the alignment/click line. **In scope:** (1) collapse the 2-item wall dropdown → **one** checkable button, default **Line**, dropdown deleted; (2) **"W"** tool shortcut (fires-always on canvas focus + no engaged HUD, like L/R/C/A) in `_TOOL_SHORTCUTS`; (3) **←/→ primitive cycle** Line→Polyline→Rectangle (built primitives only, no inert entries; Wall-Rect = existing axis-aligned 2-corner 4-wall builder); (4) **Spacebar → wall alignment** Center→Left→Right (sole binding; Left-Shift retired for walls; matches opening convention); (5) **↑/↓ reserved** for future polygon-walls; (6) **true wall centerline** = derived accessor on `Wall` (from `_pt1/_pt2`+alignment+thickness, **no serialization change**); (7) repoint hosted features to the centerline — `wall_opening.center_on_wall` (:282), 3D (:744), section (`elevation_scene.py:893`) — existing openings on Left/Right walls self-correct to true center on load (accepted, no migration); (8) **walls as full inference participants** — provide H/V refs (endpoints+midpoint+**centerline** endpoints) AND consume H/V guides during placement, reusing the existing engine, **spatial-filtered** (perf rule); (9) shared-machinery contract = wall registers into the dispatch surface, per-primitive **wall appliers** produce `WallSegment`(s) (not zero-wall-code coupling). **Acceptance (posted-event, real shown view; functional; ground-truth; parity-diffed vs old `_press_wall`/`_press_wall_rect`):** W→wall mode · ←/→ cycles Line→Polyline→Rect · Spacebar cycles alignment · Left/Right wall exposes true centerline at geometric center · opening on Left/Right wall renders at true center · Center wall+opening round-trips byte-identical · wall provides+consumes H/V inference. Extends `wall-room-floor-system.md`, `inferred-dimension-driven-placement.md §4.7/§5`, `2d-geometry.md §4`, `ribbon-bar.md` (updated in place at wrap-up; no orphan gate). `main.py`, `ribbon_bar.py`, `model_space.py`, `model_view.py`, `wall.py`, `wall_opening.py`, `elevation_scene.py`, `view_3d.py`, `snap_engine.py`, `inference_engine.py` [type:Task] [P2] [subject:Architecture]
  - **Follow-ups (filed from the 2026-08-24 grill):**
    - [x] **Angled Wall-Parallel / Wall-Perpendicular inference guides + distance labels** **Mostly DONE via ALIGN [done:2026-08-26]:** angled tracking now works at any angle — Extension (parallel-to-object, collinear) and Perpendicular (across, dir rotated 90°) rays from acquired directional endpoints. Remaining minor follow-up: per-guide distance LABELS on the tracking vectors (filed nowhere else — note it here or leave as the one open sub-item). — extend `inference_engine.py` beyond H/V to emit non-axis-aligned guides (spec `inferred-dimension-driven-placement.md §5.1/§5.5`, orange). General engine upgrade, not wall-specific. `inference_engine.py`, `model_space.py`, `model_view.py` [type:Task] [P3] [subject:Architecture]
    - [x] **Inference at range: acquire-based tracking (AutoCAD OTRACK) + band queries** **DONE via ALIGN [done:2026-08-26]:** the acquire+track model shipped (dwell→acquire→infinite tracking rays); the box-filter band-query concern is moot for acquired rays (they're infinite lines, not proximity-boxed). (user smoke-test 2026-08-24) — today wall inference uses a BOX spatial filter (±tol around the cursor), which is semantically too tight for H/V alignment: to line up with a far reference you match one coordinate while the other is far, so the box excludes it → guides only fire when near the reference point. Two fixes: (a) **band queries** — query two thin scene-spanning strips (cursor.x±tol full-Y, cursor.y±tol full-X) instead of a box, so far-but-aligned refs are found while staying spatially bounded; (b) **acquire-based tracking** — hover to "acquire" a reference (AutoCAD OTRACK `+` marker), then only acquired points emit infinite tracking guides (infinite extent, bounded perf, intentional). Revit does automatic proximity alignment; AutoCAD does acquire+track. `inference_engine.py`, `model_space.py`, `model_view.py` [type:Task] [P2] [subject:Architecture]
    - [x] **Compose inference guides as transient SNAP sources in the picker** **DONE via ALIGN (design decision D1):** transient tracking rays feed `SnapEngine.find(align_paths=…)` as candidates in the one priority-band picker. [done:2026-08-26] (from the 2026-08-25 SNAP-refinement grill — the "use them together, don't merge" end-state) — today `get_effective_position` runs OSNAP→underlay→inference as three *separate* priority tiers; the clean architecture is to feed inference guides (H/V + future angled/tracking lines) into `SnapEngine`'s picker as **transient `intersection` candidates** so a guide×geometry crossing and a guide×guide crossing are ranked in the *one* priority scheme alongside real endpoints/mids (exactly how AutoCAD composes OTRACK tracking-lines with OSNAP). Deliberately deferred out of the SNAP-refinement task (it redesigns the resolution pipeline + crosses the inference governing spec); belongs here with the OTRACK work. Depends on the SNAP-refinement task landing first (one object-snap brain + shared pixel→scene conversion helper). `inference_engine.py`, `snap_engine.py`, `model_space.py` [ref:snapping-engine, inferred-dimension-driven-placement] [type:Task] [P2] [subject:Architecture]

- [x] **Opening element (Phase A) — first-class, Feature-based architecture element (doors + windows + blank, wall-hosted)** (user, 2026-08-23 grill→plan→subagent-driven build; **SHIPPED & smoke-tested** on `feat/opening-feature-element`, ~40 commits, per-task two-stage review + holistic review; spec `docs/specs/wall-room-floor-system.md §7.1–7.17` stamped as-built 2026-08-24; full suite green). MVP: Door/Window/Blank host on walls; active placement mode with **Spacebar** cross-wall alignment (Centered/Flush-front/Flush-back) + continuous cross-wall offset (proud allowed, sign-flips on facing mirror), **←/→ hinge** mirror, **↑/↓ facing** mirror; each opening has a **plan schematic**, a **section/elevation schematic** (NEW — not projected today), a **3D representation** (hole + closed leaf), and an **opening size**; level default-from-wall-base but assignable. Built on the new **Feature** data model (Feature > Category > Type, `host_type`); 3 seed door Features (813/914/1829). Reuse sweep found a real-but-partial `wall_opening.py` (superseded model, §7.17) governed by §7. Decisions (2026-08-23 grill): **first-principles redesign**, **spec home = expand §7** (no new file), **Hybrid** first-class (dies with wall), representations = authored Feature-owned artifacts (parametric-by-scale), **As-built refinements** (from smoke tests, see spec §7 2026-08-24 impl note): opening z-order pinned above host wall so the plan gap cuts; gap fill = scene-bg on screen / paper-white on sheets; door swing arc spans the opening; **frame is fixed-depth** (only the cut matches wall depth); 3D built from `quad_points` (perpendicular jambs on joined walls) + **watertight** capped reveals; openings use the **Wall paper category**; **pre-placement property template** (sill/size/orientation, QSettings `template/opening`); openings ride the existing snapshot undo (no new mechanism). New modules `feature.py`, `feature_browser.py`. `wall_opening.py`, `wall.py`, `elevation_scene.py`, `view_3d.py`, `model_space.py`, `main.py`, `display_manager.py`, `level_manager.py`, `paper_display.py`, `scene_io.py`, `constants.py`, `docs/specs/wall-room-floor-system.md §7` [ref:wall-room-floor-system §7] [type:Task] [P2] [subject:Architecture] [done:2026-08-24]
  - **Follow-ups / phasing (filed from the 2026-08-23 grill; spec §7.16):**
    - _(retracted: "arch elements in the undo network" — mis-read; `_capture_network`/`_restore_network` already handle walls+openings+floors+roofs+rooms. Real Phase-A work is just ensuring every opening mutation path calls `push_undo_state()`, §7.11.)_

- [x] **2D geometry: first-class level-plane placement + fill (foundation for 3D extrude)** (user, 2026-08-22 grill→brainstorm→plan→subagent-driven build; branch `feat/geo2d-level-fill`, 15 commits, per-task two-stage review + controller verification; spec `docs/superpowers/specs/2026-08-22-2d-geometry-level-plane-placement-fill-design.md`). **Shipped & smoke-tested; full suite 3049 passed.** **Phase A — placement:** new `Geometry2DMixin` (over `DisplayableItemMixin`) on the 5 draw classes (line/polyline/rect/circle/arc; **not** gridlines) collapses per-class duplication and adds `_level_offset_mm` (mirrors `FloorSlab`, default 0, +up) + `z_range_mm()` → items sit at `level.elevation + offset` with full view-range participation + elevation-based z-ordering. New **`Z_CAT_CONSTRUCTION`** (= `Z_CAT_NODE + 0.1`) → 2D geometry **wins** over building geometry at equal elevation, below annotation/symbol/design bands. Panel: Level + Level Offset + read-only Elevation; offset joins the session `GeometryTemplate`. **Phase B — fill as item property:** closed shapes gain `fill`=None/Solid/Hatch(any `PATTERN_NAMES`)+colour, drawn in the item's own `paint()` via new `draw_fill()` (shares `_apply_hatch_pattern` with `draw_section_hatch`; fixes the rotated-rect hatch bug); **editable per-item Fill Opacity** (default 45%, solid only; hatch lines crisp); filled=interior-clickable. New **"2D Geometry" Display-Manager category** (resolves the filed model-side category follow-up). **Retired `HatchItem`** (legacy `hatch` → filled closed polyline on load; solid→Solid, diagonal→diagonal, cross→cross_hatch) and **`ConstructionLine`** (full removal, silent-drop legacy, `K` shortcut gone, zero-length bug moot). **Surfaces:** Placement+Fill groups on the **2D Geometry contextual ribbon tab** (`geo2d`; reusable `_build_placement_group`) + Fill right-click submenu (both `Model_View` menu paths) + panel. **Deferred (filed below):** vertical/"elevation plane" (section) anchoring, elevation-view projection, 3D render/extrude. Folded into `view-relationships.md §3.3/§7.3` + `ribbon-bar.md §3.8`. `construction_geometry.py`, `displayable_item.py`, `annotations.py`, `model_space.py`, `scene_tools.py`, `level_manager.py`, `display_manager.py`, `scene_io.py`, `snap_engine.py`, `paper_display.py`, `model_view.py`, `entity_context_menu.py`, `ribbon_bar.py`, `main.py`, `constants.py` [ref:view-relationships §3.3/§7.3, ribbon-bar §3.8] [type:Task] [P2] [subject:Architecture] [done:2026-08-22]
  - **Follow-ups (from the 2026-08-22 build):**
  - **2D-geometry polish cluster (user, 2026-08-24 grill→brainstorm→plan→subagent-driven build; branch `feat/geo2d-polish-cluster`, ~24 commits, per-task two-stage review + holistic seams review + smoke test; spec `docs/superpowers/specs/2026-08-24-2d-geometry-polish-cluster-design.md`). **SHIPPED & smoke-tested.** Governance: promoted the scattered 2D-geometry contract into the new governing spec `docs/specs/2d-geometry.md` (+ SPEC-INDEX row) — this **retires backlog "Spec session: construction geometry system"** below. Full suite green (chunked). Notable catches from the review gates: a real `_MOVE_DISPATCH` runtime gap (ghost/HUD didn't track the cursor), 3 latent T1/T4 regressions surfaced by the first full-suite run (hatch-migration closure, schema-registry guards), a whole "parallel-list" bug class (polygon missing from level-visibility / display-category / level-rename / 3D collectors), and a Y-up rotation-convention bug (polygon rotated opposite the cursor) caught by a ground-truth test.**
    - [x] **Closed-polyline placement (click start to close)** — `PolylineItem` now uses an explicit `_closed` flag (FloorSlab model, no duplicate vertex → single shared start/end grip moving both segments); click the start vertex (≥3) to close with a **distinct blue close-ring cue**; double-click/Enter finish open; **Delete pops the last vertex** (routed via a `Model_View` ShortcutOverride so it beats the window Delete shortcut). Back-compat: legacy coincident-vertex polylines + `scene_io` hatch-migration → flagged closed, fill preserved. `construction_geometry.py`, `model_space.py`, `model_view.py`, `scene_io.py`, `scene_tools.py` [type:Task] [P2] [subject:CAD] [done:2026-08-24]
    - [x] **Regular-polygon tool (parametric)** — new **`RegularPolygonItem`** (Geometry2D: level/offset/fill/"2D Geometry" category) storing centre/#sides/radius/rotation/inscribed, deriving vertices; **Y-up** rotation (CCW+, mutual-inverse vertices/apply_grip). **3-step placement centre→radius→rotate** (mirrors centre-rectangle) with **↑/↓** sides + **←/→** inscribed/circumscribed live at every step, a dashed **reference circle** (placement + when selected), a live-seeding **HUD Angle** at the rotate step, and **P** shortcut. Panel edit (Sides/Radius/Rotation/Shape) + centre/regular-preserving vertex grips. Snap: vertices + edge-mids + centre + edges (intersection/nearest/perp). Dual-path serialization + paste + all level/display/3D collectors. Contextual "2D Geometry" tab + entity menu. `construction_geometry.py`, `model_space.py`, `model_view.py`, `dynamic_input.py`, `snap_engine.py`, `scene_io.py`, `scene_tools.py`, `level_manager.py`, `level_widget.py`, `display_manager.py`, `view_3d.py`, `main.py` [type:Task] [P2] [subject:CAD] [done:2026-08-24]
    - [x] **Rename Create→"Geometry" ribbon group to "2D Geometry" + remove the "Single Place" toggle** — group renamed, Polygon button added; `single_place_mode` attribute + ~19 `if single_place_mode:` branches removed (always continuous; Esc exits); ~5 coupled tests repurposed. `main.py`, `model_space.py` [type:Task] [P2] [subject:UX] [done:2026-08-24]

- [x] **Ribbon overhaul — workflow-ordered restructure + contextual tabs + unified Preferences + icon style-guide** (user, 2026-08-22 grill→brainstorm→plan→subagent-driven build; branch `feat/ribbon-overhaul`, ~19 commits, two-stage reviews per task + holistic seams review; extends `docs/specs/ribbon-bar.md`, resolves D8/D2/D6). **Shipped & smoke-tested.** Reshaped the ribbon from 6 fixed tabs to **7 base tabs + on-demand contextual tabs**, folded scattered settings into one tabbed **Preferences** dialog (`preferences_dialog.py`, 5 panes, live-apply + snapshot/revert), forged the **icon style-guide** spec + a themed two-token icon loader (`icons.py`/`svg_utils.py`, missing-icon fallback), and audited out dead/duplicate buttons. Full suite green (2956). Smoke-test tweak: underlay import moved to **View → Underlay** ("Underlay Manager"). Spec `docs/specs/ribbon-bar.md` rewritten as-built (§3.4/§3.8/§3.9). WHAT locked in grill:
  - **Base tabs (in order):** **Manage** (File · Import · Preferences button · Snap quick-toggles · always-visible Undo/Redo) → **View** (Fit to Screen · Display Manager · Panels dock-toggles) → **Create** (2D geometry + Blocks; generic 3D solids = follow-up) → **Architecture** (renamed Build: Wall/Floor/Roof/Room/Door/Window/Detail + **Levels + Gridline** datums) → **Sprinkler Systems** (Pipe/Sprinkler/Water Supply/Design Area · Auto-Populate/Coverage/Sprinkler Manager · **Hydraulics** Run/Clear/Equiv + Export PDF/CSV, moved from Analyze) → **Analyze** (Thermal Radiation only) → **Draft** (model annotations Dimension/Text/Hatch moved from Draw + sheet Add Text · Font · Page · Plot; Workspace group deleted — Browser handles switching).
  - **Contextual tabs (mechanism this task, tools = follow-up):** Modify tab removed as a base tab; contextual tabs appear + auto-activate on `scene.selectionChanged`, disappear on deselect. Taxonomy: single **2D Geometry** / **3D Geometry** / **Annotation** tabs; per-sub-type **Wall/Floor/Roof/Room/Opening/Detail**, **Pipe/Sprinkler/Water Supply/Design Area**, **Gridline/Level**, **Viewport/Sheet Text**; mixed-family selection → generic **Modify** tab. Naming = bare type name. Each stub ships a shared universal **Edit** group (Delete/Copy/Cut/Paste/Duplicate); type-specific tools deferred. Legacy Modify→Text group (D2) dropped (property panel covers it); Draft→Font left as-is (migrates to a Sheet Text contextual tab later).
  - **Unified Preferences dialog** (Manage): tabs **Snapping · Units & Precision · Import/Conversion · General · Project Info**. Mixed persistence — Project tab → project `_project_info` dict + title block; the rest → QSettings. New settings look here first. Absorbs Snap Settings + Snap Tolerance + Units/Precision menus + ODA path + Project Info. Display Manager stays standalone (on View); Import Underlay dialog + templates stay as workflow surfaces.
  - **Icons:** forge an **icon style-guide spec** (orphan — no governing spec today) + build a **themed (light/dark) icon loader** with a real missing-icon fallback (generalize `display_manager._recolor_svg_bytes`). Authoring the ~47 placeholder icons = follow-up (mockup-gated).
  - **Audit removals:** Manage Export stub (D6), dead `FSVisibilityDialog`, duplicate Build "Display" button, legacy Modify→Text group — no functional command lost (all reachable via shortcut/right-click/panel/Browser).
  - **Absorbs earlier filed TODOs:** "Move Annotations group Draw→Draft" (§B), "Contextual Modify tab (D8)" (§B). `main.py`, `ribbon_bar.py`, `preferences_dialog.py`, `icons.py`, `svg_utils.py`, `docs/specs/ribbon-bar.md`, `docs/specs/icon-style-guide.md` [ref:ribbon-bar-spec D8] [type:Task] [P2] [subject:UX] [done:2026-08-22]
  - **Follow-ups (filed from the 2026-08-22 build + reviews):**

- [x] **Project Info address lines** (user, 2026-08-04) — Project Information table: City + State/Province replaced by free-form **Address Line 1/2/3** and new **Client Address Line 1/2/3** rows. `PROJECT_STD_KEYS` regenerated (token keys = new display names, picker auto-follows); `migrate_project_info` in `titleblock_template.py` (one-way, idempotent: address→address1, city+state join→address2; new keys win; wired into the scene_io load path); legacy tokens stay renderable via `PROJECT_TOKEN_ALIASES` (@[Address] mirrors Line 1 — seeded default untouched; @[City]/@[State] known-but-empty); `_SAMPLE_VALUES` mirrors the new keyset (DD-13 parity test guards it); HC-report address join reads the new keys. 9 new tests. `main.py`, `paper_space.py`, `titleblock_template.py`, `titleblock_editor.py`, `scene_io.py`, `hydraulic_report.py` [ref:titleblock-template-system DD-9] [type:Task] [P2] [subject:UX] [done:2026-08-04]

- [x] **Bug: gridline input table doesn't normalize dimension input** (user-reported 2026-07-14) — grew into four fixes via smoke test: **(1) Normalization:** Offset/Spacing/Length columns now edit through a shared `DimensionDelegate` (`dimension_edit.py` — generalized from `level_widget.py`'s private `_ElevationDelegate`, both consumers share it) whose cell editor is the canonical `DimensionEdit`; commit reformats to project units, exact mm lives in the `UserRole+1` sort slot (readers prefer it over re-parsing quantized display text), invalid input reverts. Default Length + Quick Fill Spacing became `DimensionEdit`s; Angle cells parse-or-revert (junk no longer raises inside a Qt slot). **(2) Tab-commit:** Qt calls `setModelData` *before* `editingFinished` on Tab-to-next-cell, silently reverting the typed value — delegate now calls new `DimensionEdit.commit()` first. **(3) In-dialog undo/redo:** per-tab snapshot stack, Ctrl+Z/Y (+Ctrl+Shift+Z) via `keyPressEvent` (open cell editors keep native text undo); one step per user action incl. offset↔spacing sync effects; populate = baseline; backing refs survive restores. **(4) Length grew 6% per open→OK** (+ angled-gridline offset drift): populate read raw endpoint distance incl. the bubble overshoot; now inverts the construction exactly via shared `GRIDLINE_BUBBLE_OVERSHOOT_FRAC` (`constants.py`; was a duplicated `0.06` literal with a stale "2%" docstring). 32 new tests (`tests/test_grid_dialog_dimension_input.py`, delegate-path functional, red-verified); 1434 non-3D + 75 3D green. Spec §7.3/§7.3.2/§7.6 as-built; property-panel §3.8 delegate contract. `grid_lines_dialog.py`, `dimension_edit.py`, `level_widget.py`, `model_space.py`, `constants.py` [type:Bug] [P2] [subject:CAD] [done:2026-07-16]

- [x] **Gridline Revit-aligned UX re-architecture** (user, 2026-08-12 grill+brainstorm; branch `feat/gridline-revit-ux`; 26 commits, subagent-driven with two-stage reviews + 2 smoke rounds). Replaced the modal Grid Lines dialog with **on-canvas placement** (Draw-tab Gridline tool riding the Line-tool handlers one-for-one: click/click, Ctrl-constrain, Tab exact input) + **Properties-panel geometry editing**. New **parametric data model** (`_origin/_length/_angle_deg/_bubble1_offset/_bubble2_offset` + single `_rebuild_geometry` writer; `line()` kept in sync so snap/spacing/elevation readers untouched); breaking `.fpd` schema (origin/length/angle/offsets) with legacy p1/p2 migration in both serialization paths. **Grip drag** fixed to extend/shorten along-line (opposite end fixed); grips adopt the house selection style (white + `SELECTION_OUTLINE_COLOR`). **Angled spacing** pairs by true parallelism (angle-mod-π), fixed perpendicular sign. **Bubble offset** = absolute per-end standoff (default `GRIDLINE_BUBBLE_OFFSET_MM=1000`), retiring the length-proportional overshoot. **Dash-dot**: model-absolute on screen (scales with zoom, 600/300/60 mm), fixed on-paper mm on the PDF (6/3/1, no viewport-scale collapse). **Naming flipped** (vertical→numbers, horizontal→letters, matches seed; counter synced before placement). **Y-up** panel display + `-0` normalize; double-click selects; Ctrl+Z-wipes-seed startup fix. Dialog + `apply_grid_dialog` + tests deleted; `DEFAULT_GRIDLINE_SPACING_IN/_LENGTH_IN`→`_MM`; `GRIDLINE_BUBBLE_OVERSHOOT_FRAC` retired. ~130 new/changed gridline tests; full suite green. Spec `docs/specs/grid-system.md` rewritten as-built. `gridline.py`, `model_space.py`, `property_manager.py`, `model_view.py`, `paper_display.py`, `scene_io.py`, `constants.py`, `ribbon_bar.py`, `main.py` [ref:grid-system] [type:Task] [P2] [subject:CAD] [done:2026-08-13]
  - **Follow-ups (from the 2026-08-12/13 build + smoke tests):**
    - [x] **Gridline placement alignment snapping** — shipped on `feat/gridline-inference-array-offset` (2026-08-13/14 grill→brainstorm→plan→subagent build; 6 smoke rounds) as the **first slice of a generic inference engine** (`inference_engine.py`, entity-agnostic). During `draw_gridline` placement (both points) **and endpoint grip-drag**, the active point H/V-snaps to any grabbable point (endpoints + bubble centers) of other gridlines via a duck-typed `alignment_reference_points()`; guide-intersection + single-guide snap; cyan dashed guide + crosshair glyph in `drawForeground`; priority `OSNAP > guide-intersection > single-guide > free`; own toggle (snap-dialog Inference tab + GUIDES pill + F12 + QSettings); `INFERENCE_TOL_PX=65`. `get_effective_position` hooks the engine after OSNAP miss; `_collect_alignment_refs` iterates `self._gridlines`. Governing spec `inferred-dimension-driven-placement.md` → status:partial. `inference_engine.py`, `model_space.py`, `model_view.py`, `gridline.py`, `constants.py`, `main.py` [ref:inferred-dimension-driven-placement, grid-system§17.8] [type:Task] [P2] [subject:CAD] [done:2026-08-14]
    - [x] **Grab-handle style consistency** — **partially addressed + superseded 2026-08-29** (design-token build): the reuse sweep found all model-space grips route through ONE renderer (`model_view.drawForeground`), now re-colored to read the `selection`/`selection_active` theme tokens (green), so wall/room/floor/roof/construction/gridline grips conform in one place. The base-style *review* + full **selection-box** treatment (8 handles + rotation, per-item baked-geometry) is now the **"Adopt the `SelectionBox` manipulator"** task (see the design-token follow-ups) with its own [P1] "match every selection-box item" sweep. `constants.py`, `theming.md`, `model_view.py` [type:Task] [P3] [subject:UX] [done:2026-08-29]
    - [x] **On-canvas array/offset for gridlines** — shipped on `feat/gridline-inference-array-offset` (2026-08-14). Two transient modes (`gridline_offset`/`gridline_array`) from the gridline right-click (entity menu + `model_view._build_plan_context_menu` selection fallback); cursor-live perpendicular spacing/side + live "Spacing…×Count"/"Distance…" Dim HUD + Tab/any-digit `_DynInput` (Spacing + **integer** Count); `drawForeground` ghost preview; commit builds real gridlines via `GridlineItem.offset_copy`/`array_copies` (rigid parallel translation, inherit angle/length/bubble-offsets/visibility/display-overrides, unlocked, fresh sequential labels via counter-sync+`auto_label`), one undo step. Also: **gridline copy/paste** round-trips (paste_items reconstructs gridline dicts) and a **properties-panel placement template** (non-geometric preset). No `.fpd` schema change. Spec `grid-system.md §17`. `gridline.py`, `model_space.py`, `model_view.py`, `entity_context_menu.py`, `constants.py` [ref:grid-system§17] [type:Task] [P2] [subject:CAD] [done:2026-08-14]
    - **Follow-ups (from the 2026-08-14 inference/array-offset build + smoke tests):**
      - [x] **Paste-mode ghost + inference** — shipped on `feat/gridline-oncanvas-interaction` (2026-08-14 grill→plan→subagent build, 11 tasks, two-stage reviews + holistic). Reworked to **AutoCAD MOVE/COPY** on the shared paste/move flow, entity-**generic**: base-point pick → destination pick, both with snap + alignment-inference active; a cosmetic scene-coord **silhouette ghost** (`_build_move_ghost_base`/`_shape_paths_for_move`/`_clipboard_ghost_paths` → `Model_View.drawForeground` block 8) rides the cursor and replaces the node-dot for **paste and move**; live dx/dy/dist readout; **move self-excludes the moving items** from inference refs (`_inference_exclude_ids`); exits after one placement, Esc cancels, one undo step. Gridline ghost uses the centerline (not the fat hit-strip). `model_space.py`, `model_view.py` [ref:grid-system, inferred-dimension-driven-placement] [type:Bug] [P2] [subject:CAD] [done:2026-08-14]
      - [x] **Type-to-seed the `_DynInput` field** — shipped on `feat/gridline-oncanvas-interaction`. The digit that opens the popup is forwarded into the primary field (Spacing/Distance/Length) with the cursor at end; also **extended digit-open to `draw_line`/`draw_gridline` placement** (was Tab-only). Fixed a test-pollution bug en route: reassigning the sip method `QDialog.exec` corrupts its C++ slot binding process-wide — replaced with a live-dialog `QTimer`+`activeModalWidget` pattern. `model_space.py` [ref:inferred-dimension-driven-placement §4.3] [type:Task] [P3] [subject:UX] [done:2026-08-14]
      - [x] **On-canvas bubble-offset grips** — shipped on `feat/gridline-oncanvas-interaction`. `grip_points()` grew to 4 entries (endpoints + bubble centres); `apply_grip` indices 2/3 slide each end's standoff along the axis (perp discarded, floored at 0); `grip_hittable(idx)` gates a hidden-bubble/locked grip in both `_find_grip_hit` and the `drawForeground` render loop (no phantom handle); `_drag_grip_to` extracted so bubble multi-select (same delta, locked skipped) is testable; one undo step per drag. `gridline.py`, `scene_tools.py`, `model_space.py`, `model_view.py` [ref:grid-system §5.6] [type:Task] [P2] [subject:CAD] [done:2026-08-14]
      - _↳ (open parent) Dynamic Input engine_
          - [x] **Placement-UX overhaul** (arc revamp + rectangle rotate + ghost-on-commit) — shipped on `feat/placement-ux-overhaul` (2026-08-21 grill→brainstorm→plan→subagent build, 15 plan tasks + a large smoke-fix batch, two-stage reviews). **Arc:** two ←/→-cycled variants (centre-first/start-first); step-aware DI (Radius+Start° via `line` reuse, then `arc_span` Span°⇄Arc-length coupled via new `FieldKind.SPAN`); `_commit_draw_arc_at`/`_commit_draw_arc_rim_at`; block-4 readout retired; Ctrl angle-snap; span-step reference guides (0° datum + start radial + live sweep); rich `ArcItem.get_properties`. **Rectangle:** corner/centre variants keep 2-click sizing + a rotate step; `RectangleItem` gains stored `angle` (native Qt transform, grips/snap/**both serialization paths** rotation-aware; Y-up→Qt-CW negation; `translate` carries the pivot); rotate reference guides; Ctrl angle-snap. **#3 ghost-on-commit** across all HUD clients (`fieldCommitted` + `_preview_from_resolved`). **Generic `_PLACEMENT_VARIANTS` ←/→ cycle** (step-0, session-sticky) + single-key tool shortcuts L/R/C/A/G (scene-focus-gated; `set_mode` refocuses the view), Line/Rect ribbon dropdowns retired, Align→Shift+A. Fixed the move ghost (grip base-click skipped the build). Governing spec `inferred-dimension-driven-placement.md §4` rewritten (§4.7-4.10). Full suite green (chunked). `model_space.py`, `dynamic_input.py`, `construction_geometry.py`, `model_view.py`, `main.py`, `scene_io.py` [ref:inferred-dimension-driven-placement §4] [type:Task] [P1] [subject:CAD] [done:2026-08-21]
            - **Follow-ups (from the 2026-08-21 build + smoke tests):**
              - [x] **Hatch fill on a rotated rectangle renders axis-aligned** — moot as of 2026-08-22: `HatchItem` retired; fill is now drawn in the shape's own `paint()` (item-local coords) so it rotates with the shape. `construction_geometry.py` [type:Bug] [P3] [subject:CAD] [done:2026-08-22]
          - [x] **`construction_line` typed input can build a zero-length line** — moot as of 2026-08-22: `ConstructionLine` fully retired (tool/mode/typed-input path removed), so the buggy path no longer exists. `model_space.py` [type:Bug] [P3] [subject:Architecture] [done:2026-08-22]

- [x] Voronoi relaxation algorithm for auto-populate sprinkler placement [type:Recently Completed] [subject:Sprinkler Design]

- [x] Level manager elevation enhancements (guard against deleted C++ objects) [type:Recently Completed] [subject:CAD]

- [x] Sprinkler property display improvements (absolute X/Y/Z read-only properties) [type:Recently Completed] [subject:Sprinkler Design]

- [x] Algorithm selection dropdown in auto-populate dialog [type:Recently Completed]

- [x] Room name as read-only property on sprinklers and nodes [type:Recently Completed]

- [x] Room name tagging on sprinkler nodes for reliable detection [type:Recently Completed]

- [x] Auto-populate removes existing room sprinklers before placing new ones [type:Recently Completed]

- [x] Fix stacked room sprinkler detection (Z-range filtering) [type:Recently Completed]

- [x] Elevation-based Z-ordering for plan view depth sorting [type:Recently Completed]

- [x] Detail markers filtered by view range [type:Recently Completed]

- [x] Model browser refresh on undo/redo [type:Recently Completed]

- [x] README.md for the repository [type:Backlog] [subject:Documentation] [P3] [done:2026-05-03]

- [x] Unit tests for hydraulic solver — 59 tests covering Hazen-Williams formula, BFS tree, supply curve, validation guards, flow assignment, end-to-end 2/3-node networks, elevation effects [type:Backlog] [subject:Hydraulic Calculator] [done:2026-05-01]

- [x] Unit tests for auto-populate algorithms — 76 tests covering density/area interpolation, polygon area, grid dimensions, rectangle decomposition, sprinkler grid computation [type:Backlog] [subject:Sprinkler Design] [done:2026-05-01]

- [x] Unit tests for geometry utilities (CAD_Math, geometry_intersect) — 113 tests covering all public functions: vector ops, intersections, projections, rotations, edge cases [type:Backlog] [subject:CAD] [done:2026-05-01]

- [x] Full code review, develop specs for various existing features that don't have specs and do full documentation for the project — **completed 2026-04-09**: audit identified 16 spec gaps, 5 test gaps, and 1 existing spec update; all decomposed into tasks below [type:Backlog] [P1] [subject:Documentation] [done:2026-04-09]

- [x] Spec & grill session: define and refine the relationship between views — see `docs/specs/view-relationships.md` [type:Backlog] [P1] [subject:Architecture] [done:2026-04-08]

- [x] Spec & grill session: wall, room & floor slab system — see `docs/specs/wall-room-floor-system.md`. Covers wall geometry (Center/Left/Right alignment), joinery (Auto/Butt/Solid), room boundary detection (tightest-CW-turn graph walk), NFPA coverage model, floor slab occlusion, wall opening lifecycle, cross-entity interactions. Divergences from current implementation flagged with migration paths. [type:Backlog] [P1] [subject:Architecture] [done:2026-04-27]

- [x] Spec & grill session: snapping engine — see `docs/specs/snapping-engine.md` (14 roadmap items added below) [type:Recently Completed] [subject:CAD]

- [x] Add absolute elevation for node 1 and 2 in pipe properties template [type:Task] [subject:Sprinkler Design] [done:2026-05-03]

- [x] Bug: DXF/PDF underlay lines render too thick and scale with zoom — `_build_batched_underlay_group` used a non-cosmetic `QPen(color, 1.5 * import_scale)` (scene-mm width), so stroked geometry thickened as you zoomed in and ballooned for scaled imports. Fixed: cosmetic pen + fixed pixel width `UNDERLAY_LINE_WIDTH_PX` (`constants.py`, 1.5, matches gridlines); dropped the `import_scale` multiply at all 4 build sites. Spec §3.4 added; §3.1 `user_layer` removed. `model_space.py`, `constants.py`, `docs/specs/underlay-workflow.md` [type:Bug] [P2] [subject:CAD] [done:2026-06-23]

- [x] Investigate `Room.z_range_mm()` vs user expectations [ref:view-relationships§7.2] [subject:CAD] [done:2026-04-08]

- [x] Bug: Room fill/highlight disappears when room label is scrolled off-screen in plan view — root cause was `Room.shape()` override returning only the label area; Qt used it in the viewport paint path. Fixed by removing the override. [type:Task] [P2] [subject:CAD] [done:2026-04-08]

- [x] Restore label-only click-selection for rooms — absorbed into selection-mode spec (`docs/specs/selection-mode.md` §5.4). Room click restricted to label-bg rect. [type:Task] [P2] [subject:CAD] [done:2026-05-02]

- [x] Audit other `shape()` overrides in the codebase — 23 overrides checked. One fix: `ElevGridlineItem.shape()` returned only bubble ellipses while `paint()` drew the line; added stroker path for the line segment. All other overrides use safe patterns (stroker, full rect, or matching paint area). `elevation_scene.py` [type:Task] [P3] [subject:CAD] [done:2026-05-03]

- [x] Spec session: grid system architecture — consolidated into single `GridlineItem` with lock, pull-tab grips, perpendicular reposition, on-selection spacing dimensions, edit-existing dialog, cardinal-only elevation filtering, per-view Z-extent overrides. Legacy `GridLine` removed. See `docs/specs/grid-system.md` [type:Backlog] [P1] [subject:Architecture] [done:2026-04-10]

- [x] Spec session: scale calibration & underlay workflow — see `docs/specs/underlay-workflow.md`. Covers underlay lifecycle (import → place → persist → reload → refresh), per-level visibility, per-source-layer visibility, browser tree management, file-not-found handling, path resolution, transform origin fix. ScaleManager documented as fixed global constant (1 unit = 1 mm). [type:Backlog] [P1] [subject:Architecture] [done:2026-04-13]

- [x] Spec session: sprinkler system components — see `docs/specs/sprinkler-system-components.md`. Covers sprinkler database (SprinklerRecord, JSON-backed CRUD, manager dialog), pipe diameter/schedule/material system (internal keys, OD/ID tables, C-Factor, auto-Main), fitting assignment (0–4 pipe junction rules, vertical detection, SVG alignment/scaling), node Z-position (ceiling_level + ceiling_offset formula, z_offset legacy path), sprinkler visual (SVG scaling, selection delegation), SprinklerSystem container, WaterSupply entity, design area (NFPA 13 density/area curves, hazard classification). 9 divergences flagged with migration paths. [type:Backlog] [P1] [subject:Architecture] [done:2026-04-28]

- [x] Spec session: hydraulic solver & reporting — see `docs/specs/hydraulic-solver-and-reporting.md`. Covers 4-phase Hazen-Williams algorithm (flow assignment, required pressure backward pass, supply curve check with hose stream, actual pressure forward pass), NFPA 13 §22.4.2 formulas, network validation (5 guards + loop detection + uncalibrated scale), node numbering (BFS major/minor), equivalent pipe length table (NFPA 13 Table 22.4.3.1.1), report widget (3 tabs: Summary with design criteria, NFPA-format Node Summary Table, Hydraulic Graph), PDF/CSV export, on-scene overlay (node badges + pipe hf heatmap). 10 divergences flagged with migration paths. [type:Backlog] [P1] [subject:Architecture] [done:2026-04-28]

## Snapping Engine Roadmap (from `docs/specs/snapping-engine.md` §12)

- [x] Snap picker: same-parent intersection suppression + endpoint protection band — fixes wall-corner and hatch case studies [ref:snap-spec§6.3] [type:Backlog] [P1] [subject:CAD] [done:2026-04-07]

- [x] Snap phase-4 segment-source filter audit — restrict `_check_geometry_intersections` to matrix-supported types; add generic `QGraphicsPathItem` (DXF) + DXF underlay group descent [ref:snap-spec§5,§6] [type:Backlog] [P1] [subject:CAD] [done:2026-04-10]

- [x] WallSegment named-target marker variants — hollow square / hollow triangle for face corners and face midpoints [ref:snap-spec§8] [type:Backlog] [P1] [subject:CAD] [done:2026-04-07]

- [x] Decouple `nearest` from the perpendicular toggle in `_geometric_snaps` [ref:snap-spec§5-note-1] [type:Backlog] [P2] [subject:CAD] [done:2026-04-08]

- [x] ArcItem tangent support in `_geometric_snaps` [ref:snap-spec§5-row-ArcItem] [type:Backlog] [P2] [subject:CAD] [done:2026-04-10]

- [x] Generic `QGraphicsPathItem` (DXF) phase-4 segment extraction [ref:snap-spec§5-note-7] [type:Backlog] [P2] [subject:CAD] [done:2026-04-10]

- [x] Snap engine geometric primitive unit tests (`_line_line_intersect`, `_line_circle_intersect`, `_project_to_segment`) [ref:snap-spec§10.1] [type:Backlog] [P2] [subject:CAD] [done:2026-04-08]

- [x] Snap engine matrix fixture test harness — one headless fixture per ✓-cell in §5 (59 tests, 11 classes) [ref:snap-spec§10.2] [type:Backlog] [P2] [subject:CAD] [done:2026-04-10]

- [x] Snap engine case-study regression tests pinned to §7.1 and §7.2 [ref:snap-spec§10.3] [type:Backlog] [P2] [subject:CAD] [done:2026-04-07]

- [x] Bind F3 to global OSNAP toggle and reflect state in status bar [ref:snap-spec§9.4-§9.5] [type:Backlog] [P3] [subject:CAD] [done:2026-04-08]

- [x] Confirm or build per-type OSNAP toggle UI surface (`snap_endpoint`, `snap_midpoint`, etc.) [ref:snap-spec§9.5] [type:Backlog] [P3] [subject:CAD] [done:2026-04-08]

- [x] Spec session: OSNAP toolbar — per-type toggle UI, dockable placement, indicator layout, interaction with status bar pill. See `docs/specs/osnap-toolbar.md`. [ref:snap-spec§9.5] [type:Backlog] [P1] [subject:CAD] [done:2026-04-28]

- [x] Implement OSNAP toolbar — `_OsnapToolbar(QToolBar)` with 8 toggle buttons, 8 SVG icons, dialog sync, F3 dimming, right-click Enable/Disable All, QSettings persistence, `_STATE_VERSION` 4→5. As-built deviation: hidden on first launch (no menu bar in this ribbon app), surfaced via a "OSNAP Bar" toggle button in the Manage → Snap group. 8 new tests (test_osnap_toolbar.py + test_osnap_ui.py). See `docs/specs/osnap-toolbar.md` [ref:osnap-toolbar-spec] [type:Task] [P1] [subject:CAD] [done:2026-06-22]

- [x] View3D/pyvista plotter not finalized on MainWindow teardown — added `View3D.cleanup()` (idempotent `plotter.close()` + `closeEvent`) called from `MainWindow.closeEvent` (child widgets don't receive closeEvent). Releases the VTK GL context on close instead of leaking it. Verified: pre-fix a 2nd MainWindow hung on `wglMakeCurrent failed (handle invalid)`; post-fix 2 MainWindows build+render+close cleanly. 3 new tests in `tests/test_view_3d.py`. `view_3d.py`, `main.py` [type:Bug] [P3] [subject:Testing] [done:2026-06-22]

- [x] Clean up `_open_snap_tolerance_dialog` snap-type mapping — `snap_types` now maps "Intersection" → `snap_intersection`; removed the dead `"snap_endpoint"` value, the per-row Intersection special-casing, and the obsolete `hasattr(eng, "snap_intersection")` guards. `main.py` [type:Task] [P3] [subject:Code Quality] [done:2026-06-22]

- [x] Fix test-ordering failures in `test_snap_engine_matrix.py` — root cause: `test_osnap_ui.py` creates MainWindow which reads QSettings `snap/tolerance_px=20` into module-level `SNAP_TOLERANCE_PX`, halving the snap search radius for subsequent tests. Fixed by saving/restoring `SNAP_TOLERANCE_PX` in the `_main_window_singleton` fixture teardown. `tests/test_osnap_ui.py` [type:Bug] [P2] [subject:Testing] [done:2026-05-03]

- [x] Spec session: inferred / dimension-driven placement — see `docs/specs/inferred-dimension-driven-placement.md`. Greenfield design covering three capabilities: dynamic input (floating length field at cursor, angle read-only for pipes), alignment guides (H/V alignment, wall proximity, extension lines, equal spacing — color-coded, weak snap), selection dimensions (post-placement node spacing edit along pipe direction). Snap priority hierarchy: OSNAP > guide intersection > single guide > free cursor. [ref:snap-spec§2.3] [type:Backlog] [P1] [subject:Architecture] [done:2026-04-28]

- [x] Extract snap primitive epsilons (`1e-10` line-line denom, `1e-12` degenerate segment/radius) to named constants on `SnapEngine` — `_EPS_PARALLEL`, `_EPS_DEGENERATE`, `_EPS_COINCIDENT` added to module-level constants [type:Backlog] [P3] [subject:CAD] [done:2026-05-03]

- [x] QPainterPath MoveTo element handling — `_collect`, `_geometric_snaps`, and `_check_geometry_intersections` all pair consecutive path elements as segments without checking element type; MoveToElements in multi-contour DXF paths create phantom connecting segments. Fix consistently across all three sites. [type:Backlog] [P3] [subject:CAD] [done:2026-05-03]

- [x] Phase-4 intersection highlight should show both source items — fixed: `ctx.check()` now accepts `src_item2` and `source_lines`; phase-4 passes both source items and actual segment geometry; renderer draws only participating segments instead of full items. Also fixed: Phase 1 underlay child dead code, gridline force-win removed, wall width grip added, rubber-band selection no longer blocked by items. [type:Backlog] [P2] [subject:CAD] [done:2026-05-01]

- [x] Snap search rect performance at low zoom — `SNAP_TOLERANCE_PX / scale` creates very large search rects when zoomed out (e.g., 800×800 scene units at scale=0.1), pulling in far too many items for the O(n²) phase-4 pairing. Fixed: cap scene-unit tolerance at `SNAP_MAX_SCENE_TOL` (200mm), skip phase 4 when item count exceeds `_PHASE4_MAX_ITEMS` (64). [type:Backlog] [P1] [subject:CAD] [done:2026-05-03]

- [x] Fix: intersection snapping (phase-4) lost at low snap tolerance — root cause (user-confirmed): the picker's priority-override band was `tolerance × 0.3`, so lowering the snap tolerance to 5px collapsed it to ~1.5px and `intersection` (priority 0) lost to the *closer* `nearest`/`perpendicular` foot near a crossing; endpoints/midpoints unaffected, so it read as "intersection snap broken" scene-wide on any import. NOT import-dialog-specific, NOT a flag/engine-logic bug (all 8 snap flags were `true`; matrix tests passed at the 40px default). Floored the band at `SNAP_PRIORITY_BAND_PX = 12` (capped at tolerance, `max(tol*0.3, min(tol, 12/scale))`) — identical at/above the 40px default, only the sub-40px regime changes. 1 parametrized regression test (`tests/test_snap_priority_band.py`, tol 5/10/20/40); full snap suite (101) + full non-3D suite (1119) + 3D-in-isolation (74) green. Spec §6.1 updated + impl-stamped. `snap_engine.py`, `docs/specs/snapping-engine.md` [ref:snap-spec§6.1] [type:Bug] [P1] [subject:CAD] [done:2026-06-25]

- [x] Fix: intersection snapping never worked *within* a placed DXF/PDF underlay (pre-existing, not a regression — surfaced while fixing the tolerance bug, user-confirmed). Root cause: all segments from an underlay `UnderlaySnapIndex` were tagged with the underlay **group** as their phase-4 source, and §6.3 Change A's same-parent suppression (`src1 is src2`, built for wall-internal face crossings) then dropped **every** crossing inside one underlay — and the importer bundles a whole `LWPOLYLINE` (wall/room/duct outlines) into one geom, so even a polyline crossing itself was suppressed. Fix: phase-4 segments now carry a separate `parent_key`; scene items use the item (wall-face / native-polyline self-suppression preserved), underlay-index segments use `parent_key = None` (exempt — an underlay is "just lines on a drawing"). Polyline-vertex pseudo-intersections stay clean via Change B's endpoint protection band. 4 regression tests (`tests/test_snap_underlay_internal_intersection.py`: two lines, line×polyline, self-crossing polyline, vertex guard); full snap suite (105) + non-3D suite (1123) green. Spec §6.3 carve-out added. `snap_engine.py`, `docs/specs/snapping-engine.md` [ref:snap-spec§6.3] [type:Bug] [P1] [subject:CAD] [done:2026-06-25]

## View Relationships Follow-Ups (from `docs/specs/view-relationships.md` §11)

- [x] Extract plan-family Z-band magic numbers to named constants in `constants.py` — 18 named constants added (Z_CAT_*, Z_ELEV_SCALE, static Z bands); applied in level_manager.py, node.py, pipe.py, sprinkler.py, detail_view.py, water_supply.py, construction_geometry.py, view_marker.py, hydraulic_node_badge.py, design_area.py, gridline.py, room.py [ref:view-relationships§7.3] [type:Backlog] [P3] [subject:CAD] [done:2026-05-03]

- [x] Investigate elevation marker persistence — resolved by section view subsystem spec: section markers persist via `to_dict()`/`from_dict()`, elevation views retired and replaced by section views. See `docs/specs/section-view-subsystem.md` §10.5, §14. [ref:view-relationships§6.3] [type:Backlog] [P2] [subject:CAD] [done:2026-04-29]

- [x] Spec session: Section view subsystem — arbitrary-angle cut planes replacing cardinal-only elevation views. See `docs/specs/section-view-subsystem.md`. Covers projection math, cut/behind rendering, SectionMarker with grips, SectionScene/SectionView/SectionManager, elevation retirement, cardinal shortcuts. [ref:view-relationships§4.1] [type:Backlog] [P1] [subject:Architecture] [done:2026-04-29]

- [x] Document or deprecate `Node.z_offset` legacy field — deprecated: no longer written on save/copy; ceiling_offset is sole Z source; specs updated [ref:view-relationships§3.3] [type:Backlog] [P3] [subject:CAD] [done:2026-05-03]

- [x] Remove vestigial `display_mode` from `Level` and `LevelManager.apply_to_scene()` — Hidden/Faded/Visible modes were unused; all visibility driven by Z-range filtering. Removed `DISPLAY_MODES`, `FADE_OPACITY`, `_reapply_fade()`, display combo from `level_widget.py`. Serialization ignores old `display_mode` field on load. [type:Backlog] [P3] [subject:CAD] [done:2026-05-03]

## Additional Spec Sessions

- [x] Spec session: Selection mode — see `docs/specs/selection-mode.md`. Authoritative selection model for plan view: hover pre-highlight (cyan glow), pre-click Tab-cycle disambiguation, formalized Z-order priority, direction-dependent rubber-band (window/crossing), universal Ctrl+click toggle, room label-only click, underlay Tab-reachability, grip protocol. Absorbs "Restore label-only click-selection for rooms". [type:Backlog] [P1] [subject:Architecture] [done:2026-05-02]

- [x] Spec session: Floor elements — **SUPERSEDED by the 2026-08-27/28 floor-workflow task** (two-boundary Z model, degenerate-safe z-range, elevation projection, placement workflow all shipped + reconciled into `wall-room-floor-system.md §11`). `floor_slab.py` [type:Backlog] [P2] [subject:Architecture] [done:2026-08-28]

- [x] Spec session: Paper space — see `docs/specs/paper-space.md`. Covers sheet management, sheet views (linked view references), scale control, PDF export/print, title block template system, annotations, labels/thin-lines, layer overrides, DXF export target. Three-phase implementation plan (MVP → annotations/overrides → future). [type:Backlog] [P1] [subject:Architecture] [done:2026-04-09]

## Code Review Audit — Spec Gaps (from 2026-04-09 audit)

### P2 — Existing Spec Updates

- [x] Update pipe-placement-methodology.md — "Known Implementation Bugs" section already present as §16 in spec revision 2 (B1-B12 + E1-E3 documented with severity, location, and fix descriptions) [type:Backlog] [P2] [subject:Documentation] [done:2026-05-03]

### P3 — Nice-to-Have

- [x] Spec session: construction geometry system — **SUPERSEDED 2026-08-24 by the governing spec `docs/specs/2d-geometry.md`** (forged during the 2D-geometry polish cluster; covers the item models incl. `RegularPolygonItem`, placement workflows, closed-polyline model, snap contribution, dual-path serialization, display). `ConstructionLine` was retired 2026-08-22. [type:Backlog] [P3] [subject:Architecture] [done:2026-08-24]

## Hydraulic Solver Follow-Ups (from `docs/specs/hydraulic-solver-and-reporting.md` §12)

- [x] Add equivalent pipe lengths to friction loss calculation — NFPA 13 Table 22.4.3.1.1 lookup by fitting type and pipe diameter; sum both-end equivalents per pipe (supply node excluded). Reference dialog accessible from Hydraulics toolbar and report. New module `equivalent_length.py`. `hydraulic_solver.py`, `hydraulic_report.py`, `main.py` [ref:hydraulic-spec§10,D2] [type:Task] [P1] [subject:Hydraulic Calculator] [done:2026-04-29]

- [x] Bug: Hose stream allowance not consumed — added to supply demand GPM at supply curve check; three graph markers (origin, sprinkler demand, total demand); separate line items in report summary (sprinkler/hose/total). Omitted when zero. `hydraulic_solver.py`, `hydraulic_report.py` [ref:hydraulic-spec§4.5,D3] [type:Bug] [P1] [subject:Hydraulic Calculator] [done:2026-04-29]

- [x] Expose required_node_pressures in HydraulicResult — added field to dataclass; badges show both Required P and Actual P (required first); report pipe results table includes equivalent/total length columns. `hydraulic_solver.py`, `hydraulic_report.py`, `hydraulic_node_badge.py`, `model_space.py`, `node.py` [ref:hydraulic-spec§6,D6] [type:Task] [P1] [subject:Hydraulic Calculator] [done:2026-04-29]

- [x] Add loop detection warning — compares reachable pipe count vs BFS tree edges; warns with excluded pipe count and singular/plural grammar. `hydraulic_solver.py` [ref:hydraulic-spec§7.3,D1] [type:Task] [P2] [subject:Hydraulic Calculator] [done:2026-05-01]

- [x] Consolidate report to 3 tabs — **shipped 2026-07-10** (spec §9 as-built, D7 resolved): Summary gains Project / Design Criteria / Water Supply / Results sections (messages on top; "—" degradation; data from `_project_info` + active DesignArea + WaterSupply incl. new **Test Date** property); 14-column NFPA **Node Summary Table** (node-oriented via new `HydraulicResult.node_parent_pipe`; velocity plain informational; Notes = K on sprinklers / fitting on junctions / "Supply"); Pipe Results + Sprinkler Schedule + Pipe Schedule removed; CSV/PDF rebuilt on shared `_summary_sections`/`_node_summary_rows` helpers (WYSIWYG minors toggle, PDF embeds graph image, default save to `<project>/HC Reports`); **G6 guard** — no design area → calc refuses (all-sprinklers fallback retired); zero-flow pipes no longer labelled 0.0 gpm/psi. Bonus: PDF export had PyQt5-era APIs and had never worked — fixed. 40 new tests (`tests/test_hydraulic_report.py` + solver guard tests); 1,228 tests green (chunked). `hydraulic_solver.py`, `hydraulic_report.py`, `water_supply.py`, `pipe.py`, `main.py` [ref:hydraulic-spec§9,D7] [type:Task] [P1] [subject:Hydraulic Calculator] [done:2026-07-10]

- [x] Place WaterSupply on-node — change from proximity search to direct node placement (same as sprinklers). `model_space.py`, `hydraulic_solver.py` [ref:hydraulic-spec§7.2,D5] [type:Task] [P2] [subject:Hydraulic Calculator] [done:2026-05-06]

## Sprinkler System Components Follow-Ups (from `docs/specs/sprinkler-system-components.md` §14)

- [x] Bug: Sprinkler Manager template uses wrong property keys — `_apply_sprinkler_template_from_record()` uses `"Temp Rating"` (should be `"Temperature"`), `"Type"` (should be `"Orientation"`), and omits Manufacturer/Model. Only K-Factor, Min Pressure, Coverage Area actually transfer. `main.py:2207-2211` [ref:sprinkler-spec§4.4,D9] [type:Bug] [P1] [subject:Sprinkler Design] [done:2026-05-02]

- [x] Add "Concealed" to Sprinkler Orientation options — `Sprinkler._properties["Orientation"]["options"]` only has Upright/Pendent/Sidewall. `sprinkler.py` [ref:sprinkler-spec§4.4,D3] [type:Task] [P1] [subject:Sprinkler Design] [done:2026-05-02]

- [x] SprinklerDatabase singleton + stable path — shipped on `feat/sprinkler-db-singleton` (2026-08-11 /todo Medium workflow: grill → plan → executing-plans, smoke-tested). Default path moved to `%APPDATA%/FirePro3D/sprinklers.json` via `_default_db_path()` (fallback `~/FirePro3D/…`; `path=` still injectable), with a one-time **copy-if-absent** migration of any legacy CWD `sprinklers.json` (`_migrate_legacy_db`, idempotent) + `makedirs`-before-save. `MainWindow` owns **one** `SprinklerDatabase`, injected via `set_sprinkler_db()` into the scene (auto-populate), property panel, and Manager dialog; the `property_manager` module-level cache is **removed** (headline fix — Manager edits now visible in the property-panel dropdown + auto-populate picker with no restart). Leaf consumers keep a `db or SprinklerDatabase()` fallback; tracked repo seed file left in place. 11 new tests (`tests/test_sprinkler_db_singleton.py`, windowless end-to-end shared-state) + auto-populate test isolation fix; full suite green (1968 non-3D + 75 3D; the 2 `fitz`-dependent modules unrun locally — PyMuPDF not installed). Spec §4.2 rewritten + D1 resolved. `sprinkler_db.py`, `main.py`, `model_space.py`, `property_manager.py` [ref:sprinkler-spec§4.2,D1] [type:Task] [P1] [subject:Sprinkler Design] [done:2026-08-11]
  - **Follow-ups (from the 2026-08-11 build):**

- [x] Add missing pipe sizes (¾", 1-¼", 2-½") — add to `_INTERNAL_DIAMETERS`, `NOMINAL_OD_IN`, `INNER_DIAMETER_IN` (all schedules), display mappings. 2-½" classified as Branch. `pipe.py` [ref:sprinkler-spec§5.1,D6] [type:Task] [P2] [subject:CAD] [done:2026-05-03]

- [x] Unify Node Z-position on ceiling_offset — remove z_offset as computation source; derive from ceiling_offset on load if missing. Stop writing z_offset on new saves (keep reading for old-file compat). `node.py`, `model_space.py`, `scene_io.py`, `property_manager.py` [ref:sprinkler-spec§7.5,D4] [type:Task] [P2] [subject:CAD] [done:2026-05-03]

- [x] C-Factor read-only, material-derived — add `MATERIAL_C_FACTOR` mapping, make C-Factor readonly in property schema, derive on Material change. Future: Material Property Manager. `pipe.py` [ref:sprinkler-spec§5.5,D5] [type:Task] [P2] [subject:CAD] [done:2026-05-03]

- [x] SprinklerRecord schema expansion — add optional fields: response_type, max_s_spacing, max_l_spacing, thread_size, listing, deflector_min, deflector_max. `sprinkler_db.py` [ref:sprinkler-spec§4.1,D2] [type:Task] [P2] [subject:Sprinkler Design] [done:2026-05-03]

## Wall, Room & Floor Slab Follow-Ups (from `docs/specs/wall-room-floor-system.md` §13–§14)

- [x] **Three-wall junction renders a bad joint** (user smoke-test screenshots 2026-07-11 + 2026-07-13) — two root causes, both from "3+ walls → Butt" ending every wall flat/perpendicular at its endpoint (a diagonal's flat end can't mate with anything): (1) **3 shared endpoints** → new **full-miter pie join** (user-picked from rendered mockups): each face miters to its angular neighbour's wedge face, junction-wedge vertex inserted so all three fills cover the triangle (seamless with scene-global hatch), end edges suppressed; Butt fallback past the `MAX_MITER` clamp; 4+-way crossings stay Butt. (2) **Tee joins** (endpoint mid-span on a host wall — the user's actual screenshot topology): `_auto_join_wall` Pass 2 now snaps to the host **centerline** (was: face — the picked point visibly "jumped"), and `_tee_cope_corners` copes the end onto the host's near face at any angle (also heals legacy face-snapped tees on reload). 14 regression tests. `wall.py` (`_pie_miter_corners`, `_tee_cope_corners`, wedge-point fill), `model_space.py` (tee snap) [ref:wall-spec§5] [type:Bug] [P2] [subject:CAD] [done:2026-07-13]

- [x] Rename wall alignment Interior/Exterior → Left/Right — update `ALIGN_INTERIOR`/`ALIGN_EXTERIOR` constants, property dropdown, serialization migration (`from_dict` maps old names). `wall.py`, `model_space.py` [ref:wall-spec§4.2] [type:Task] [P2] [subject:CAD] [done:2026-04-27]

- [x] Remove Miter join mode — delete from enum and property dropdown, serialized `"Miter"` mapped to `"Solid"` on load (preserves corner geometry). `wall.py` [ref:wall-spec§5.1] [type:Task] [P2] [subject:CAD] [done:2026-04-27]

- [x] Enforce 1mm minimum wall thickness — clamp on property set and `from_dict`. `wall.py` [ref:wall-spec§4.3] [type:Task] [P2] [subject:CAD] [done:2026-04-27]

- [x] Add opening reposition in `_rebuild_path()` — call `_reposition()` on all owned openings whenever wall geometry changes. `wall.py` [ref:wall-spec§7.3] [type:Bug] [P2] [subject:CAD] [done:2026-04-27]

- [x] Add offset clamping for wall openings — clamp `_offset_along` to `[0, centerline_length]` in `_reposition()` and `translate()`. `wall_opening.py` [ref:wall-spec§7.4] [type:Task] [P2] [subject:CAD] [done:2026-04-27]

- [x] Fix contradictory comments in room inset logic — corrected Left/Right/Center inset descriptions to match code behavior per spec §8.4. `model_space.py` [type:Task] [P3] [subject:CAD] [done:2026-05-01]

- [x] Remove dead code in room.py — removed unreachable `return result` statements. `room.py` [ref:wall-spec§13] [type:Task] [P3] [subject:CAD] [done:2026-05-01]

- [x] Extract wall join constants to `constants.py` — MITER_TOL, MAX_MITER_FACTOR, AUTO_JOIN_TOLERANCE, TEE_TOLERANCE moved from wall.py/model_space.py to constants.py. [ref:wall-spec§5.3] [type:Task] [P3] [subject:CAD] [done:2026-05-01]

## Code Review Audit — Test Gaps (from 2026-04-09 audit)

- [x] Unit tests for wall, room & floor slab entities — 98 tests covering alignment, normal, grips (incl. width grip), joinery, serialization, room area/perimeter, floor slab points/edges [type:Backlog] [P2] [subject:Testing] [done:2026-05-01]

- [x] Unit tests for sprinkler system components — 65 tests covering DB CRUD, pipe diameter/schedule/C-factor, node creation/serialization, fitting type assignment, sprinkler properties [type:Backlog] [P2] [subject:Testing] [done:2026-05-01]

- [x] Unit tests for paper space — 43 tests covering paper sizes, title block, viewport, scene setup, widget integration, layout constants [type:Backlog] [P2] [subject:Testing] [done:2026-05-01]

- [x] Unit tests for scene tools — 104 tests covering offset, fillet/chamfer, break, perpendicular distance, grip hit detection, edge extraction, make_offset_item [type:Backlog] [P3] [subject:Testing] [done:2026-05-01]

- [x] Unit tests for 3D view — 71 tests covering mesh generation, flux colors, visibility, coordinate transforms, camera angles, scene bounds, actor management, dirty flag [type:Backlog] [P3] [subject:Testing] [done:2026-05-01]

## Underlay Workflow Follow-Ups (from `docs/specs/underlay-workflow.md` §15)

- [x] Implement `Underlay` data model changes — new fields (`level`, `visible`, `hidden_layers`, `import_mode`), serialization, backward compat [ref:underlay-spec§3] [type:Backlog] [P1] [subject:CAD] [done:2026-04-13]

- [x] Implement underlay path resolution — relative/absolute save/load rules, `..` depth guard [ref:underlay-spec§4] [type:Backlog] [P1] [subject:CAD] [done:2026-04-13]

- [x] Implement file-not-found handling — placeholder item, aggregate warning, relink action [ref:underlay-spec§5] [type:Backlog] [P1] [subject:CAD] [done:2026-04-13]

- [x] Fix underlay transform origin to bounding rect center [ref:underlay-spec§6] [type:Backlog] [P1] [subject:CAD] [done:2026-04-13]

- [x] Implement per-level underlay visibility [ref:underlay-spec§7] [type:Backlog] [P1] [subject:CAD] [done:2026-04-13]

- [x] Implement per-source-layer visibility for DXF underlays [ref:underlay-spec§8] [type:Backlog] [P1] [subject:CAD] [done:2026-04-13]

- [x] Add underlay section to browser tree with context menus [ref:underlay-spec§9] [type:Backlog] [P1] [subject:CAD] [done:2026-04-13]

- [x] Add PDF DPI dropdown to import dialog [ref:underlay-spec§10.2] [type:Backlog] [P2] [subject:CAD] [done:2026-04-24]

- [x] Add PDF import mode toggle (vector/raster/auto) to import dialog [ref:underlay-spec§10.3] [type:Backlog] [P2] [subject:CAD] [done:2026-04-24]

- [x] Update refresh-from-disk to preserve new underlay state [ref:underlay-spec§11] [type:Backlog] [P2] [subject:CAD] [done:2026-04-24]

- [x] Unit tests for underlay path resolution and serialization — 36 tests covering dataclass fields, to_dict/from_dict, backward compat, path edge cases, relativization [ref:underlay-spec§14.1] [type:Backlog] [P2] [subject:Testing] [done:2026-05-01]

- [x] Integration tests for underlay file-not-found and refresh — 44 tests covering placeholder creation, Z-ordering, display settings, hidden layers, remove, refresh, geom_to_item [ref:underlay-spec§14.2] [type:Backlog] [P2] [subject:Testing] [done:2026-05-01]

- [x] **Underlay Manager (modeless, consolidated)** — shipped on `feat/underlay-manager` (2026-08-29; grill→plan→subagent-driven build, 20 tasks, two-stage reviews per task, ~3400 suite green + user smoke test). A dedicated **modeless** manager window (expandable tree table: Name/Source · Type · Vis · Snap · Colour · Weight · Levels-as-chips; per-source-layer child rows for vis/colour/weight overrides) is now the **single home** for underlay management; adopts the prototype aesthetic (own `underlay_manager_theme.py` for now — see house-theme follow-up). **Schema:** `Underlay.level:str`→**`levels:list[str]`** (explicit multi-level, `["*"]`=all levels, transparent backward-compat migrate); new **`snap:bool`** (per-underlay OSNAP; replaces the global "Snap to Underlay" toggle + ribbon button); **`hidden_in_views` DELETED codebase-wide** (per-view exclusion re-homed to the view — follow-up). **Consolidation:** Display Manager "Underlays" tab removed (−821 LOC); Model Browser underlay nodes navigation-only (no layer children / Change-Level / Relink editing); canvas context-menu opacity retired. **Modify flow:** import-dialog prefill + `Model_Space.replace_underlay` overwrites geometry+placement only, **preserves** management fields (levels/colour/line_weight_name/layer_overrides-by-name/hidden_layers/visible/snap); import-dialog **Level combo removed** (new imports default `[active_level]`). New `underlay_manager{,_model,_delegates,_theme}.py`; touches `underlay.py`, `model_space.py`, `level_manager.py`, `level_widget.py`, `paper_display.py`, `underlay_snap_index.py`, `dxf_preview_dialog.py`, `display_manager.py`, `model_browser.py`, `underlay_context_menu.py`, `main.py`. Spec `underlay-workflow.md §16` rewritten + stamped @56c8148. [ref:underlay-spec§16] [type:Task] [P2] [subject:CAD] [done:2026-08-29]
  - **Follow-ups (from the 2026-08-29 build + smoke test):**
    - [x] **House theme adopts the manager palette** — **SHIPPED 2026-08-29** on `feat/design-token-system` (grill→mockup-gated brainstorm→plan→subagent build + holistic review + LIGHT/DARK smoke). Reframed to a **two-layer design-token system**: `Theme` = 16 authored **primitives** (ground/surface/sunken/raised · line/line_strong · ink/muted/faint · accent/accent_ink · selection/selection_active · ok/warn/danger) + derived **semantic** `@property` aliases (keeps consumer names bg_raised/text_primary/… + manager needs accent_soft/chip/…) + `Theme.color()`. DARK dialed from the manager green palette (`accent #63BE8B`, mockup variant B: rounded+roomy, green active grip); LIGHT legible-but-provisional. `underlay_manager_theme.py` **deleted** (manager reads house theme via `build_underlay_manager_qss`); 9 chrome hard-coders migrated + **anti-drift hex-guard** (`# theme-exempt` markers); interim grip-renderer re-color (`model_view` reads selection tokens); **Preferences → UI** theme selector (System/Light/Dark, `detect()` honors `ui/theme`). Spec `docs/superpowers/specs/2026-08-29-design-token-system-design.md`; governing `theming.md` stamped @ba9e090. `theme.py`, `preferences_dialog.py`, `main.py`, `model_view.py`, +9 chrome modules, `theming.md`, `underlay_manager_theme.py` (deleted) [type:Task] [P2] [subject:UX] [done:2026-08-29]
      - **Follow-ups (from the 2026-08-29 design-token-system build):**
        - [x] **Adopt the `SelectionBox` manipulator app-wide** — **v1 SHIPPED 2026-08-30** on `feat/selection-manipulator` (grill→brainstorm→spec `selection-manipulator.md`→plan→subagent build, 10 tasks + holistic review + full-suite gate + live smoke). Scene-level `SelectionManipulator` (ported prototype math in `manip_math.py`) drives model + paper selection: baked **move** (universal, incl. multi-select group), **rotate + scale** on box-native items (RectangleItem/paper viewport/text), 8 handles (circle corners / square midpoints) + rotate knob (style A, theme-derived fill), click-through, Esc-cancel, modifiers, HUD readout + typed input; RectangleItem migrated to **bake-at-rest** (`_angle` data, map overrides); paper `SheetViewport`/`TextAnnotationItem` handle code **retired** with crop×scale / box parity. Self-heals across scene-clears. **Coexists** with the legacy grip system via the `provides_handles_for` seam — which still leaks known interaction bugs (deferred to the Unification roadmap below, which structurally eliminates them). `selection_manipulator.py`, `manip_math.py`, `model_view.py`, `scene_tools.py`, `model_space.py`, `paper_space.py`, `construction_geometry.py`, `dynamic_input.py`, `constants.py` [ref:selection-manipulator] [type:Task] [P2] [subject:Architecture] [done:2026-08-30]
          - **Follow-ups — Unification roadmap (2026-08-30; full plan in `docs/specs/selection-manipulator.md` §Unification Roadmap).** v1 shipped TWO overlapping manipulation systems — the legacy per-item grip protocol (`grip_points()`/`apply_grip()` via `drawForeground` + `scene_tools._find_grip_hit`) and the `SelectionManipulator` — arbitrated by the `provides_handles_for` predicate. That seam is transitional: every v1 smoke bug (double handles, deselect-on-handle-press, stolen press) was the two systems fighting over one item. End-state = ONE capability-driven handle system: each item exposes all affordances (transform + parametric) as `Handle`s; the manipulator is the single renderer/hit-tester/undo-funnel; `grip_points`/`apply_grip` survive only as mutation primitives Handles call. Phased, each step independently shippable + parity-tested:
            - [x] **U1 — universal rigid rotate** — shipped on `feat/u1-universal-rigid-rotate` (2026-08-31, `/todo` grounded → grill → plan → subagent-driven build → holistic review → live smoke). Every parametric item now implements baked `manip_rotate(angle_deg, pivot)` via the reused `CAD_Math.rotate_point(p, pivot, -angle_deg)` primitive (Y-up CCW+; angle-carriers gridline `_angle_deg`/arc `_start_deg`/regpoly `_rotation_deg`/badge `_angle` accumulate `% 360`): WallSegment, Node (pipes ride via itemChange, z_pos preserved), GridlineItem (lock-aware), Room (**group-follow only** via `MANIP_NO_SOLO_ROTATE` — no solo knob), FloorSlab/RoofItem (elevation invariant), Line/Polyline/Circle/Arc/RegularPolygon, DesignAreaBadge (baked `_angle`, dual-serialized `badge_angle`). Pipe stays a documented no-op (derived geometry; lone pipe excluded from the wrap = move parity). Manipulator deltas: shared `_refresh_fittings` across move/rotate/scale bakes; lone-item knob gating. **Bug fixed en route (systematic-debugging, live smoke):** `hit_test` mapped `ItemIgnoresTransformations` handles with plain `mapFromScene` (correct only at m11==1), so the rotate knob was unhittable at the fit-to-view zoom and the press cleared the selection — now mapped via the view's `deviceTransform` (+ `_view()` prefers a visible view; also fixes box-native resize handles at zoom). ~30 new tests (`test_manip_rotate.py` per-item math + `test_manip_rotate_integration.py` live gesture/undo/parity/elevation/zoom-regression); 2 pre-U1 behavior tests updated. Full-suite failures proven pre-existing on `main` (placement-cycle ×3, ribbon prefs-tabs; environmental VTK segfault). `wall.py`, `node.py`, `pipe.py`, `gridline.py`, `room.py`, `floor_slab.py`, `roof.py`, `construction_geometry.py`, `design_area.py`, `network_codec.py`, `scene_io.py`, `model_space.py`, `annotations.py`, `selection_manipulator.py` [ref:selection-manipulator §Unification] [type:Task] [P1] [subject:Architecture] [done:2026-08-31]
              - **Follow-ups (from the U1 build + holistic review):**
    - _Note: "Include underlays in the undo system" (existing item above, P2) is now more visible — manager edits are instant-apply/not-undoable by design._

- [x] ~~Detail-marker rename remap for underlay view keys~~ — **OBSOLETE 2026-08-29:** `Underlay.hidden_in_views` was removed entirely with the Underlay Manager; per-view underlay exclusion is being re-homed onto the view (see the Manager follow-up "View-owned per-view underlay visibility"). No underlay-side view-key remap remains. [ref:underlay-spec§16] [type:Task] [P3] [subject:CAD] [done:2026-08-29]

- [x] Bug: Refresh-from-disk loses import scale and base-point offset — `_commit_place_import` bakes scale/base-point into geometry coordinates but doesn't store them in the Underlay record. On refresh, raw DXF is re-parsed without those transforms. Fix: store `import_scale` and `base_x`/`base_y` on Underlay, re-apply transform on refresh/load instead of baking into geometry. Also affects save/load. `model_space.py:_commit_place_import`, `underlay.py` [type:Bug] [P1] [subject:CAD] [done:2026-04-13]

- [x] Fix snap not working in import dialog preview — three root causes: (1) view parent() returned layout container instead of dialog, (2) Phase 4 sceneBoundingRect failed for thin cosmetic-pen items (2/21387 pass), (3) Phase 1 iterated all children without spatial filter. Fixed by using Qt scene.items() spatial index, storing direct _dialog ref, adding z-value filter for cursor crosshairs. Also: click uses snap point, crosshairs track snap, dialog opens maximized. `dxf_preview_dialog.py`, `snap_engine.py` [type:Bug] [P1] [subject:CAD] [done:2026-04-27]

- [x] Two-point calibration distance input should reflect display units — `QInputDialog.getText` + `parse_dimension` with unit hint. `dxf_preview_dialog.py:_on_pick2_pt()` [type:Task] [P2] [subject:CAD] [done:2026-04-24]

- [x] Replace QDoubleSpinBox with DimensionEdit for Base X/Y in import dialog — Scale and Angle spinners kept (not lengths). `dxf_preview_dialog.py` [type:Task] [P2] [subject:CAD] [done:2026-04-24]

- [x] Add checkbox toggles for underlay and layer visibility in browser tree — file nodes (show/hide, tri-state for DXF), source-layer nodes (show/hide layer). `model_browser.py` [type:Task] [P2] [subject:CAD] [done:2026-04-24]

- [x] Bug: PDF interactive placement (via import dialog) loses vector geometry on save/reload — `import_pdf()` always rendered raster. Fixed by adding vector re-extraction branch: checks `import_mode`, calls `extract_pdf_vectors_sync()` + `_geom_to_item()` pipeline, mirroring DXF reload path. New `_import_pdf_vectors()` helper applies stored import transform, groups items, applies hidden-layer state. `model_space.py:import_pdf`, `model_space.py:_import_pdf_vectors` [type:Bug] [P1] [subject:CAD] [done:2026-04-27]

- [x] **Bug: silent crash untoggling an underlay source-layer in the model browser** — root cause (from WER dump analysis, NOT the toggle code): PyQt6 escalates any unhandled Python exception in Qt-invoked code (slots, timers, virtual overrides like `boundingRect`) to `qFatal()` → silent 0xC0000409 fail-fast in Qt6Core, no traceback. The user's 7 dumps split into two signatures: a Python `boundingRect` override raising during the post-toggle scene-dirty pass, and pyvistaqt's `QVTKRenderWindowInteractor.timerEvent` raising during 3D processing. The per-layer toggle path itself is spec-conformant and never raised in 5 repro variants (incl. the real project + real 176MB DXF). Fix: `main.install_excepthook()` (called first in `main()`) — app survives such exceptions; traceback → stderr + `%LOCALAPPDATA%\FirePro3D\error.log`. Verified empirically (slot exception kills the process without the hook, survives with it); 4 tests (`tests/test_excepthook.py`); 1,319 tests green (chunked). `main.py` [type:Bug] [P1] [subject:CAD] [done:2026-07-10]

## Code Health & Architectural Debt (from 2026-04-29 gap analysis)

- [x] Remove layer system, rely solely on Display Manager — UserLayerManager, UserLayerWidget, per-item user_layer removed. Display Manager is sole visibility/appearance system. `user_layer_manager.py` deleted (648 LOC). Old .fpd files load cleanly (stale keys ignored). [type:Task] [P1] [subject:Architecture] [done:2026-05-13]

- [x] Signal disconnection audit — 302 `connect()` calls audited across 31 files. False positive: all connections are safe. Modal dialogs (11 files) created fresh via `.exec()` and auto-destroyed. Persistent docks wired once in `__init__()` for app lifetime. DXF worker has proper `_cleanup_dxf_worker()` (quit/wait/None). Context menus created fresh each invocation. The sole disconnect needed (viewport scene re-linking in `paper_space.py`) already exists. No memory leaks or stale callbacks found. [type:Task] [P1] [subject:Architecture] [done:2026-05-14]
- _↳ (open parent) Model_Space decomposition_
    - [x] **Slice 2 — `SceneToolsMixin` mixin→composition (B)** — `self._tools = SceneTools(self)`; redirect borrowed state attrs + geometry registries to `self._scene.*`; audit `self.mode` direct writes vs `set_mode` side-effects; keep dispatch `getattr` resolution working. **As-built (slice 2, a715e7a):** `class SceneTools(scene)` in `scene_tools.py`; 62 call sites → `self._tools._X` (model_space 52, scene_io 2, model_view 3); `_press_align`/`_move_align`/`array_items` = thin forwarding stubs on Model_Space; fixed a `hasattr(sc, "_find_grip_hit")` guard the move exposed. `scene_tools.py`, `model_space.py` [type:Task] [P2] [subject:Architecture] [done:2026-08-28]
    - [x] **Slice 4a — `NetworkCodec`, serialize half unified** (c675721) — the 6 hand-serialized types (node/pipe/dimension/note/water_supply/design_area, which lack `to_dict`/`from_dict`) now serialize through `firepro3d/network_codec.py`; `save_to_file` + `_capture_network` route through it, so serialize field lists can't drift. `.fpd` byte-identical. (Slice 3 already fixed the 3 known live divergences surgically.) `network_codec.py`, `scene_io.py`, `model_space.py` [ref:model-space-architecture §4] [type:Task] [P2] [subject:Architecture] [done:2026-08-28]
    - [x] **Slice 4b — deserialize half unified** — the 6 hand-serialized types deserialize through `network_codec.deserialize_*`; `load_from_file` + `_restore_network` route through it (each keeps its own orchestration); net −252 LOC off `model_space.py`+`scene_io.py`; parity/byte-identical/undo-stable tests green; smoke-confirmed. (`feat/model-space-slice-4b-deserialize`, merged 648b6b9). `network_codec.py`, `scene_io.py`, `model_space.py` [ref:model-space-architecture §4] [type:Task] [P2] [subject:Architecture] [done:2026-09-02]
    - [x] **Underlay domain slice** — the first domain-concern slice: underlay/import (~28 methods, ~10 state attrs, async DXF worker, cache, `place_import`) extracted to `firepro3d/underlay_controller.py` (**`class UnderlayController(scene)`**, a plain object). Pure behavior-preserving relocation: `.fpd` byte-identical, undo untouched (concern was already excluded from `_capture_network`), full back-compat by delegation (`underlays` read-property + 8 method shells + `underlaysChanged` re-emit — **repoint nothing**, 10 prod files + ~200 test sites unchanged). Freeze controller deliberately STAYS on the scene (view-gesture concern reached via `model_view.py` `hasattr` guards + ~40 tests — would trip the hasattr trap). `place_import` dispatch handlers stay as scene-side forwarders (dispatch tables untouched); `set_mode` teardown → `UnderlayController.clear()`. `model_space.py` underlay refs 150→78. Grill→design→plan→9-subagent build (C0 characterization net → C1 scaffold → C2a–d method moves → C3 clear()+RED-demo → C4 full-suite gate → C5 spec stamp); full-suite gate caught 2 bugs the targeted set missed (`_append_geom_to_path` static-shell regression; DWG temp-file-leak from a deleted bridge property), both fixed. Smoke-tested 2026-09-02 (the reported "30° import" was a pre-existing sticky dialog-rotation field, branch-vs-`main` parity proven — NOT the slice). (`refactor/model-space-underlay-slice`). `underlay_controller.py`, `model_space.py`, `scene_io.py`, `tests/test_underlay_slice_parity.py` [ref:model-space-architecture §5] [type:Task] [P2] [subject:Architecture] [done:2026-09-02]

- [x] Unchecked `.get()` None-return audit — all `level_manager.get()` and `user_layer_manager.get()` calls in `model_space.py` and `elevation_scene.py` already have None guards; remaining files (wall.py, roof.py, floor_slab.py, view_range_dialog.py, etc.) also guarded. [type:Task] [P2] [subject:Code Quality] [done:2026-05-03]

- [x] Define `__init__.py` public API — 46 core types in `__all__` with lazy `__getattr__` imports (no circular import risk). Entities, managers, snap engine, hydraulics, constraints, annotations, construction geometry. `firepro3d/__init__.py` [type:Backlog] [P3] [subject:Architecture] [done:2026-05-03]

- [x] Deduplicate `HAZARD_CLASSES` — defined in both `constants.py:34-42` and `auto_populate_dialog.py`. Remove the copy in `auto_populate_dialog.py` and import from `constants.py`. [type:Task] [P3] [subject:Code Quality] [done:2026-05-02]

## Pipe Placement Bug Fixes (from `docs/specs/pipe-placement-methodology.md` §9)

- [x] Fix `find_nearby_node()` Z-blind snap — currently matches nodes on any level by XY proximity. Add `z_hint` parameter to filter by active level elevation. Causes 5+ downstream bugs on multi-level projects. `model_space.py` [type:Bug] [P1] [subject:CAD] [done:2026-05-06]

- [x] Fix 2D-only geometry checks in pipe placement — added Z-coplanarity filter (`Z_COPLANAR_TOL = 1mm`) to `_would_backtrack`, `_would_backtrack_at`, `_validate_4th_branch`, and connection-limit checks in `_press_pipe`. Risers and cross-level pipes no longer trigger false positives. `model_space.py`, `constants.py` [type:Bug] [P1] [subject:CAD] [done:2026-05-07]

- [x] Fix riser auto-build bypassing `add_pipe()` — all three riser creation sites now route through `add_pipe(_propagate_ceiling=False)`, getting category defaults, geometry updates, fitting updates, and display setup. `model_space.py` [type:Bug] [P2] [subject:CAD] [done:2026-05-07]

- [x] Pipe label clipped by walls on higher levels — labels are now top-level scene items at Z_OVERLAY (200). `Pipe.setVisible()` cascades to label. Non-interactive (rejects mouse clicks/hover). `pipe.py`, `model_space.py` [type:Bug] [P2] [subject:CAD] [done:2026-05-10]

- [x] Riser fitting stacking visibility — `fitting.update()` now checks `other.isVisible()` before hiding the lower fitting. Lower fitting shows when higher stacked node is hidden (outside view range). `fitting.py` [type:Task] [P2] [subject:CAD] [done:2026-05-10]

- [x] Pipe `ceiling_level`/`ceiling_offset` attributes removed — pipe no longer stores ceiling attrs. All consumers read from nodes. Serialization ignores old ceiling fields on load. Ceiling Level/Offset removed from pipe property panel. `pipe.py`, `scene_io.py`, `model_space.py`, `main.py` [type:Bug] [P2] [subject:Architecture] [done:2026-05-10]

- [x] Hidden items reappear after level switch — `_set_level_vis()` now checks `_display_overrides["visible"]` and early-returns. Universal fix for all entity types. `level_manager.py` [type:Bug] [P2] [subject:CAD] [done:2026-05-10]

- [x] Riser pass-through indicator in plan view — vertical pipes render a yin-yang SVG symbol at XY location. Shows when no visible endpoint has horizontal pipes. Vertical pipe `shape()` returns circular hit area for split_pipe. `pipe.py`, `riser_passthrough.svg` [type:Task] [P2] [subject:CAD] [done:2026-05-10]

- [x] Fitting visibility when pipe is hidden — fittings group added to model browser with individual hide/show. `fitting.update()` respects `_display_overrides["visible"]`. `_hide_items`/`_show_items`/`_show_all_hidden` handle Fitting objects. `fitting.py`, `model_space.py`, `model_browser.py` [type:Task] [P2] [subject:CAD] [done:2026-05-10]

## Import

### PDF Import Polish (cluster) — 2026-08-28, branch `feat/pdf-import-polish` (SHIPPED, smoke-tested)

- [x] **(5) Underlay Manager icon** — dedicated `underlay_icon.svg` (document-over-underlay, occluded portion cut off, matched fillets; two-token; mockup-gated + user-approved via a localhost preview). Wired into View→Underlay; guard test extended. Also removed the ribbon **Refresh All** button (per-underlay refresh stays in the browser context menu). `graphics/Ribbon/underlay_icon.svg`, `main.py`, `test_icon_theming.py` [type:Task] [P3] [subject:UX] [done:2026-08-28]

- [x] **(1) DPI/import-mode preference wiring** — `ImportPane` gains PDF DPI + import-mode defaults (`import/pdf_dpi`/`import/pdf_import_mode`); the dialog seeds its combos from them (one-off, no write-back). `preferences_dialog.py`, `dxf_preview_dialog.py`, `test_import_prefs_wiring.py` [type:Task] [P3] [subject:UX] [done:2026-08-28]

- [x] **(3) Multi-page PDF page selection by name** — `pdf_page_names()` (PyMuPDF label else "Page N"); thumbnail-strip captions + selected-name echo in the info label. Single-page import unchanged. `pdf_import_worker.py`, `dxf_preview_dialog.py`, `test_pdf_page_names.py` [type:Task] [P3] [subject:CAD] [done:2026-08-28]

- [x] **(2) PDF vector line-width preservation** — `_extract_path` carries stroke width; the underlay builder sub-batches by width (one cosmetic pen per bucket, floored at `UNDERLAY_LINE_WIDTH_PX`); a DM per-file Line-Weight override flattens (wins); `repen_underlay` preserves each child's width (data(7)) on live DM edits. DXF unchanged (no width → one bucket). `pdf_import_worker.py`, `model_space.py`, `test_pdf_line_width.py` [type:Task] [P2] [subject:CAD] [done:2026-08-28]

- [x] **(6) PDF text sizing/offset fix** — text rendered ~1.33× oversized + offset (point-size against a point-path). Fixed to pixel/**fractional** sizing at the span **baseline origin**, x-scaled to the source span **width** (no substitute-font drift). Verified glyph-for-glyph against a real reference PDF (overlay). Both `_append_geom_to_path` copies. `pdf_import_worker.py`, `model_space.py`, `dxf_preview_dialog.py`, `test_pdf_text_render.py` [type:Bug] [P2] [subject:CAD] [done:2026-08-28]

- [x] **(7) PDF architectural/engineering scale presets + ratio display** — pick a plot scale (`3/8"=1'-0"`, `1"=20'`, …) → real-world size, no calibration; math verified vs the calibration ground truth (`scale = real_mm/points` → `import_scale = M·25.4/72`). PDF-only (points are physical). Real-size read-out; Custom/calibrated factor annotated with its ratio (`1:M` + named scale). `dxf_preview_dialog.py`, `test_pdf_arch_scale.py` [type:Task] [P2] [subject:CAD] [done:2026-08-28]

- [x] **(4) Shared snap renderer (DRY)** — **→ shipped in Import Dialog Polish v2 cluster item (5) (2026-09-02, `9939e3c`).** the snap-indicator painter is hand-duplicated in `model_view.drawForeground` and `_PreviewView.drawForeground` (drifted: main scene has `source_item2` + filled `face-`; import dialog neither). Detection already unified via `SnapEngine`. Extract `paint_snap_indicator(painter, view, snap_result)` into `snap_engine.py`; both call it. Parallel snap session has landed — **ready to build**. `snap_engine.py`, `model_view.py`, `underlay_import_dialog.py` [type:Task] [P2] [subject:CAD]
  - **Follow-ups filed 2026-08-28:**
    - [x] **Two-field ratio Custom input** — **→ shipped in Import Dialog Polish v2 cluster item (4) (2026-09-02, `1615e48`).** replace the single mm-per-point factor field with a `[paper unit] = [real unit]` ratio input; calibration fills it. (Read-only ratio *display* shipped; two-field *input* deferred.) `underlay_import_dialog.py` [type:Task] [P3] [subject:UX]
    - [x] **DXF/DWG architectural scale** — **→ shipped (reframed) as Import Dialog Polish v2 cluster item (3) (2026-09-02, `efa18b0`):** grill 2026-09-01 decided arch presets stay PDF-only (wrong for real-units DXF); scope shrank to the `$INSUNITS` 25.4× unit-bug fix. `_DXF_INSUNITS` "to-inches" factors are inconsistent with `scale = real_mm/source_units`; a mm DXF auto-detects ~25.4× off. `underlay_import_dialog.py` [type:Bug] [P2] [subject:CAD]
    - [x] **De-dup `_append_geom_to_path`** — **→ shipped in Import Dialog Polish v2 cluster item (6) (2026-09-02, `2793b22`); §17.4 de-dup follow-up fully discharged.** the whole geom→path builder (not just text/snap) is duplicated in `model_space` and `underlay_import_dialog`; extract one shared helper. `model_space.py`, `underlay_import_dialog.py` [type:Task] [P3] [subject:Code Quality]

- [x] DWG import with layout selection — .dwg files import via ODA File Converter (DWG→DXF conversion) through existing DXF pipeline. Always imports Model space (paper layouts are viewport references, not geometry). Converted DXFs saved to UNDERLAY_REF/. ODA auto-detected from PATH/QSettings/common dirs. ODA GUI suppressed. `dwg_converter.py`, `underlay.py`, `dxf_preview_dialog.py`, `model_space.py`, `scene_io.py`, `model_view.py` [type:Task] [P2] [subject:CAD] [done:2026-05-14]

- [x] Import dialog loading progress bar — reusable `LoadingBar` widget (`loading_bar.py`) with shared blue style, indeterminate/determinate modes, cancel button. Replaces modal QProgressDialog for DXF, drives DWG multi-stage progress, wraps PDF extraction. Splash screen shares same style. `loading_bar.py`, `dxf_preview_dialog.py`, `main.py` [type:Task] [P2] [subject:CAD] [done:2026-05-15]

- [x] Fix silent crash importing large multi-layout DXF (Sleeman Vernon Master, Basement layout, 169MB / ~49.6k entities) — root cause: `LoadingBar.update/start/start_determinate/busy` called `QApplication.processEvents()` from worker progress/status signal handlers that are themselves dispatched *during* `processEvents()` → unbounded re-entrant event-loop recursion → C-stack overflow (Win32 0xC00000FD in Qt6Gui; invisible to Python's recursion machinery). Fixed by `self.repaint()` (immediate widget paint, no event dispatch) instead of `processEvents()`. Also: made the import bar descriptive (per-phase labels read/scan/extract/clip/sheet-annotations + live `current / total` counts) and smoothed progress (worker `usleep` yield after each emit so the bar advances stepwise instead of jumping 200→48.8k). 5 regression tests incl. one asserting LoadingBar never calls `processEvents`. Verified on the real file (was 4/4 crash → 6/6 clean). `loading_bar.py`, `dxf_preview_dialog.py`, `tests/test_import_dialog_progress.py` [type:Bug] [P1] [subject:CAD] [done:2026-06-23]

- [x] DWG/DXF import: text alignment, rotation, and entity coverage — MTEXT attachment_point alignment (top-left default fix), TEXT get_placement() for correct anchor, TEXT/MTEXT rotation, INSERT ATTRIB text extraction, LEADER/MULTILEADER/SOLID/POINT entity support. Gridlines/dimensions confirmed extracting correctly but positioned at separate model-space coordinates (Revit DWG viewport layout). `dxf_import_worker.py`, `dxf_preview_dialog.py`, `model_space.py` [type:Bug] [P2] [subject:CAD] [done:2026-05-16]

- [x] Import dialog: preview placeholder text doesn't update on layout selection — **→ shipped in Import Dialog Polish v2 cluster item (7) (2026-09-02, `b17ea37`).** after picking a layout, the preview area still reads "Select a layout to preview." while the loading bar extracts geometry; it should read "Loading <layout name>…" during extraction (user-reported 2026-07-09 with screenshot, ~49.6k-entity DWG). `underlay_import_dialog.py` [type:Bug] [P3] [subject:CAD]

## Paper Space Follow-Ups (from 2026-05-11 implementation)

- [x] Title block template editor — shipped on `feat/titleblock-templates` (2026-07-21 grill + brainstorm; spec `docs/specs/titleblock-template-system.md`; ~35 commits, subagent-driven with two-stage reviews; 3 smoke rounds). **Parametric** single-size templates (grill: replaces the DXF-artwork/ATTDEF plan — that chain is now the no-template fallback only): margins, filleted/sharp bordered areas, right-strip stack of typed cells (field/static/logo/revision-table/stamp; per-cell fill+border+text style; **static/dynamic sizing** — dynamic rows absorb leftover strip height so the strip fills exactly). New `titleblock_template.py` (dataclasses + pure solver + `%APPDATA%` library + `.fpd` embed w/ push/pull/keep divergence prompt + legacy-field migration w/ shipped-defaults filter), `titleblock_editor.py` (single-column editor: Overview[Paper Setup+Preview]/Drawing Area/Info Strip tabs, size dropdown + orientation radios — **template drives the sheet's page**, live preview via the real renderer, snapshot undo, `parse_dimension` inputs), `TitleBlockTemplateItem` renderer + value model (auto→sheet→Project Info) + panel protocol + `RevisionsDialog` (undo-routed; `TitleBlockDialog` retired). Bonus fixes en route: viewport **view-title pt→mm** (§4.11 bug, part), app-wide `QRadioButton::indicator` QSS gap, File→New project-info leak, legacy `TitleBlockItem` KeyError crash on migrated sheets. ~250 new tests; full suite 1717+75 green. `titleblock_template.py`, `titleblock_editor.py`, `paper_space.py`, `paper_commands.py`, `paper_export.py`, `scene_io.py`, `theme.py`, `main.py` [ref:titleblock-template-system] [type:Task] [P1] [subject:Architecture] [done:2026-07-22]
  - **Follow-ups (user, 2026-07-22 smoke round 3):**
    - [x] **"Fields" + "Arrangements" editor tabs** — shipped on `feat/titleblock-fields-arrangements` (2026-07-22 grill + brainstorm; spec rev 3; 11-task subagent-driven build with two-stage reviews + holistic seam review; 1 smoke fix round). **Rev-3 model:** `CellSpec`/`cells` → **`FieldDef` pool + `Slot` rows** (pairing = two-slot row; unrepresentable-invalid pair states gone); cell kinds collapse to `field` + `revision_table` (stamp = empty field, logo = image-only; a field carries multi-line **`@[Key]` token text** AND an optional image — remainder-band stacking, DD-15). Token engine: known-empty → empty, unknown → literal + warn; known-key set = `build_field_values().keys()` (seeded from `PROJECT_STD_KEYS` + `DEFAULT_TITLE_BLOCK_FIELDS`, one home; editor `_SAMPLE_VALUES` derived from the same constants). **Fields tab**: roster w/ ●/○ placed indicators + intrinsics form (multi-line text + Insert-field token menu, image thumbnail, type combo) + live single-cell true-render preview. **Arrangements tab** (new `titleblock_arrange.py`): pool cards | true-render strip canvas (manual drag machinery — no native DnD; place/reorder/pair/swap/unplace gestures, Esc/focus-loss cancel, select+Delete unplace, adaptive insertion bands, **dry-run drop prediction** = zero empty undo snapshots, white paper backdrop + fit-on-first-show) | placement props + relocated strip-border group; inline solver-warning banner. One-way idempotent rev-2 migration (library + `.fpd`; frozen-parity regression vs `23fa804`); seeded default re-authored natively. ~180 new tests (widget-driven, sabotage/red-verified); full suite 1840+75 green. `titleblock_template.py`, `titleblock_arrange.py`, `titleblock_editor.py`, `paper_space.py`, `constants.py` [ref:titleblock-template-system] [type:Task] [P2] [subject:UX] [done:2026-07-24]
      - **Follow-ups (from the 2026-07-24 build + reviews):**
      - **Follow-ups (user, 2026-08-04):** — all six shipped as the **editor-UX batch** on `feat/titleblock-editor-ux` (2026-08-04 grill; subagent-driven build, two-stage reviews per task + holistic seam review [3 SHOULD-FIX carry-ins fixed: saved use-intent survives Close, corrupt-embed guard in the live-save slot, preview re-solve for tall content]; ~30 new tests; full suite 1876+75 green @7e89d5d; spec DD-15 revised + DD-19..22 added).
        - [x] **Editor Save without closing** — buttons now Save (ApplyRole, stays open, emits `templateSaved` → MainWindow live-applies embed+sheet on uuid match, corrupt-embed-guarded) / Save && Close (old behavior; double-apply idempotent by contract) / Close (rejects; a SAVED "Use for this project" intent survives Close via `result_saved`). Validation gates both save buttons. `titleblock_editor.py`, `main.py` [ref:titleblock-template-system DD-20] [type:Task] [P2] [subject:UX] [done:2026-08-04]
        - [x] **Tooltips identifying fields in Arrangements** — `StripCanvas.viewportEvent` ToolTip intercept (solved-rect hit-test, name-or-id, hideText off-cell, drag machinery untouched) + pool-card `setToolTip`; stamps + revision table covered. `titleblock_arrange.py` [ref:titleblock-template-system DD-22] [type:Task] [P3] [subject:UX] [done:2026-08-04]
        - [x] **Per-edge cell borders** — `BorderStyle.edge_top/bottom/left/right` (default on; old dicts identical); renderer paints per-edge segments when partial (SquareCap closes corners), closed rect/fillet when all four; fillet requires all four edges (controls disable + tooltip); edge checkboxes only on the Cell Border group (`_BorderGroup(show_edges=True)`), frames untouched. `titleblock_template.py`, `paper_space.py`, `titleblock_editor.py` [ref:titleblock-template-system DD-19] [type:Task] [P2] [subject:CAD] [done:2026-08-04]
        - [x] **Image picker UX + image sizing** — DD-15 revised: `FieldDef.image_height_mm` (0 = legacy remainder band, byte-identical; positive = cell grows to label+image+text, band exactly that height); Image group = 120×72 thumbnail + Choose/Clear + Height (mm) DimensionEdit (0 = auto); Fields preview re-solves at natural cell height so tall content never clips. `titleblock_editor.py`, `titleblock_template.py` [ref:titleblock-template-system DD-15] [type:Task] [P2] [subject:UX] [done:2026-08-04]
        - [x] **Group the Fields intrinsics form** — Identity / Content / Text Style / Fill & Border QGroupBoxes (Bold+Italic one row; Cell Border nested in Fill & Border); layout-only, wiring verbatim. `titleblock_editor.py` [ref:titleblock-template-system DD-18] [type:Task] [P3] [subject:UX] [done:2026-08-04]
        - [x] **Cell text colour** — `FieldDef.text_color` (default `#000000`) through the `_draw_text_mm` choke point: label + body + revision text recolour, divider lines stay black; Text Style swatch picker. `titleblock_template.py`, `titleblock_editor.py`, `paper_space.py` [ref:titleblock-template-system DD-21] [type:Task] [P2] [subject:UX] [done:2026-08-04]
        - **Follow-ups (from the 2026-08-04 build + reviews):**

- [x] Paper space display manager tab — Paper Space and Line Weights tabs added to Display Manager. Per-category color/fill/section/line-weight/opacity/visibility overrides, B&W/Full Color/Custom color modes, named line weight definitions, live preview, project persistence, type-aware viewport rendering. `display_manager.py`, `paper_display.py`, `paper_space.py` [type:Task] [P1] [subject:Architecture] [done:2026-05-13]

- [x] Underlay display management & view assignment — shipped on `feat/underlay-display` (§16 of `underlay-workflow.md`; /todo Large workflow: grill+brainstorm design → §16.2–§16.7 built across ~13 commits with two-stage reviews, smoke-tested). Delivered: per-underlay + per-source-layer colour/line-weight (named weights shared with Line Weights tab; screen-hinted cosmetic px, paper-true mm), opacity/visibility, and **per-view visibility** (hide an underlay in any subset of plan/detail views — `hidden_in_views`) from a new Display Manager **Underlays** tab (rows + Views… menu + snapshot/Cancel + browser sync). Honoured on screen (§7.2 pass clause + `repen_underlay`) and in plotted output (§9.9 override-pass underlay stage: B&W→black, Full Color→effective layer colours, named weights at true mm). Line-weight remove/rename guards scan underlay refs. §16.7 edge wiring: level rename remaps underlay `hidden_in_views` keys **and** `record.level` (else the underlay orphaned + vanished — adjacent bug fixed same loop); detail-marker rename remap has no trigger (no detail-rename path exists — documented dependency). Import-dialog part (a) was already covered by the Level selector (§16.1-3). `underlay.py`, `model_space.py`, `level_manager.py`, `level_widget.py`, `paper_display.py`, `paper_space.py`, `display_manager.py`, `constants.py` [ref:underlay-spec§16] [type:Task] [P1] [subject:CAD] [done:2026-08-10]

- [x] **Gridline bubbles true-scale in paper space** — shipped on `feat/gridline-bubbles-paper-scale` (2026-08-07..08 /todo full workflow: grill → §9.9.1 design → 9-task subagent-driven build with two-stage reviews + holistic seam review, smoke-tested). Bubbles render at true paper size through viewports/PDF/print (WYSIWYG 1:1, min-ratio scale under KeepAspectRatio): label cap height = **Grid Line paper category `bubble_label_height_mm`** (Display Manager Paper Space tab "Label Ht" `DimensionEdit` column; factory 3.0mm → ≈10.4mm head via Consolas metrics; per-item `paper_height_mm` retired, legacy key ignored); category **line weight now actually drives** the gridline line + bubble border (was a dead parameter; factory bumped Very Light→**Medium**, mockup-gated); authoring aids never plot (selection ring/grips/lock/duplicate-orange in all color modes/origin cross). Mechanism = the general §9.9 label system (mutate/render/restore geometry stage) — room/pipe labels adopt later. Bonus fixes en route: **pre-existing idle repaint loop** (override pass echoed `scene.changed` → 1 repaint/event-loop turn — measured, then scene-scoped one-shot guard; per-viewport flags ping-ponged, red-verified), exception-safe pass unwind, removed viewports now `disconnect_source()`. 28 tests (`tests/test_gridline_paper_scale.py`, render-measurement + PyMuPDF export parity, red-verified); full suite 1975+75 green. Specs stamped: paper-space §9.9/§9.9.1 as-built, grid-system §10.2 + stale `paper_height_mm`/`user_layer` rows purged. `gridline.py`, `paper_display.py`, `paper_space.py`, `display_manager.py`, `constants.py` [ref:paper-space§9.9.1, grid-system§10.2] [type:Task] [P1] [subject:CAD] [done:2026-08-08]
  - **Follow-ups (from the 2026-08-07..08 build + smoke test):**

- [x] Underlay caching for fast reload — geometry dicts cached to `.fpd.cache/` sibling directory on import. Project reload skips DXF/PDF re-parsing when cache is fresh. Timestamp-based invalidation. Missing source files render from cache with warning tag. Cache written on import + save. `underlay_cache.py`, `model_space.py`, `scene_io.py`, `underlay.py` [type:Task] [P1] [subject:CAD] [done:2026-05-13]

- [x] Title block field overlay with measured positions — **OBSOLETED 2026-07-22**: the parametric template system (grill decision) replaced the DXF-artwork path entirely; field placement is owned by the template's cell layout. The DXF chain survives only as the no-template fallback with its artwork-baked text. [ref:titleblock-template-system] [type:Task] [P2] [subject:Architecture] [done:2026-07-22]

- [x] PDF export — **single sheet**, vector output via `QPdfWriter` (`paper_export.export_pdf`, off-screen `PaperScene` render, 150/300/600 DPI). Batch multi-page / per-sheet files **deferred** (need multi-sheet management). See `docs/specs/paper-space.md` §7. `paper_export.py` [type:Task] [P1] [subject:Architecture] [done:2026-06-25]

- [x] Print — **single sheet** via `QPrintDialog` (`paper_export.print_sheets`). Batch with mixed page sizes **deferred** (need multi-sheet management). See `docs/specs/paper-space.md` §7.4. `paper_export.py` [type:Task] [P2] [subject:Architecture] [done:2026-06-25]

- [x] **Multi-sheet management** — shipped on `feat/multi-sheet` (2026-08-05..07 /todo full workflow: project-browser spec forged via orphan gate, grill, §19 design, 9-task subagent-driven build with two-stage reviews + holistic seam review, smoke-tested). `SheetManager` (pure invariants, by-reference on `scene._sheets` — **killed the headline bug**: save collapsed the list to [active], load kept only [0]); identity = unique `Sheet.number`; pure-push browser sheet tree (number-keyed rows, instant create, confirm-delete, drag-reorder via guarded dropEvent, placed-view italics in place, selection-preserving pushes); sheet properties panel (Number/Name; paper-tab Esc falls back to it — never blank); sheet ops non-undoable + undo clears on switch (§17.5/§19.1); **uniform paper size** project-wide (mixed sizes deferred behind per-size template library); batch PDF export + print via `paper_export_dialog.py` (multipage / per-file `{number} - {name}.pdf`, overwrite confirm, per-file error attribution); **titleblock Title/Drawing No/Sheet No derive from sheet identity** (smoke-round decision — typed fields retired, legacy adoption w/ shipped-defaults filter). ~75 new tests; full suite green (chunked; env-127 flake mid-scene_tools unrelated). Specs stamped: paper-space §19 as-built, project-browser D1/D2/D3/D5/D7 resolved, titleblock value model. `paper_space.py`, `paper_export_dialog.py`, `project_browser.py`, `titleblock_editor.py`, `main.py` [ref:paper-space§19, project-browser] [type:Task] [P2] [subject:Architecture] [done:2026-08-07]
  - **Follow-ups (from the 2026-08-05..07 build + reviews):**

- [x] Consolidate the two `PaperSpaceWidget` instances — **stale entry, closed 2026-08-05**: already shipped with the 2026-07-02 sheet-annotations prereq (§17.6 "one canonical PaperScene"); `_activate_paper_sheet` reuses the single authoritative widget, one construction site. This item was never checked off. [type:Task] [P2] [subject:Architecture] [done:2026-07-02]
