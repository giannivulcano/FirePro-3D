# TODO — Open Tasks

> Active/backlog tasks only. Completed work lives in `todo_closed.md` (archive). The `/todo` skill reads THIS file for task selection and moves finished tasks to `todo_closed.md`.

## 🎯 Current Focus — MVP: AHJ Package (decided 2026-06-23 grill; full rationale in `DOCS-REVIEW.md`)

### B. Paper-space AHJ output (the MVP blocker — most machinery already exists)
MVP = the plotted **AHJ submittal package (drawings + calcs)** for the Sprinkler Design core of the FPE suite (see `project-suite-vision` / `project-mvp-priority-model` memories). Build order:
- _↳ (done parent) Sheet-space annotations_
    - _↳ (done parent) Replace the text Properties dialog with the right-side property panel + a per…_
        - [ ] **Per-character (rich-text run) formatting** for sheet text — formatting currently applies to the whole block. Spec-level change: `TextAnnotationData` moves from block-level fields to runs (§5.3); QGraphicsTextItem rich-text/QTextCharFormat path; panel + template semantics for partial selections. Needs a spec session first. `paper_space.py`, `docs/specs/paper-space.md §5.3/§9` [type:Task] [P2] [subject:Architecture]
        - _↳ (done parent) Per-viewport view isolation + robust crop clip_
            - [ ] **`WallSegment.paint` cosmetic-pen ignores paper line-weight** — `paint()` builds a local `QPen(...).setCosmetic(True)` and never reads `self.pen()`, so `apply_paper_overrides`/`_apply_generic`'s `setPen(width=lw_mm)` is a no-op on wall outlines (walls plot at a fixed cosmetic width regardless of the paper "Wall" line-weight category). Route the wall outline pen through `_display_color`+category weight like the fill. **Sibling reference:** the pipe line-weight paper-normalization was fixed 2026-09-03 (`fix/pipe-paper-line-weight` → `paper_display._apply_pipe` now divides `lw_mm/paper_scale`, §9.9.1) — the wall version needs the *opposite* fix (make `WallSegment.paint` read the applied pen instead of a hardcoded cosmetic one). `wall.py`, `paper_display.py` [type:Bug] [P3] [subject:CAD]
            - [ ] **Isolation session-batching (perf, only if needed)** — v1 applies/restores per differing viewport; a many-distinct-levels sheet does N sweeps. If it ever lags, batch in `paper_export.render_sheet` (group viewports by level, apply once per level, restore once). Filed in `paper-space.md §6.6`; not needed at current viewport counts. `paper_export.py` [type:Task] [P3] [subject:Architecture]
        - [ ] **Retrofit wall/floor/roof/geometry template persistence** (property-panel spec D0 — pipe/sprinkler/text persist, these reset per session). `main.py`, `model_space.py` [type:Task] [P3] [subject:Architecture]
    - [ ] **Text-box placement polish** (user, 2026-07-20 wrap): (a) click-**drag rubber-band placement** — drag on Add-Text click sets the initial box size (click-only keeps auto-size); (b) property-panel **"Fit to contents"** button — resets `box_height_mm`/wrap to the content extents (undo-routed). `paper_space.py`, `property_manager.py` [type:Task] [P2] [subject:UX]
    - [ ] **Absorb the Modify→Text group into the entity-aware Font group** (ribbon-bar spec D2 end state) — NoteAnnotation gains family/hex-color/underline (data-model upgrade) and the Font group targets model text too (routed via `push_undo_state`); legacy Modify→Text group deleted. `main.py`, `annotations.py`, `font_group.py` [ref:ribbon-bar-spec D2] [type:Task] [P3] [subject:UX]
    - [ ] **Move the Annotations group Draw→Draft** — execute the target taxonomy (Draft = documentation tab: model+sheet annotations, page, plot; Draw = pure geometry+blocks). `main.py` [ref:ribbon-bar-spec §1] [type:Task] [P3] [subject:UX]
    - [ ] **Colored highlight for sheet text** — `opaque_bg: bool` → background color (True→white migration); Word-style highlight palette in Font group + panel. `paper_space.py`, `font_group.py` [type:Task] [P3] [subject:CAD]
    - [ ] **Contextual Modify tab** (from the 2026-07-16 ribbon-bar spec grill, D8) — Revit-style: Modify tab hidden when nothing is selected, appears+activates on selection, contents specific to the selected entity type, disappears on deselect. Replaces the always-visible tab + force-switch. Needs a design pass (per-entity group registry, QTabBar dynamic insert/remove). `main.py`, `ribbon_bar.py` [ref:ribbon-bar-spec D8] [type:Task] [P3] [subject:UX]
    - [ ] **Sheet-text leaders** — right-click add/delete leader; leader properties in the panel. (Deferred in the 2026-06-25 grill; pulled back by smoke test.) `paper_space.py` [type:Task] [P2] [subject:Architecture]
    - [ ] **Sheet-text border property** (user request, 2026-07-20 smoke) — panel dropdown for a printed box border: None (default) / Solid / Dashed / etc.; renders in export as authored; new `TextAnnotationData` field + panel enum row + paint. Distinct from the selection boundary (which is UI-only). `paper_space.py` [type:Task] [P3] [subject:CAD]
    - [ ] Fix latent **point-size ~2.4× PDF over-sizing** in the legacy `TitleBlockItem`/`TitleBlockFieldOverlay` — **scoped down 2026-07-22**: viewport view-titles + the "View not found" placeholder were converted to the mm primitive (`_draw_mm_text`, DPI-regression-tested) with the titleblock-template build; the remaining pt text lives only in the no-template fallback chain (renders for projects without a parametric template). `paper_space.py` [ref:paper-space§4.11] [type:Bug] [P3] [subject:CAD]
    - [ ] Expose paper-space **text as a Display-Manager category** (project-level colour / opaque-bg customization). `display_manager.py`, `paper_display.py`, `paper_space.py` [type:Task] [P3] [subject:Architecture]

### C. Hydraulic calc deliverable
- _Cardinal elevations on sheets: ALREADY WORKS (`ViewResolver`). Riser (MVP): cardinal elevation + imported standard detail — reuse, no build._
- _↳ (done parent) Design-Area Criteria System_
    - [ ] **Storage protection criteria system (NFPA 13 Table 4.3.1.7.1)** — when Room hazard is Miscellaneous/Low-Piled/High Piled Storage, conditional Protection Criteria fields appear (Commodity Classification I–IV/Group A plastics, Type of Storage, Storage Height; ceiling height already computed); table lookup resolves to a design curve (OH1/OH2/EH1/EH2 or "See Chapter 25") + hose allowances + duration; resolved curve inherited by design areas AND consumed by auto-populate; badge STORAGE HEIGHT cell fills. Today all three storage classes disengage inheritance with a warning. Needs its own grill (table encoding scope, Chapter-25 rows, in-rack). `room.py`, `nfpa_curves.py`, `design_area.py`, `auto_populate_dialog.py` [type:Task] [P2] [subject:Sprinkler Design]
    - [ ] **Hose allowance inside/outside split** — WaterSupply gains Inside + Outside hose allowance (old single value migrates to Outside on load); solver total = inside + outside; report + badge HOSE cells (currently TBD) read both. `water_supply.py`, `hydraulic_solver.py`, `hydraulic_report.py`, `design_area.py` [type:Task] [P3] [subject:Hydraulic Calculator]
    - [ ] **Domestic water allowance → solver demand** — currently informational (badge/WaterSupply property only); add to the supply-curve check like hose stream, with report line items. `hydraulic_solver.py`, `hydraulic_report.py` [type:Task] [P3] [subject:Hydraulic Calculator]

### Deferred OUT of the MVP (2026-06-23)
- Arbitrary-angle section-view subsystem — see **"Implement section view subsystem"** (now P3, deferred). Cardinal elevations suffice.

- [ ] Auto-generated one-line riser diagram from pipe/valve topology — desired POST-MVP differentiator (plays to the hydraulic/topology strength). `model_space.py`, new module [type:Task] [P3] [subject:Sprinkler Design]

### Post-MVP order

- [ ] **#1 post-MVP:** Implement selection-mode hub — hover pre-highlight + crossing rubber-band + Tab disambiguation of stacked items + label-only click. Spec done (`docs/specs/selection-mode.md`). [ref:selection-mode-spec] [type:Task] [P2] [subject:Architecture]

- [ ] Doc reorg execution — `docs/specs/`→`docs/design/`, `docs/superpowers/`→`docs/_archive/` (excluded from build), backfill `status`/`applies-to` frontmatter on specs, add a Design nav tab. Milestone-level. See `DOCS-REVIEW.md` Part 3 + `docs/specs/SPEC-INDEX.md`. [type:Task] [P2] [subject:Documentation]

## Tasks
_Architecture posture: arm's-length modules (read the model → emit results/overlays/reports; never entangle logic into `Model_Space`); let the platform/module seam emerge. Thermal radiation = first suite module (kept, NOT MVP)._

### Block System (2D symbol definitions + instances; sibling to Features) — governing spec `docs/specs/block-system.md`
Slice build order per the spec. **S1 LANDED 2026-09-04** on `feat/block-system` (data model + lifecycle): flyweight `BlockDefinition`/`BlockInstance` (shared render-ops, no per-instance geometry copies), scene-owned registry, `.fpd` embed + `_capture_network` undo, legacy `BlockItem` **retired** (resolves the undo-orphan half of the paste bug below; seam-reviewed SHIP; 70-test serialization regression + 17 block tests green). Remaining slices:
- **S2 — Create & place loop — LANDED 2026-09-04** on `feat/block-system-s2` (`de91a93`…`061a838`): `BlocksBrowser` dock + `blockDefinitionsChanged` signal; `place_block` 2-step mode (position→rotation, ghost, Enter=0°, HUD `angle_deg`, repeat-until-Esc); `make_block_from_selection` (consume → def+instance, one undo); the three seam fixes (`translate` movability, `block_instance` copy/paste branch, orphan placeholder); ribbon Make/Insert/Manager. Seam-reviewed (one blocker fixed: ghost teardown on same-mode re-entry). 4 smoke-fix rounds landed (move-harmony via pose-in-geometry, rotate reference-lines + live HUD angle, snap-to-blocks, themed `MakeBlockDialog` + `ThemedMessageDialog`). 48 block/parity tests green; all smoke-tested. **Smoke follow-ups (S2.x):**
    - [ ] **Interactive snapped origin-pick for make-from-selection** — v1 uses the selection bounding-box top-left as the block origin (modeless, testable); wire a `set_scale`-style transient single-click snapped origin-pick so the user chooses the insertion base. `main.py`, `model_space.py` [type:Task] [P3] [subject:UX]
    - [ ] **Snap/ALIGN completeness for blocks (first cut landed 2026-09-04)** — `snap_engine._collect` now emits a BlockInstance's insertion origin (center) + transformed geometry **endpoints**; ALIGN point-acquire follows for free. Deferred: **midpoints/quadrants/intersections** of block geometry (circles emit bézier nodes, not center/quadrants), and **directional ALIGN** (parallel-to-a-block-edge — `_source_item_direction` returns None for blocks today). `snap_engine.py`, `model_space.py` [type:Task] [P3] [subject:CAD]
- **S3 — Library layer — LANDED 2026-09-04** on `feat/block-system-s3` (`897aa11`…`71cdf6a`): shared `app_data.py` `app_data_dir()` (folds in the `_app_data_dir` todo — sprinkler_db + titleblock_template now route through it); `block_library.py` (2-tier `.fpdb` tree + per-Series `index.json`, atomic write, tolerant load, `version` divergence, `source_status`/`reload_from_library`/`delete_from_library`); `themed_confirm` Yes/No; "save to library?" prompt wired into the Make-Block flow. Seam-reviewed SHIP; 57 block/parity tests green. Thumbnails reserved for S4. **S3.x follow-ups (S4):** (a) same-sanitized-name library collision silently overwrites a different-`id` block (uniquify filename or warn) — `block_library.py save_to_library`; (b) `source_status`/`reload_from_library` scan only the def's current Library/Series folder, so a post-save metadata edit misses the library copy (scan by `id` across the tree, or re-file on tier change) — `block_library.py`. [type:Task] [P3] [subject:CAD]
- **Data-folder location setting — LANDED 2026-09-04** on `feat/data-folder-setting`: `app_data.app_data_dir()` honors a persisted override (`paths/user_data_root` in QSettings); Preferences→General→"Data folder" Browse/Reset picker relocates the **whole** FirePro3D data root (blocks + sprinklers + titleblocks), switch-only (existing content not moved). **Follow-ups (deferred, not chosen):** (a) **list of search paths** (personal + shared network "company library" tier, one write target) — the spec's third tier; (b) **offer-to-migrate** existing content on change. Also: Preferences dialog remains a SPEC-INDEX orphan — a governing spec is still owed (this added one setting to `GeneralPane`). [type:Task] [P3] [subject:UX]
- **S4 + S4.5 + S4.6 — LANDED 2026-09-05** on `feat/block-manager-s4` (grill→brainstorm→plan→subagent build, 3 smoke rounds + 3 seam reviews). **S4 Block Manager:** modeless `BlockManagerDialog` (MVC + `FramelessShellMixin`) over an arm's-length `Model_Space` block API (`instance_count`, `delete_block_definition`, `reload_block_definition`, `set_block_metadata`, `blockInstancesChanged`); Delete refused while instanced (undoable), live count, source-status badge, Save/Reload divergence. **S4.5 Load-from-Library (Revit Load Family):** `block_library.load_block_file` + `Model_Space.load_blocks_from_files` (browse-anywhere multi-select `*.fpdb`, batch collision rules skip/replace/refuse, one undo) + `_swap_block_definition` shared with reload; "Open in Editor" stub. **S4.6 Excel-style flat autofilter:** flat sortable `QTableView` + `BlockFilterProxy` per-column multi-select+search funnel popup (`FilterHeader`/`_FilterPopup`), numeric `SortRole`; superseded the interim tree. **Details panel is read-only — metadata editing reserved for the Block Editor (v2).** Thumbnails deferred. 81 block/theme tests green; governing spec `block-system.md` + design `docs/superpowers/specs/2026-09-04-block-manager-s4-design.md`. **S4.x follow-ups:**
    - [ ] **Block thumbnails** (the deferred S4 "+ thumbnails") — render `BlockDefinition.render_ops()` → `QPixmap`; PNG-next-to-`.fpdb` + `index.json` `thumbnail` field; keyed `(id, version)`; preview column in the Manager + Blocks browser. `block_manager.py`, `block_library.py`, `blocks_browser.py` [type:Task] [P3] [subject:UX]
    - [ ] **Wire "Open in Editor"** to the real Block Editor once it exists (currently a stub message); the Manager's read-only detail fields become editable there via the existing `Model_Space.set_block_metadata`. `block_manager.py` [type:Task] [P2] [subject:UX]
    - [ ] **Default / preloaded blocks** — a curated block set shipped with the app + auto-preload into a project via project templates / user profile (user's stated direction; S4.5 loads only what's on disk). [type:Task] [P3] [subject:Architecture]
    - [ ] **Stale-`.fpdb` cleanup + scan-by-id** — editing library/series (in the future Editor) or re-saving under a new name leaves the old `.fpdb`; source-status keys by `(library,series,id)` so a re-filed block reads `project-only`. Re-file on tier/name change (or scan by `id` across the tree). `block_library.py` [type:Bug] [P3] [subject:CAD]
    - [ ] **Load-collision: rename-on-load** — loading a different-`id` block whose `(library,series,name)` is already loaded is refused; offer a rename-on-load instead. `model_space.py`, `block_manager.py` [type:Task] [P3] [subject:UX]
    - [ ] **Autofilter polish** — group-count roll-up N/A (flat); consider: funnel vs native sort-indicator overlap on the right edge (widen band or suppress native indicator); footer "showing X of N" wording; persist column widths/sort. `block_manager.py` [type:Task] [P3] [subject:UX]
- [ ] **S5 — Icons & polish** — author themed Blocks-group ribbon icons (Make/Insert/Manager), mockup-gated per `icon-style-guide.md`; S2–S4 run on the placeholder-icon fallback until then; final smoke. [type:Task] [P3] [subject:UX]
- [ ] **Feature re-architecture (deferred sibling milestone)** — the locked-but-unbuilt Feature contract from the block grill: 3-tier `Class/SubClass/Type` + `.fpdf`, **non-parametric** (size = read-only attr of the Type def), openings decomposed into `Door`/`Window`/`Opening` Classes, + the Feature **projection-map** (which Block draws a Feature in plan/each elevation; no plan block → fallback to true 3D projection) + Feature Editor projection panel + "Generate from 3D". Naming/extension locked in `block-system.md`; supersedes the Feature naming item at the Opening-element cluster. Own milestone. [type:Task] [P2] [subject:Architecture]
- _↳ (done parent) Import Underlay dialog_
    - _↳ (done parent) Underlay Manager chrome-match + import polish_
        - [ ] **`levels` management-vs-placement reconciliation** — `apply_import_params_preserving_management` / `replace_underlay` no longer preserve `levels` (now dialog-authored placement), so `test_underlay_modify_flow.py::{test_apply_import_params_preserves_management_fields,test_replace_underlay_preserves_management_and_identity}` fail (pre-existing, 2 tests). Decide: update the tests to the placement-authored model, or restore levels as management. `underlay.py`, `model_space.py`, `tests/test_underlay_modify_flow.py` [type:Bug] [P3] [subject:CAD]
        - [ ] **DRY the Modify pending-state consume** — fold the 3 consume sites (sync PDF + `_on_extract_finished` + memoized `_extract_for_layout`) into one `_consume_pending_modify_state()` helper and remove the side-effecting pending-layer read from `_populate_layer_list`. `underlay_import_dialog.py` [type:Task] [P3] [subject:Code Quality]
        - [ ] **App-wide combo font warning** — the dialog-scoped fix leaves `QFont::setPointSize<=0` firing for other combos (incl. the Manager `LevelsDelegate` editor). Root fix = change `theme.py`'s global `QWidget { font-size: 13px }` (pixel → `pt`) once, app-wide; verify no DPI regression. `theme.py` [type:Task] [P3] [subject:UX]
        - [ ] **`FramelessShellMixin` governing spec** — currently documented as a convention in `theming.md`; forge a short governing spec if a 3rd dialog adopts it or it grows resize/DPI complexity. `frameless_shell.py` [type:Task] [P3] [subject:Architecture]
        - [ ] **Dark-theme white-on-accent contrast** — `on_accent=#ffffff` on the dark accent `#63BE8B` (light green) may read low-contrast on primary buttons/switch-checked; if so, use `accent_ink` for dark specifically. `theme.py` [type:Task] [P3] [subject:UX]
    - [ ] **Per-element underlay selection** (original ask #2) — select/hide individual geometry elements within an underlay; needs its own spec (conflicts with the batched-QPainterPath perf model). `model_space.py` [type:Task] [P2] [subject:CAD]
    - [ ] **App-wide typography role sweep** — apply the Title/Body/Overline/Control roles across ribbon group labels + docks (import dialog + DimensionEdit done). `theme.py`, `main.py`, `ribbon_bar.py`, `property_manager.py` [ref:theming.md] [type:Task] [P3] [subject:UX]
- _↳ (done parent) Import Dialog Polish v2_
    - [ ] **PDF custom scale not persisted to QSettings** — `_save_settings` reads the hidden raw `_custom_scale_edit` during a PDF-ratio session, so a calibrated/typed custom PDF scale resets to the seed next launch (DXF raw-factor persistence unaffected). Persist `_current_scale()` when `_ratio_fields_active()` + back-solve on restore. `underlay_import_dialog.py` [type:Bug] [P3] [subject:UX]
    - [ ] **Import rotation field is sticky across imports** (user, 2026-09-02 underlay-slice smoke — pre-existing, NOT the slice; branch-vs-`main` parity proven). The Placement-page **Rotation** field persists to `UnderlayImport/rotation` QSettings (`_save_settings`/`_restore_settings`) and is restored on the next dialog open, so a rotation set once silently reapplies to **all** subsequent imports (PDF **and** DXF — one shared field/key), which reads as "for some reason everything imports rotated N°". Decide the desired UX: reset rotation to 0 per **fresh** import (keep persistence only for Modify), or make the non-zero rotation more discoverable (highlight when ≠0). Calibration is scale-only (no angle), so this is the only sticky-rotation source. `underlay_import_dialog.py` [type:Bug] [P3] [subject:UX]
    - [ ] **Duplicate underlay drops import fields** — `underlay_context_menu._duplicate` copies neither `selected_layers` nor `import_scale`/`import_base_x/y`, so a duplicated PDF renders all layers at a wrong transform. Same class as the Modify layer bug, different entry point. `underlay_context_menu.py` [type:Bug] [P3] [subject:CAD]
    - [ ] **Name-through-Modify integration test gap** — the "edited name survives `replace_underlay`/`apply_import_params_preserving_management`" path is verified only by reading + unit tests; add an integration test so a future removal of `name` from `_GEOMETRY_PLACEMENT_FIELDS` is caught. `tests/` [type:Task] [P3] [subject:Testing]
    - [ ] **Cancel inert during GUI-thread preview-build** (same root as the spinner stutter, user-accepted) — extraction is cancellable now, but the post-extraction preview-path build blocks the GUI thread so Cancel (and the spinner) freeze there. Real fix = incremental/off-thread preview build (same territory as `underlay-workflow §18` perf work). `underlay_import_dialog.py` [type:Task] [P3] [subject:CAD]
    - [ ] **Calibration snap tolerance is strict (2%)** — a metric `1:10.3` measurement won't snap (only ≤~1:10.15). Bump `_SCALE_SNAP_TOL` to 0.03 if real-world picks land further off. `underlay_import_dialog.py` [type:Task] [P3] [subject:UX]
    - [ ] **PDF reload inline-cache key mismatch** — with a layer subset active, `_import_pdf_vectors` writes the inline cache under a `selected_layers=None` key while the reader keys include `selected_layers`, so the next reload cache-misses and re-extracts once (correct rendering throughout; masked after the first project Save). Fix the write key + tighten the "layer-agnostic cache key" comment (the key does fold in `selected_layers`; the stored geometry is full-page). `model_space.py` [type:Task] [P3] [subject:CAD]
    - [ ] **Snap marker z-order over grip during snapped grip-drag** (cosmetic; documented in `drawForeground`) — the unified painter draws the marker with the trace (before grips), so a grip fill paints over the marker centre during a snapped grip-drag. Restore marker-after-grips only if it visibly bothers. `model_view.py`, `snap_engine.py` [type:Task] [P3] [subject:UX]
    - [ ] **Task-1 minor:** redundant page-0 PDF worker churn on a Modify of a non-zero page (two workers superseded before the target); the PDF error-path test is semi-synthetic (calls `_on_pdf_extract_error` directly rather than a raising worker). `underlay_import_dialog.py` [type:Task] [P3] [subject:Code Quality]
- _↳ (done parent) PDF import geometry inflation_
  - [ ] **DXF geometry inflation — bench-first flatten tuning** (filed 2026-08-30 from task 73; PDF sibling shipped). Bench whether DXF `SPLINE entity.flattening(0.5)` (`dxf_import_worker.py:549`) and the fixed-count ARC/ELLIPSE tessellation (`steps`, `steps=64`) actually inflate a reference DXF underlay **before** changing anything. Note the unit gap: DXF tolerance is in *drawing units* (mm/inch/feet, file-dependent), not paper points — decide unit-normalization (e.g. via `$INSUNITS`/extents) so a fixed number means a fixed plotted deviation. Consider a Preferences knob mirroring the PDF one. Needs a representative DXF underlay to visually gate. `dxf_import_worker.py`, `underlay_cache.py`, `preferences_dialog.py` [ref:underlay-workflow §18.5] [type:Task] [P2] [subject:CAD]
- _↳ (done parent) Unify accent colour on the theme token + style-guide-align the status chrome_
  - [ ] **Full chrome unification (follow-up)** — the deferred remainder of the accent-unify task: (a) migrate the OFF-state pill/toolbar greys (`#888`/`#555`/`#243a4e`/`#3a607e`/`#99bbdd`) and the SNAP toolbar `:checked` background (`#2a5a8a`) onto theme tokens; (b) add **live theme-switch restyling** so a Preferences→UI change repaints without relaunch — needs icon-loader cache invalidation (spec §6 says process-lifetime today) + re-applying the pill/badge/toolbar styles; (c) add `main.py` to the `test_theme_chrome_hexguard.py` allow-list once (a) lands. Bundles with the existing "Latched `detect()` in construction-time consumers" item. `main.py`, `firepro3d/icons.py`, `firepro3d/theme.py`, `tests/test_theme_chrome_hexguard.py` [type:Task] [P3] [subject:UX]
- _↳ (done parent) Revise floor placement workflow_
    - [ ] **Roof twin** — apply this identical two-boundary + wall-mirrored-placement treatment to `roof.py`/`roof_rect` (the agreed fast follow-up; roof is structurally identical to floor). `roof.py`, `model_space.py` [type:Task] [P2] [subject:Architecture]
    - [ ] **Paper-viewport dynamic view-height parity** — T4's `compute_view_height` recompute is scoped to on-screen `_apply_plan_level`; paper `SheetViewport` still renders from the cached `pv.view_height` via the resolver. Wire the dynamic upper-bound (or the explicit flag) into paper rendering so plotted sheets match on-screen. `paper_space.py`, `paper_export.py`, `level_manager.py` [type:Task] [P3] [subject:Architecture]
    - [ ] **Wall/roof template-name behavior diverges from floor** — floor now seeds placed instances from the template name (uniquified "Slab/Slab 1/…"); walls/roofs still auto-number "Wall N"/"Roof N" and show a vestigial editable template Name. Decide whether to unify them onto the floor behavior. `model_space.py` [type:Task] [P3] [subject:UX]
    - [ ] **Floor polygon Ctrl angle-constraint** — `_move_floor` (polygon) publishes the raw snapped point; `_move_wall` applies `_constrain_angle` under Ctrl before publishing. Add Ctrl angle-snap parity to floor polygon segments. `model_space.py` [type:Task] [P3] [subject:CAD]
    - [ ] **T5 placement test-hardening gaps** (holistic-review notes): typed-HUD-dimension commit test for floor rect; center-rect 3-step placement test; cross-primitive-cycle-guard (cycling mid-placement refused) test; Delete-pop test for the floor polygon. `tests/test_floor_placement_workflow.py` [type:Task] [P3] [subject:Testing]
    - [ ] **RegularPolygon as a floor boundary primitive** (dropped scoping option, 2026-08-27) — register the parametric N-gon into the floor ←/→ cycle (↑/↓ sides), if wanted. `model_space.py`, `floor_slab.py` [type:Task] [P3] [subject:CAD]

- [ ] **Doors at ground level (elevation 0) don't render in plan/sheets until a level switch** (user, 2026-08-27 paper-space-bundle smoke test — NOT yet root-caused; a live-only render bug, needs the running app). **Symptom:** a door hosted on a ground-level (elevation 0) wall shows a **solid wall, no door** in plan view AND on sheets; the door **is** visible in 3D; switching the active level away and back (or nudging the level elevation off 0) makes it appear and it persists. **Ruled out (headless, proven):** the data/z/visibility/geometry are all correct through the FULL save→`load_from_file`→`apply_to_scene` path — after load the opening is in `wall.openings`, `isVisible()==True`, z-pinned above its host wall (`op.z=30.83 > wall.z=30.78`, the §585 `Z_CAT_OPENING` pin), path non-empty, `z_range=(0,2032)`. `_apply_z_filter` at `[view_depth=-1000, view_height=2000]` keeps it visible. The plan gap is drawn by `WallOpening._paint_symbol` filling `_gap_rect` with the scene-bg colour (needs `op.z>wall.z`, which holds) — **NOT** `_is_section_cut`-dependent. A MainWindow `_load_project` test asserts the door visible after load and **passes** (doesn't reproduce). A speculative "build ViewResolver before `_activate_plan_view`" fix was tried and **reverted** — it's a functional no-op (the resolver wraps the same `plan_view_mgr`/`level_mgr` objects either way). **Leading hypothesis:** a **live-only stale-paint / missing viewport invalidate on first load** — the correct state is set but the fresh `Model_View`'s rendered output isn't repainted for the opening's overpaint region until a full invalidate (level switch → tab-change repaint). **Next (needs the live app):** decisive probes — (a) does **zoom in/out** alone (repaint, no `apply_to_scene`) fix it? → pure stale-paint → force a repaint/`scene.update()` after load; (b) is the door **selectable** where it should be (item present, not painted) vs nothing there? Also compare vs `main` to confirm pre-existing vs a bundle regression. `wall_opening.py` (`_paint_symbol`/`_gap_rect`), `model_view.py`, `main.py` (`_apply_loaded_file`/`_activate_plan_view`), `level_manager.py` [type:Bug] [P2] [subject:CAD]


- [ ] **ALIGN: Parallel-OFFSET tracking (perpendicular offset from a reference line)** (user, 2026-08-26 ALIGN smoke test — the parallel behavior actually wanted). The shipped direction-acquire "Parallel" only constrains *direction* (draw parallel, length along it) and is **defaulted OFF** (`ALIGN_DIR_PARALLEL_DEFAULT=False`, flaky track-swap flicker). What the user wants is **in-placement OFFSET**: acquire a reference line, then a typed distance places your point/line **parallel to the reference at that perpendicular offset** (AutoCAD OFFSET as a live snapping aid). Distinct feature — needs its own **grill→design→build**: side selection (which side of the line), snap-to-offset-guide vs type-distance, single offset vs repeating array, how the offset guide renders, and whether it replaces or coexists with the direction-track parallel. Reuse the direction-acquire plumbing (`align_controller.py` flavor="direction", `align_engine.py` parallel ray) but the ray becomes an offset *line* (parallel at distance D) and the HUD field is a perpendicular offset, not along-path distance. `align_engine.py`, `align_controller.py`, `model_space.py`, `dynamic_input.py`, `preferences_dialog.py`, `docs/specs/align-placement.md` [type:Task] [P2] [subject:CAD]

- [ ] **ALIGN: discoverability + persistent render for direction-acquire (Parallel)** (user, 2026-08-26 ALIGN smoke test). A **direction-acquire** (dwell an object's body → acquire its direction for a Parallel guide) currently shows **no `+` marker** (markers are only drawn for *point*-acquires, which have a point; a direction-acquire has `point=None`) — so acquiring a direction is invisible. Worse, tracking guides only render when the cursor is *snapped* to them, so a fixed parallel guide (anchored at the from-point) is invisible until you happen to move onto it → users can't tell Parallel is active or where to snap. **In scope:** (1) a distinct **direction-acquired indicator** (e.g. a "∥" glyph on the acquired reference segment, or a highlighted segment) so the acquire is visible; (2) **persistently render the fixed parallel guide** (dashed construction line through the from-point) once a direction is acquired + a from-point exists, instead of only-when-snapped — the AutoCAD alignment-path feel; consider whether other acquired guides (extension/perpendicular) should also show a faint persistent path (weigh clutter — likely keep those snap-only). Relates to the ALIGN acquire model (`align_controller.py`/`model_view.py drawForeground`). `align_controller.py`, `model_view.py`, `model_space.py`, `docs/specs/align-placement.md` [type:Task] [P2] [subject:UX]

- [ ] **Re-evaluate the April pipe-3D work (branch `feature/pipe-3d-fixes`, 10 unmerged commits — kept as reference)** — verified 2026-08-25 that NONE of it is on `main` (not superseded): **3D-vector geometry checks** (backtrack/collinear/4th-branch/wye converted to 3D vectors; new `cad_math` utils `unit_vector_3d`/`dot_3d`/`angle_between_3d`/`outward_vectors_3d`), **vertical riser fittings** (`tee_vertical`/`cross_vertical` + SVGs for through-risers — likely valuable for the AHJ riser deliverable), **Z-stacked riser-node movement** (moving a riser node moves all Z-stacked siblings at the same XY; `z_hint` disambiguation in `find_nearby_node`), contextual `snap_point_45` (uses the reference pipe, not `pipes[0]`), and routing all pipe creation through `add_pipe()`. The branch is **1001 commits behind** → do NOT merge/cherry-pick (heavy conflicts in the evolved `model_space`/`node`/`pipe`); **re-implement the wanted pieces fresh against current `main`, using the branch as reference.** First step: decide which of these features are still wanted. `cad_math.py`, `fitting.py`, `node.py`, `pipe.py`, `model_space.py` [type:Task] [P3] [subject:Sprinkler Design]
- _↳ (done parent) Revise wall placement workflow_
    - [ ] **Arc/circle curved walls** — needs a curved `WallSegment` entity (today it's fundamentally straight; ripples through miter/join/hosting/3D/section/snap). Register the arc/circle primitives into the wall ←/→ cycle once the entity exists. `wall.py`, `model_space.py` [type:Task] [P3] [subject:CAD]
    - [ ] **Polygon walls** — closed loop of N straight mitered `WallSegment`s; ↑/↓ sets N (reserved by the parent task). Resolve the ←/→ vs ↑/↓ key contention (polygon's own placement uses ←/→ for inscribed/circumscribed). `wall.py`, `model_space.py` [type:Task] [P3] [subject:CAD]
    - [ ] **Review wall sub-variant + polygon-wall key scheme** when curved/polygon walls land — wall primitives currently expose no 2D-geo sub-variants (rect corner/centre, arc centre/start); revisit whether they should, and where the keys live. `model_space.py` [type:Task] [P3] [subject:UX]
- _↳ (done parent) Opening element_
    - [ ] **Settle the Feature hierarchy naming BEFORE Phase B/C** (raised 2026-09-04) — `feature.py` currently names the tiers **Category → Type → FeatureDef** (`features_by_category()` returns `dict[category]→dict[type]→list`), with `host_type` orthogonal. User's Revit-aligned mental model expects **Category → Family → Type** (`user_revit_mental_model`). Decide the canonical names now: renaming after Phase B (Manager UI) / Phase C (Editor) ship — and after any on-disk library keys off `category`/`type` — forces a data migration. Fold the decision into the Feature governing spec forged in Phase B. `feature.py`, `feature_browser.py` [type:Task] [P3] [subject:Architecture]
    - [ ] **Phase B — Feature Manager** (Architecture-ribbon dialog: choose which Features load into the project; template-project prepopulation; Revit "Load Family"). Promote the **Feature system** to its own governing spec here (SPEC-INDEX orphan). `main.py`, new `feature_manager*.py` [type:Task] [P2] [subject:Architecture]
    - [ ] **Phase C — Feature Editor v1 (constrained to void+symbol)** — author new Features by drawing plan/elevation schematics with the 2D-geometry tools + defining size params, wall-cut, `host_type`. Free-form 3D solid authoring deferred to Editor v2 (needs the 3D solid-modeling "operations" system). `main.py`, new `feature_editor*.py` [type:Task] [P2] [subject:Architecture]
    - [ ] **Full parametrics for Features** — beyond the Phase-A scale transform (arbitrary parameter→geometry binding / constraint engine). `constraints.py`, feature modules [type:Task] [P3] [subject:Architecture]
    - [ ] **Feasibility/brainstorm: fold Sprinkler Systems into the Feature framework** (Pipe/Fitting/Sprinkler/Valve/Pump). Reconcile the terminology tension first (there "Feature" = discipline/system grouping vs "Feature = concrete placeable definition" here). Own session. [type:Task] [P3] [subject:Architecture]
    - [ ] **Additional host strategies** — Floor-hosted (table, riser base), Ceiling-hosted (pendent sprinkler, diffuser), Face/Level-free. `host_type` is in the model from day one; wire the placement strategies. [type:Task] [P3] [subject:Architecture]
    - [ ] **3D: true solid CSG + re-mitre wall corners** — Phase A caps opening reveals (watertight shell) and switched `wall.get_3d_mesh` to `quad_points` so opening jambs stay perpendicular on joined walls, but 3D wall *corners* now butt-join (not mitred). Optionally rebuild walls+openings via PyVista `boolean_difference` (true watertight solids, mitred corners) — grounded as the alternative to reveal-capping; also sets up the future Create-tab "operations". `wall.py`, `view_3d.py` [type:Task] [P3] [subject:CAD]
    - [ ] **Opening panel/polish** (holistic-review + smoke follow-ups): remove the inactive **"Level Offset"** field from the Openings contextual tab (reused from `_build_placement_group`; openings have no `_level_offset_mm`); rename the Display-Manager category **"Opening"→"Openings"** for spec parity; remove the now-dead `DOOR_PRESETS`/`WINDOW_PRESETS`/`DOOR_DEFAULT`/`WINDOW_DEFAULT` + the `**_legacy`/`preset` ctor shim in `wall_opening.py`; simplify the redundant `isinstance(item,(DoorOpening,WindowOpening))` in `model_space.py`. `main.py`, `wall_opening.py`, `display_manager.py`, `model_space.py` [type:Task] [P3] [subject:UX]
- _↳ (done parent) 2D geometry: first-class level-plane placement + fill_
    - [ ] **Vertical / "elevation plane" (section-based) anchoring for 2D geometry** — the deferred half of the placement model: draw/anchor 2D geometry on an elevation's vertical cut plane (crosses the current 2D-plan-only authoring contract, `view-relationships §3.1`). Needs its own grill. `construction_geometry.py`, `elevation_scene.py` [type:Task] [P3] [subject:Architecture]
    - [ ] **Elevation-view projection of 2D geometry** — flat 2D geometry now has world-Z but isn't projected into elevation scenes. `elevation_scene.py` [type:Task] [P3] [subject:Architecture]
    - [ ] **3D render + extrude of 2D profiles** — the payoff this task lays the foundation for: render filled closed 2D shapes flat in 3D, then extrude to solids (ties into "Generic 3D solid modeling in the Create tab"). `view_3d.py`, `construction_geometry.py` [type:Task] [P3] [subject:Architecture]
    - [ ] **Hatch scale/density control for 2D fills** — legacy manual angle/spacing was dropped for named patterns; a per-item hatch scale knob (like `draw_section_hatch`'s `hatch_scale`) if patterns read too dense/sparse at architectural scale. `construction_geometry.py` [type:Task] [P3] [subject:CAD]
    - [ ] **Line-weight via the "2D Geometry" Display category** — T10 wired colour/visibility/opacity (mirroring Design Area, which has no weight); add a line-weight field if per-category 2D-geometry weight is wanted. `display_manager.py`, `construction_geometry.py` [type:Task] [P3] [subject:UX]
    - [ ] _Follow-up (deferred 2026-08-24):_ **Explode Polygon → closed polyline** (free per-vertex deformation) + **parametric re-edit polish** + **"Sides" as a HUD COUNT field** (Option A dropped the mixed radius+count schema; sides via ↑/↓ + panel only today). `construction_geometry.py`, `dynamic_input.py` [type:Task] [P3] [subject:CAD]
    - [ ] _Smoke follow-up:_ **Author the `polygon_icon.svg`** (the Polygon ribbon button ships with the missing-icon fallback; icon authoring is mockup-gated per the icon style-guide). [type:Task] [P3] [subject:UX]
    - [ ] **Rename module `construction_geometry.py` → `geometry_2d.py`** (matches `Geometry2DMixin` + the "2D Geometry" ribbon group/category/tab; "construction geometry" is legacy from the retired `ConstructionLine`). Note: can't be `2d_geometry.py` (module names can't start with a digit). Cross-cutting import churn (~15 files: `model_space.py`, `scene_io.py`, `snap_engine.py`, `paper_display.py`, `model_view.py`, `entity_context_menu.py`, `displayable_item.py`, …). Do as its own mechanical commit **after** the 2D-geometry polish cluster lands, to keep the behavior diff clean. [type:Task] [P3] [subject:Code Quality]
- _↳ (done parent) Ribbon overhaul_
    - [ ] Author the ~46 missing ribbon icons per the new style guide (mockup-gated). (Floor icon shipped 2026-08-28 with the floor-workflow task. **Architecture-tab set shipped 2026-08-28** on `feat/architecture-tab-icons`, grill→mockup-gate→build: authored wall/roof/room/door/window/blank/detail/levels; re-authored `gridline_icon.svg` (was hardcoded `#ffffff` → white-on-white in light theme, violated §4.1); re-authored (thinned) `floor_icon.svg` to match wall depth. Axo 3D family (2:1 dimetric matching floor) for Building elements, 2D symbols for openings/datums; two-token compliant; render-through-loader mockup harness at 54/27px light+dark. Guard tests in `tests/test_icon_theming.py` (two-token compliance §4.1 + no-fallback render). ~9 fewer — remaining placeholders live in the Create/Sprinkler-Systems/Analyze/Draft/Manage tabs + Tools/Page groups.) [type:Task] [P2] [subject:UX]
    - [ ] **Populate contextual tabs with per-entity groups** — wire type-specific modify tools + the reusable `_build_graphic_override_group` and `_build_placement_group` (both protocol-gated, currently floor/geo2d-only) into the wall/room/roof/pipe/etc. contextual tabs; carry template persistence for wall/roof (floor done). `main.py`, `model_space.py` [ref:ribbon-bar-spec] [type:Task] [P2] [subject:UX]
    - [ ] **Paper-scene contextual tabs** — wire the paper scene's selection into the contextual-tab resolver (model-scene-only today), then add the Viewport tab (scale presets / show-border / delete) and a Sheet Text tab (migrate the Draft→Font group into it). `main.py`, `ribbon_bar.py`, `docs/specs/ribbon-bar.md §3.8` [ref:ribbon-bar-spec D9, paper-space §19.4] [type:Task] [P2] [subject:UX]
    - [ ] **Forge a Preferences-dialog governing spec** — `preferences_dialog.py` is a new subsystem currently only covered by the design-of-record; promote it to `docs/specs/` + SPEC-INDEX (it's filed in the Orphans table). [type:Task] [P3] [subject:Documentation]
    - [ ] **`dock/radiation` never restored on startup** + **ImportPane forward keys read-back**: `restore_settings` restores browser/properties/hydraulics docks but not radiation; GeneralPane persists a default that has no effect until this is wired. `main.py` [type:Bug] [P3] [subject:UX]
    - [ ] Generic 3D solid modeling (extrude/hole/boolean) in the Create tab. [type:Task] [P3] [subject:Architecture]
    - [ ] **Consolidate settings into the Preferences dialog** — fold the remaining scattered/legacy dialogs (Project Info, Import/Export, Snap/OSNAP, Inference, Display) into the one Preferences dialog ("look here first"); keep the swappable source for the future **user-profile settings layer** (selectable profiles). `main.py`, `preferences_dialog.py` [type:Task] [P3] [subject:UX]
      - [ ] **Normalize the legacy snap dialog's QSettings store** (sub-step of the consolidation) — `_open_snap_tolerance_dialog`/`_open_snap_settings` (OSNAP toolbar right-click + Manage "Snap Settings") write `inference/alignment_guides` via bare `QSettings()`; SnappingPane uses `QSettings("GV","FirePro3D")`. Normalize the legacy dialog + retire the redundant Manage "Snap Settings" button (now in Preferences). `main.py` [type:Bug] [P3] [subject:UX]
- _↳ (done parent) Gridline Revit-aligned UX re-architecture_
    - [ ] **App-wide Y-up display sweep** — property panels currently show raw Qt Y (down-positive) everywhere (sprinkler/node/etc.); gridline was flipped to up-positive for this branch. Sweep the app to a consistent up-positive display/parse convention. `sprinkler.py`, `node.py`, `property_manager.py`, … [type:Task] [P3] [subject:UX]
    - [ ] **Angled gridlines in elevation views** — currently only exactly-cardinal gridlines project into elevations; angled projection deferred (section-view territory). `elevation_scene.py` [type:Task] [P3] [subject:Architecture]
    - [ ] **Perpendicular bubble elbow/leader** — jog a bubble off the line with a leader (Revit "add elbow"); per-view leader independence. `gridline.py`, `paper_space.py` [type:Task] [P3] [subject:CAD]
    - [ ] **On-canvas rotate handle + Display-Manager linetype property** for gridlines (angle currently edited in the panel; linetype is fixed dash-dot). `gridline.py`, `model_space.py`, `display_manager.py` [type:Task] [P3] [subject:CAD]
      - [ ] **Move / copy-paste polish** (user, 2026-08-14 smoke) — add a **right-click (entity + canvas) context-menu entry for Move** (parity with Ctrl+M; matches the Array/Offset menu pattern); consider a copy/paste menu entry too. Also: paste currently exits after one placement — evaluate AutoCAD-style **multi-copy** (stay live until Esc) and richer ghost fidelity for non-gridline entities. **Move-menu point superseded by the transform-tools unification below.** `model_space.py`, `model_view.py`, `entity_context_menu.py` [type:Task] [P3] [subject:UX]
      - [ ] **Transform tools: shortcuts + unified right-click across all geometry** (user, 2026-08-20 smoke) — a cohesive pass over Move/Array/Offset: **(1)** single-key shortcuts **A**=Array, **O**=Offset, **M**=Move; **(2)** add **Move** to the gridline right-click menu; **(3)** relabel the gridline entries — drop the "Gridlines" word, show the shortcut: `Array (A)`, `Offset (O)`, `Move (M)`; **(4)** expand these three right-click entries to **all drawn geometry** (line, rect, circle, polyline, arc, …), not just gridlines; **(5)** rewire the **Transform ribbon** group (`main.py` ~1690, Move/Array/Offset) onto the same unified actions. **Grill first — there are two implementations to reconcile:** the on-canvas gridline-specific replicate (`gridline_array`/`gridline_offset` via `GridlineItem.offset_copy`/`array_copies`) vs the general-geometry ribbon tools (`array_dialog.py`, `offset`/`offset_side` mode). "Expand to all geometry" means deciding which becomes the one tool (likely generalize the on-canvas replicate to any item exposing `translate`, now that `GridlineItem` has one too). `entity_context_menu.py`, `model_view.py`, `model_space.py`, `main.py`, `array_dialog.py` [type:Task] [P1] [subject:UX]
      - [ ] **Extend inference to more clients** — the engine is generic but only gridlines provide references + only gridline placement/grip consume it; add wall/pipe/sprinkler providers + placement-tool + body-drag consumers, and the deferred guide types (wall-proximity, extension, equal-spacing) per `inferred-dimension-driven-placement.md` proposal sections. `inference_engine.py`, `wall.py`, `pipe.py`, `model_space.py` [type:Task] [P3] [subject:Architecture]
      - [ ] **Dynamic Input engine — extract + generalize (§4)** (user, 2026-08-16) — `_DynInput` is a modal `QDialog` nested inside a `model_space.py` method, redefined/instantiated ad hoc per mode; extract to a reusable top-level controller/widget (`dynamic_input.py`), generalize to all placement modes (pipe/wall/arc/rectangle), and (design decision) move from Tab-triggered modal to a live non-modal HUD per §4.3/§4.5; coordinate with the inference engine at the Model_Space seam (typed value overrides snap/inference). Update `inferred-dimension-driven-placement.md §4` in place. `model_space.py`, new `dynamic_input.py`, `model_view.py` [ref:inferred-dimension-driven-placement §4] [type:Task] [P2] [subject:Architecture]
        - **`feat/dynamic-input-hud` — D3 scope COMPLETE (2026-08-20).** Plan + findings ledger in `docs/superpowers/plans/2026-08-16-dynamic-input-hud.md` (gitignored). Shipped: one-HUD lifecycle (S1–S3), Line/Rectangle/Circle schemas, polyline, D2 verdict, **T16** (gridline offset/array), **T15** (move + a `GridlineItem.translate` fix — gridlines were unmovable by the generic move), **T11** (placement cycling moved off Tab to a Left-Shift tap), **T17** (deleted `_DynInput` + dead `_handle_tab_input`; `drawForeground` block 4 kept — 11 non-plan modes still write `_draw_dim_hint`), **T21** (pipe B5 doc) + **T22** (governing spec §4 rewrite + SPEC-INDEX + grid-system sweep). Suite 2708 green; all smoke-tested. **HUD clients — both since landed:** T18 (wall) 2026-08-24 + T19 (pipe) 2026-09-03; see `todo_closed.md`. Plus the three P1 follow-ups below (Space-cycle, arc revamp, unified transform tools) and a known gap: offset_side/rotate/scale/fillet/chamfer lost their Tab exact-input (was dead) — needs re-homing. (**T18/wall landed 2026-08-24** with the "Revise wall placement workflow" build → `_apply_wall_dynamic_input` wired into `_APPLIER_FOR_MODE`, `_move_wall` publishes to the HUD, primitive-aware `active_schema`; covered by `test_wall_placement_workflow.py` "Task 8" section. The Left-Shift cycle tap has since been replaced by **Space** (`Space=align`). Moved to `todo_closed.md` 2026-09-03.)
          - _↳ (done parent) Placement-UX overhaul_
              - [ ] **Merge Line + Polyline into one ←/→ cycle tool** (user, 2026-08-21) — host both under one `draw_line` mode with a line/polyline variant flag (per the `_PLACEMENT_VARIANTS` framework), branching the existing 2-click line vs N-click polyline handlers on it. **Remove the placeholder `K` polyline shortcut** (`Model_View._TOOL_SHORTCUTS` + Polyline tooltip) once this lands. `model_space.py`, `model_view.py`, `main.py` [type:Task] [P2] [subject:UX]
              - [ ] **`arc_span` ArcLength desyncs on undo** — editing Span writes ArcLength via `set_value_mm` (no `valueChanged`), so the HUD undo stack never records ArcLength and a Ctrl+Z restores only Span, leaving the derived field stale. Low impact (ArcLength never reaches the applier). Re-run the coupling after an undo that touches a coupled field. `dynamic_input.py` [type:Bug] [P3] [subject:UX]
              - [ ] **Ctrl/Shift+arrow also cycles a placement variant** — the ←/→ cycle keys in `keyPressEvent` don't check modifiers, so a modified arrow cycles at step 0. Gate on `NoModifier` if it conflicts with any future modified-arrow binding. `model_space.py` [type:Task] [P3] [subject:UX]
              - [ ] **3-point / arbitrary-rotated rectangle as a distinct placement mode** — the current rotate step layers rotation on an axis-aligned 2-click rect; a true 3-point rotated rect (baseline + depth) is deferred. [type:Task] [P3] [subject:CAD]
              - [ ] **Persist the chosen placement variant across restarts** (QSettings) — variant choice is session-sticky only today. [type:Task] [P3] [subject:UX]
              - [ ] **Arc-wall placement mode** — the downstream goal the arc revamp unblocks (arc as a wall centreline). `wall.py`, `model_space.py` [type:Task] [P3] [subject:CAD]
          - [ ] **Retire the orphan `MainWindow.view`** (`main.py:400`) — a `Model_View` attached to the plan scene but never parented, never shown, never added to `central_tabs`, so it is `scene.views()[0]`. It broke the HUD in smoke test (built correctly inside an invisible widget tree). Worked around locally by selecting the first *visible* view, but **~12 other `views()[0]` uses in `model_space.py` have the same latent bug** — dialog parents at ~2190/6438/8106 parent onto the orphan, and zoom-scale reads at ~3634/6783/7227/7749 read its transform. Retiring it (or never attaching it to the scene) fixes all of them at once. `main.py`, `model_space.py` [type:Bug] [P2] [subject:Architecture]
          - [ ] **Qt fixtures must `show()` their view** — the dynamic-input fixtures built a `Model_View` without showing it, which made `views()[0]` trivially correct and hid the orphan-view bug class entirely. Fixed in the dynamic-input test files; **audit the other Qt fixtures in the suite for the same gap.** `tests/` [type:Task] [P3] [subject:Testing]
          - [ ] **Split `node_start_pos`** — it holds a `Node` in pipe mode and a `QPointF` in move/paste mode, and call sites now branch on `isinstance` to cope. Split into `_pipe_start_node` / `_move_start_point` (~30 call sites). Blocks clean work on T15. `model_space.py` [type:Task] [P3] [subject:Architecture]
          - [ ] **De-duplicate the `_other_end(pipe)` idiom** — inlined 5× in `model_space.py` (~1391, 1458, 1488, 5826, 5853); `Node._other_end` is a sixth and the only one guarding the detached-pipe case. `model_space.py`, `node.py` [type:Task] [P3] [subject:Architecture]
          - [ ] **`PolylineItem.last_point()`** — `model_space` reaches into `_points[-1]` directly at several sites. `construction_geometry.py`, `model_space.py` [type:Task] [P3] [subject:Architecture]
          - [ ] **Decide on decimal-comma input** (user call, 2026-08-19) — `parse_dimension`/`parse_angle` reject `1,5` as ambiguous against a thousands separator, and `1e3` as a likely typo. The comma is the natural numpad decimal key on European layouts, which matters now the numpad opens the HUD. Grammar's home is `units-and-formatting.md §3.1`. `scale_manager.py` [type:Task] [P3] [subject:UX]
          - [ ] **Narrow `reject_commit()` if it reads noisy** — a refused commit flags *every* `DIMENSION` field, since appliers refuse on a magnitude and either extent may be at fault on rectangle. Nominate the culprit if that proves annoying in use. `dynamic_input.py`, `model_space.py` [type:Task] [P3] [subject:UX]
      - [ ] **Retire construction lines in favour of alignment guides** (user, 2026-08-16) — `ConstructionLine` is an AutoCAD xline (`pt1`/`pt2` set direction only; `_recompute_line` extends past both so it reads infinite). The inference engine now covers the same job — alignment references without a persistent object — matching the Revit reference-plane model. Decide what, if anything, xlines do that inference guides + OSNAP don't, then deprecate the drawing tool. Wired into 11 modules (`construction_geometry.py`, `model_space.py`, `model_view.py`, `scene_io.py`, `snap_engine.py`, `scene_tools.py`, `level_manager.py`, `level_widget.py`, `elevation_scene.py`, `view_3d.py`, `main.py`); existing `.fpd` files hold `{"type": "construction_line"}` records so the **load path needs a decision, not just deletions**. [ref:inferred-dimension-driven-placement] [type:Task] [P3] [subject:Architecture]
      - [ ] **`DimensionEdit` arithmetic input** (user, 2026-08-16) — accept basic expressions in any dimension field and auto-evaluate on commit (`3ft - 1.5ft` → `1' - 6"`, `12" * 2`, `10ft/3`). No arithmetic parsing exists anywhere in the codebase today — net-new. Mixed units must normalise through `parse_dimension` per-term before evaluating; unparseable/unsafe expressions fall through the existing revert-to-last-valid path (never `eval()`). Benefits all 11 `DimensionEdit` consumers + the Dynamic Input HUD. `dimension_edit.py`, `scale_manager.py` [ref:units-and-formatting] [type:Task] [P3] [subject:UX]
      - [ ] **Migrate hand-rolled numeric inputs to `DimensionEdit`** (2026-08-16 reuse sweep) — `array_dialog.py`, `calibrate_dialog.py`, `main.py` (~2302/2309) parse dimensions ad hoc instead of using the canonical widget; they miss the seed guard, revert-to-last-valid, and unit-system reformat. `array_dialog.py`, `calibrate_dialog.py`, `main.py` [ref:units-and-formatting] [type:Task] [P3] [subject:Architecture]

- [ ] Consolidate the two underlay pen-rendering paths — the import **preview dialog** (`dxf_preview_dialog.py`) strokes geometry with cosmetic width-0 pens, while the **placed-underlay** builder (`model_space.py:_build_batched_underlay_group`) now uses a cosmetic `UNDERLAY_LINE_WIDTH_PX` pen. Two independent pen-construction sites that must stay visually consistent; consider a shared pen/path-builder helper so they can't drift (the original thick-line bug existed in only one of them). [type:Task] [P3] [subject:Code Quality]

- [ ] Include underlays in undo system — `_capture_network` / `_restore_network` intentionally exclude underlays; importing an underlay is not undoable (underlay persists through undo/redo). Quick fix (push_undo_state after import) done 2026-04-24; full fix requires serializing underlays into undo snapshots and clearing/restoring them in `_restore_network`. `model_space.py:_capture_network`, `model_space.py:_restore_network` [type:Bug] [P2] [subject:CAD]

## Snapping Engine Roadmap (from `docs/specs/snapping-engine.md` §12)

- [ ] Fix ConstructionLine perpendicular / nearest / phase-4 participation — **deferred**: ConstructionLine tool is not in active use; revisit if the feature sees real usage. Spec §5 note 2 corrected 2026-04-08. [ref:snap-spec§5-row-ConstructionLine] [type:Backlog] [P3] [subject:CAD]

- [ ] F3 integration test on real keypress — QTest.keyClick did not dispatch through QAction shortcut on headless Windows; investigate pytest-qt / qtbot or alternate dispatch [type:Backlog] [P3] [subject:Testing]

- [ ] Decide whether F3 / global OSNAP toggle should also disable `_snap_to_underlay` (DXF underlay snap), or document the separation in the snap spec [type:Backlog] [P3] [subject:CAD]

- [ ] Spec session: pipe-with-fitting named targets [ref:snap-spec§8.3] [type:Backlog] [P2] [subject:CAD]

- [ ] Underlay intersection snapping bails in very dense regions — phase-4 returns early once segment extraction exceeds `_PHASE4_MAX_SEGMENTS` (256) to bound O(n²) pairing, so a cursor over a dense DXF area (many segments inside the tolerance box) gets no intersection snap at all. Consider: spatial pre-pairing / only pairing segments whose bbox is near the cursor, or raising the cap with a smarter pairing structure. `snap_engine.py:_check_geometry_intersections` [ref:snap-spec§6.1] [type:Task] [P3] [subject:CAD]

## View Relationships Follow-Ups (from `docs/specs/view-relationships.md` §11)

- [ ] Implement section view subsystem — SectionScene, SectionView, SectionMarker, SectionManager, section placement tool, cardinal shortcuts, elevation system retirement. See `docs/specs/section-view-subsystem.md` **[DEFERRED 2026-06-23 grill — cardinal elevations already host on sheets (ViewResolver); arbitrary-angle sections not needed for the AHJ MVP. Spec stays a proposal.]** [ref:section-view-spec] [type:Task] [P3] [subject:Architecture]

- [ ] Spec session: Drafting overrides / view templates — defines resolution rules on top of catalog [ref:view-relationships§7.4] [type:Backlog] [P2] [subject:Architecture]

- [ ] Spec session: Cross-view selection / interaction sync [ref:view-relationships§1.3] [type:Backlog] [P2] [subject:Architecture]

- [ ] Spec session: Paper-viewport-specific overrides (depends on view-templates spec landing first) [ref:view-relationships§7.4] [type:Backlog] [P3] [subject:Architecture]

## Additional Spec Sessions

- [ ] Spec session: Elevation view selection mode — selection/interaction model for elevation scenes. Depends on plan-view selection mode spec for shared conventions. `elevation_scene.py` [type:Backlog] [P2] [subject:Architecture]

- [ ] Spec session: 3D view selection mode — selection/interaction model for 3D viewport (pick ray, actor selection, highlight). Depends on plan-view selection mode spec for shared conventions. `view_3d.py` [type:Backlog] [P2] [subject:Architecture]

- [ ] Spec session: Roof elements — 4 roof types (flat/gable/hip/shed), ridge/hip line computation, pitch-to-peak-height formula, overhang offset algorithm (perpendicular-edge intersection with degenerate fallback), "auto" ridge direction heuristic (longest-edge midpoints), 3D mesh generation (pitched roofs incomplete today). `roof.py` (848 LOC) [type:Backlog] [P2] [subject:Architecture]

- [ ] Spec session: Door & window elements — wall-relative positioning (`offset_along` centerline), width-to-scene conversion chain (3-level fallback), swing arc geometry (doors), crossing-diagonal symbol (windows), hit-test scaling by zoom, preset libraries (doors 820-1800×2040mm, windows 600×1800mm), sill height. `wall_opening.py` (404 LOC) [type:Backlog] [P2] [subject:Architecture]

- [ ] Spec session: Floor openings & stairs — openings, stair geometry, multi-level connectivity, sprinkler coverage implications. No implementation exists today; this is a greenfield design spec [type:Backlog] [P2] [subject:Architecture]

## Code Review Audit — Spec Gaps (from 2026-04-09 audit)

### P2 — Views & Managers
_Grid system, scale calibration & underlay, wall/room/floor system, sprinkler components, hydraulic solver — see Tasks section above_

- [ ] Spec session: elevation scene projection & rendering — cardinal-axis coordinate mapping (N/S/E/W → H,V plane), world Z → vertical axis, entity projection rules, Z-range filtering & view-depth semantics, gridline/datum placement, rebuild triggers, `_ROLE_SOURCE` sync-back to 2D. `elevation_scene.py` (1233 LOC) [type:Backlog] [P2] [subject:Architecture]

- [ ] Spec session: detail view markers & crop geometry — rounded-rect crop box (fillet radius = 1.5× gridline bubble), bubble placement algorithm (leader line, "below center" default), dragging vs resizing interaction model, per-detail view-range override (None = inherit from parent plan), bubble radius = 3× gridline bubble (visual hierarchy). `detail_view.py` (566 LOC) [type:Backlog] [P2] [subject:Architecture]

- [ ] Spec session: 3D view rendering pipeline — PyVista/VTK mesh generation from entity geometry, 200-pipe cylinder threshold (fallback to lines), pick ray casting (15px tolerance), actor-to-entity bidirectional mapping lifecycle, dirty-flag lazy rebuild, radiation heatmap overlay, roof 3D mesh gap (flat only, no pitch), plotter/VTK GL-context lifecycle (`View3D.cleanup()` finalizes the `QtInteractor` on close — mandatory or contexts leak and crash multi-window/test scenarios; added 2026-06-22). `view_3d.py` (1762 LOC) [type:Backlog] [P2] [subject:Architecture]

- [ ] Spec session: view markers & shared crop box — tangent-line circle geometry (R/sin(40°) point distance), four-marker cardinal positioning at crop-box edges, single shared crop box (not per-marker), 8-handle grip system, double-click → elevation view activation. `view_marker.py` (507 LOC) [type:Backlog] [P2] [subject:Architecture]

- [ ] Spec session: layer management system — DXF layer extraction (group data(2) field), QGraphicsItem data(1) layer-name matching, UserLayer lineweight mapping (5 named values, mm → cosmetic px best-fit), active-layer tracking, default layers (Default/Underlay/Annotations/Gridlines), per-item layer assignment protocol. `layer_manager.py` (128 LOC), `user_layer_manager.py` (647 LOC) [type:Backlog] [P2] [subject:Architecture]

- [ ] Spec session: property manager & type system — property type dispatch (label/string/enum/combo/color/level_ref/layer_ref/button/dimension), multi-select conflict resolution (blank when values differ), debounced refresh (50ms QTimer), lazy SprinklerDatabase loading, numeric field auto-validation. `property_manager.py` (553 LOC) [type:Backlog] [P2] [subject:Architecture]

- [ ] Spec session: display system override resolution — formalize the three-tier cascade contract (per-instance > project > QSettings > factory default), scope of per-instance vs per-category applicability, SVG recolouring cache invalidation, interaction with future view templates (view-relationships §7.4). Deepens existing `docs/architecture/display-system.md`. `display_manager.py` (2071 LOC) [type:Backlog] [P2] [subject:Architecture]

- [ ] Spec session: scene tools (geometry editing) — 16 tools in `SceneToolsMixin`: offset (line intersection, polyline offset), array (linear/polar, 200-copy preview cap), rotate/scale/mirror (anchor point transforms), join/explode (merge segments, decompose groups), break/break-at-point (segment splitting), fillet/chamfer (corner rounding/bevelling), stretch (crossing-window selection), trim/extend (to intersections), merge/hatch (polygon merging, fill patterns), constraints (creation and solving). Per-tool workflow, algorithm, edge cases, and mode state machine integration. `scene_tools.py` (1612 LOC) [type:Backlog] [P2] [subject:Architecture]

### P3 — Nice-to-Have

- [ ] Spec session: auto-populate sprinkler placement algorithm — NFPA 13 density/area curve interpolation, polygon decomposition into rectangles (scanline), branch-line direction detection (1/2/3+ pipe logic), `_walk_branch()` algorithm, wall-proximity 2× rule, edge cases (L-shaped rooms, concave rooms, dead-end branches, multiple design areas). `auto_populate_dialog.py` (1640 LOC), `design_area.py` (558 LOC) [type:Backlog] [P3] [subject:Architecture]

- [ ] Spec session: parametric constraint system — extend foundation spec (`docs/specs/parametric-constraint-system.md`) with resolution order design, over-constrained detection, constraint visualization, editing UI, dependency graph, and new constraint types (H/V lock, equal spacing, tangent, parallel, perpendicular, fix/pin). Foundation spec covers existing 3 types + solver + serialization + lifecycle. `constraints.py` (550 LOC) [type:Backlog] [P2] [subject:Architecture]

- [ ] Spec session: annotations & hatch patterns — NoteAnnotation (MText-like word-wrap, bold/italic, alignment), DimensionAnnotation (two-point + offset witness lines), HatchItem (region fill with constraint interaction), SVG hatch pattern loader (24×24 viewBox tiling, seamless rules), built-in Qt brush patterns. `annotations.py` (739 LOC), `hatch_patterns.py` (239 LOC) [type:Backlog] [P3] [subject:Architecture]

## Hydraulic Solver Follow-Ups (from `docs/specs/hydraulic-solver-and-reporting.md` §12)

- [ ] Block hydraulic calculation when scale uncalibrated — REVISIT: `is_calibrated` only applies to underlay scaling; scene is always 1 px = 1 mm so pipes drawn directly have correct lengths. Guard would block valid calculations on projects without underlays. Need to rethink when/if this is needed. `hydraulic_solver.py` [ref:hydraulic-spec§7.3,D8] [type:Task] [P2] [subject:Hydraulic Calculator]

- [ ] **Hydraulic-results view (separate view + hf heatmap)** — create a dedicated results view (not a plan-view colour override) so it displays independently in paper space; colour pipes by friction loss normalized to system max (green/orange/red hf heatmap) instead of the current velocity colouring. `hydraulic_solver.py`, `model_space.py`, `hydraulic_report.py` [ref:hydraulic-spec§11.2,D4, project-hydraulic-results-view] [type:Task] [P2] [subject:Hydraulic Calculator]

- [ ] Sprinkler legend/schedule as **paper-space sheet content** — the report's Sprinkler Schedule tab was removed (3-tab consolidation); an AHJ package wants the sprinkler legend on the drawing, not in the calc report. `paper_space.py` [type:Task] [P3] [subject:Architecture]

- [ ] Pipe material takeoff (BOM/estimating) — the report's Pipe Schedule tab was removed (3-tab consolidation); takeoff belongs in a future BOM/estimating feature. [type:Task] [P3] [subject:Sprinkler Design]

- [ ] Node Summary Table numeric column sorting — header-click sorting is lexicographic ("10" < "2"; numeric columns sort as strings). Default BFS order is correct; fix via `Qt.ItemDataRole.EditRole` numeric data or disable sorting. `hydraulic_report.py` [type:Task] [P3] [subject:Hydraulic Calculator]

- [ ] Report exports re-read the live scene at export time while the embedded graph uses populate-time state — editing the water supply between Run and Export yields a PDF whose Water Supply section, graph, and stored result disagree. Consider snapshot-at-populate for AHJ documents. `hydraulic_report.py` [type:Task] [P3] [subject:Hydraulic Calculator]

- [ ] Multi-system hydraulic export — per-system sections in combined PDF. Depends on sprinkler spec D8. `hydraulic_report.py` [ref:hydraulic-spec§9.4,D9] [type:Task] [P3] [subject:Hydraulic Calculator]

- [ ] Professional PDF templates — company logo, engineer stamp area, page numbers. `hydraulic_report.py` [ref:hydraulic-spec§9.4,D10] [type:Task] [P3] [subject:Hydraulic Calculator]

## Sprinkler System Components Follow-Ups (from `docs/specs/sprinkler-system-components.md` §14)
- _↳ (done parent) SprinklerDatabase singleton + stable path_
    - [ ] **Retire the git-tracked repo `sprinklers.json`** — now that runtime reads/writes `%APPDATA%`, the tracked seed file only feeds the one-time migration and dirties dev checkouts no more; `git rm` + `.gitignore` once migration has shipped a while. `sprinklers.json`, `.gitignore` [type:Task] [P3] [subject:Code Quality]
    - [ ] **Shared `_app_data_dir()` helper** — `sprinkler_db._default_db_path()` and `titleblock_template._library_dir()` duplicate the `%APPDATA% or ~` + `FirePro3D` resolution; extract one helper both call. `sprinkler_db.py`, `titleblock_template.py` [type:Task] [P3] [subject:Code Quality]

- [ ] SVG symbol system expansion — asymmetric sidewall symbol, orientation-driven selection, wall auto-detection, tab-cycle orientation, in-app symbol editor. `sprinkler.py` [ref:sprinkler-spec§8.1,D7] [type:Task] [P3] [subject:Sprinkler Design]

- [ ] Multi-system SprinklerSystem — per-node/pipe system assignment, multiple instances, independent supply nodes, per-system hydraulic calculations. Separate spec recommended. `sprinkler_system.py`, `model_space.py` [ref:sprinkler-spec§9,D8] [type:Task] [P3] [subject:Architecture]

## Wall, Room & Floor Slab Follow-Ups (from `docs/specs/wall-room-floor-system.md` §13–§14)

- [ ] **Legacy face-snapped tee endpoints never form room-detection T-nodes** — `_detect_room_boundary` only tees endpoints within `TOL*3` (6mm) of the host *centerline*; pre-2026-07-13 files have tee endpoints on the *face* (~half-thickness away), so their tees never split walls in the room graph (pre-existing, surfaced by the tee-join rework). New walls are fine (centerline snap). Consider a load-time migration (re-snap face-parked tee endpoints to the centerline) or widening the detection band. `model_space.py:_detect_room_boundary` [type:Bug] [P3] [subject:CAD]

- [ ] Extend the 3-wall full-miter pie join to N-way junctions — 4+-way crossings currently keep Butt (near-always orthogonal, looks fine); the angular-neighbour miter + junction-polygon vertices generalize if diagonal 4-ways show up in practice. `wall.py:_pie_miter_corners` [type:Task] [P3] [subject:CAD]

## Underlay Workflow Follow-Ups (from `docs/specs/underlay-workflow.md` §15)
_Existing test gap tasks (hydraulic solver, auto-populate, geometry utilities) already in Tasks section above_
- _↳ (done parent) Underlay Manager_
    - _↳ (done parent) House theme adopts the manager palette_
        - [ ] **Audit the ENTIRE codebase for non-tokenised chrome** — the build migrated the 9 audited chrome hard-coders and added an anti-drift hex-guard, but the guard is scoped to that allowlist only. Sweep all of `firepro3d/` for chrome **text and colours** that bypass the `theme.py` two-layer tokens: raw hex / named colours (`grey`, `#...`, `rgb(...)`) in `setStyleSheet`, `QColor(...)`/`QPen`/`QBrush`/`QPalette` used for chrome (NOT canvas/geometry entities, which are Display-Manager-owned), and hard-coded `font-size`/`font-family`/point-size literals that should read from tokens. Classify chrome (in scope) vs canvas content (out); tokenise the chrome; then **extend `tests/test_theme_chrome_hexguard.py` to the full chrome file set** so drift stays caught app-wide. `theme.py`, all chrome modules, `tests/test_theme_chrome_hexguard.py` [ref:theming.md] [type:Task] [P1] [subject:UX]
        - _↳ (done parent) Adopt the `SelectionBox` manipulator app-wide_
            - _↳ (done parent) U1_
                - [ ] **Label-rotate: NoteAnnotation + DimensionAnnotation** — "all labels should be rotatable" (2026-08-31 grill). They're translate-only today, so a mixed selection including a note/dimension hides the group rotate knob. Give each a baked `manip_rotate` + `{"translate","rotate"}` capability (notes rotate the text glyph; dimensions rotate the two endpoints), routed through their serialize paths. `annotations.py` [type:Task] [P2] [subject:UX]
                - [ ] **RectangleItem group-rotate external-pivot reconciliation** — `RectangleItem.manip_rotate` stores the group's external scene pivot as `_pivot` (baked-at-rest); the footprint renders correctly rotated, but `rect()` (axis-aligned storage) no longer matches the visual centroid, so a *subsequent resize* can jump. Pre-existing RectangleItem design, exposed now that a rect can ride a mixed group rotate. Reconcile `rect()`/`_pivot` after a group rotate (or re-derive the pivot to the rect centre on the next resize). `construction_geometry.py` [type:Bug] [P3] [subject:CAD]
                - [ ] **Reuse cleanup: `rotated_rect_corners` via `CAD_Math.rotate_point`** — the standalone helper duplicates the Y-up→Qt rotation math the primitive now provides; collapse to one home (skipped in U1 to minimise change surface; verify `test_rectangle_bake.py` stays green). `construction_geometry.py` [type:Task] [P3] [subject:Code Quality]
            - [ ] **U2 — the `Handle` model** — define one `Handle` abstraction (role + scene position + drag→edit + commit) and a `manip_handles(self) -> list[Handle]` capability; re-express the manipulator's own resize/rotate handles as `Handle`s (internal refactor, identical behavior). `selection_manipulator.py` [ref:selection-manipulator §Unification] [type:Task] [P1] [subject:Architecture]
            - [ ] **U3 — migrate items onto `manip_handles`, one per PR** — each item exposes its parametric points as `Handle`s whose drag calls its existing `apply_grip`; the manipulator renders/hit-tests them inside the frame. MUST carry the per-item drag semantics that live in `model_space` today: Ctrl angle-constrain (wall/line/gridline endpoints), gridline multi-select **parallel-delta**, wall-endpoint propagation, and the **constraint-solver** pass. Parity-test each item (posted-event drag == legacy grip drag). per-item modules, `model_space.py` [ref:selection-manipulator §Unification] [type:Task] [P1] [subject:Architecture]
            - [ ] **U4 — retire the parallel systems** — once every item provides `manip_handles`, delete the `drawForeground` grip loop, `scene_tools._find_grip_hit`, and the `provides_handles_for` arbitration predicate. One render path, one hit-test, one undo funnel; the "two systems fighting" bug class becomes impossible. `model_view.py`, `scene_tools.py`, `selection_manipulator.py`, `model_space.py` [ref:selection-manipulator §Unification] [type:Task] [P1] [subject:Architecture]
            - [ ] **U5 — fold in selection-mode + other scenes** — integrate hover pre-highlight / Tab-cycle / rubber-band (`selection-mode.md`) against the unified handles; add `manip_handles` providers for the elevation and 3D scenes (their own selection specs). `model_space.py`, `elevation_scene.py`, `view_3d.py` [ref:selection-manipulator §Unification, selection-mode] [type:Task] [P1] [subject:Architecture]
        - [ ] **Level-chip tint parity** (holistic-review note) — delegate level chips derive `chip`→`raised (#24282D)` / `chip_ink`→`muted (#98A1AA)`, slightly darker/dimmer than the old bespoke `#2C3137`/`#B7BFC7`. Legible, accepted at smoke; promote `chip`/`chip_ink` to their own primitives if exact parity is wanted. `theme.py`, `underlay_manager_delegates.py` [type:Task] [P3] [subject:UX]
        - [ ] **Latched `detect()` in construction-time consumers** — migrated dialogs + the 3D toolbar call `detect()` in `__init__`, so a runtime Preferences→UI theme switch doesn't restyle already-open ones (they update on reopen). If live full-app theme switching is wanted, have `MainWindow._apply_theme` re-run each open consumer's styling (or repaint registry). Pre-existing pattern, not a regression. `main.py`, chrome modules [type:Task] [P3] [subject:UX]
        - [ ] **`roof_dialog._img_label` light-on-dark border** (nit) — the schematic-preview frame keeps a `# theme-exempt` fixed dark `#1e1e1e` backdrop but a token `border_subtle` border; under LIGHT that's a light border on a dark box. Cosmetic; tokenise or fully-exempt if it bothers. `roof_dialog.py` [type:Task] [P3] [subject:UX]
    - [ ] **View-owned per-view underlay visibility** — re-home the removed `hidden_in_views` capability onto the view (PlanView/DetailView/`SheetViewport` stores which underlays IT hides); requirement of the drafting-overrides / view-templates spec (`view-relationships §7.4`). Restores per-viewport underlay hiding (regressed in the interim). [type:Task] [P2] [subject:Architecture]
    - [ ] **Underlay contextual ribbon tab** — Scale/Rotate/Lock/Refresh/Remove on underlay selection (rides the contextual-tab-population work); canvas context menu is the interim home. `main.py`, `ribbon_bar.py` [type:Task] [P3] [subject:UX]
    - [ ] **Optional details-panel dimmer** — opacity was fully retired; if colour-only dimming proves insufficient for tracing, re-add an opacity slider in the manager details panel (the `Underlay.opacity` field still exists). `underlay_manager.py` [type:Task] [P3] [subject:UX]
    - [ ] **Post-build cleanups** — vestigial `source_view_key` param on `paper_display.apply_paper_overrides` (only the deleted underlay-suppression stage used it; remove + update `paper_export`/`paper_space` callers); `_menu_pos` None-guard in `underlay_manager_delegates.py`. [type:Task] [P3] [subject:Code Quality]

- [ ] Preserve source DXF colours option — re-deferred in the 2026-08-09 display-management design (needs (layer, colour) re-batching; per-layer overrides landed instead) [ref:underlay-spec§2.2,§16-D5] [type:Backlog] [P3] [subject:CAD]

- [ ] Undoable underlay operations [ref:underlay-spec§2.2] [type:Backlog] [P3] [subject:CAD]

- [ ] **Fix the two latent exceptions behind the former silent crashes** — now survivable + logged, still bugs: (a) a scene item's Python `boundingRect` raises during `_q_processDirtyItems` (dump sig: `boundingRect` + 2× `itemChange`); (b) pyvistaqt `timerEvent` raises during 3D rebuild (numpy `logical_and` frame nearby). Watch `%LOCALAPPDATA%\FirePro3D\error.log` after real sessions — the traceback pinpoints the raiser; fix surgically then. [type:Bug] [P2] [subject:CAD]

- [ ] Remove dead `layer_manager.py` — `LayerManager` is never instantiated (docstring-only usage); if ever wired up, its `refresh()→_apply_all()` would fight `Underlay.hidden_layers` (defaults every layer to visible). Delete the module or reconcile with the browser-tree layer visibility path. `layer_manager.py` [type:Task] [P3] [subject:Code Quality]

- [ ] Route `main.py` `sceneModified.connect(self.model_browser.refresh)` through `schedule_refresh` — the direct connection rebuilds the whole tree synchronously on every scene change (duplicate work: `set_scene` also connects `sceneModified→schedule_refresh`), and a synchronous rebuild mid-`itemChanged` emission `clear()`s the tree item Qt is still processing (re-entrancy landmine; didn't crash in repros but is fragile). `main.py`, `model_browser.py` [type:Task] [P3] [subject:Architecture]

- [ ] Ability to select/access individual items within an underlay group — future feature to interact with sub-items of an imported underlay [type:Backlog] [P3] [subject:CAD]

## Code Health & Architectural Debt (from 2026-04-29 gap analysis)

- [ ] **Model_Space decomposition** (in progress on `refactor/model-space-slice1-tool-geometry`; grill→map→spec→slice-1 build, 2026-08-28). `model_space.py` had grown to **~11,700 lines / 344 methods** (the old "7,677/191" figure was badly stale; an interim "10,715" was a PowerShell mismeasurement); the class was `Model_Space(SceneToolsMixin, SceneIOMixin, QGraphicsScene)` (slice 2 composed `SceneToolsMixin` out → now `(SceneIOMixin, QGraphicsScene)`) — a **~200-instance-attribute god-object** (the two mixins share `self`, so it's one object, not three). **Phase 1b done:** 9-agent read-only map (`docs/superpowers/specs/2026-08-28-model-space-decomposition-map.md`), structural governing spec **forged + grilled** (`docs/specs/model-space-architecture.md`, added to SPEC-INDEX). Grilled contract: real scene-referencing collaborators reached via a layered on-ramp; four binding seams (scene-graph-mutation-is-universal, undo-snapshot glue, **dual-serialization parity → unify to one `NetworkCodec`**, `set_mode` teardown → ordered idempotent `clear()` hooks); "pure core out, side-effect shell stays"; extracting a concern converts `main`/`view` bare-attr reach-ins into public scene methods same-commit. **Slice roadmap:** (1 ✅) **A+C** — pure item-aware geometry math → new `tool_geometry.py`; constraint solver split (pure `solve_constraints()` → `constraints.py` + scene-side repaint/report shell); `SceneToolsMixin` delegates via thin wrappers (parity net + all callers unchanged); `scene_tools.py` −552 lines; new `tests/test_tool_geometry.py` + `tests/test_constraint_solver.py`. (2 ✅) **B** — `SceneToolsMixin` → composed `SceneTools` (`scene._tools`, explicit `self._scene` boundary; smoke-tested, merged a715e7a). (3 ✅) **`NetworkCodec` unify** — serialize (4a) + deserialize (4b) unified (fixes the §4 divergence ledger); net −252 LOC, parity/byte-identical/undo-stable tests green, smoke-confirmed 2026-09-02. (4 ✅) **Underlay domain slice** — extracted `UnderlayController(scene)` (plain object) → new `underlay_controller.py`; `model_space.py` underlay refs 150→78; pure relocation, `.fpd` byte-identical, undo untouched, back-compat delegation (repoint nothing); smoke-tested 2026-09-02 (`refactor/model-space-underlay-slice`). (5 ✅) **Pipe/Node domain slice** — extracted `PipeNetworkController(scene)` (plain object) → new `pipe_network_controller.py`; ~22 pipe/node methods + `press_pipe`/`move_pipe` bodies + Tab-cycle state + fitting-update orchestration relocated; `model_space.py` 10,699→9,655 (−1,044); pure relocation (`.fpd` byte-identical, undo bytes unchanged — undo/serialization + `sprinkler_system` + `preview_*` + `node_start_pos` stay on scene), back-compat delegation (6 public shells + dispatch forwarders; `complete_confirmation` stays a router); `main._on_escape` reach-in cleaned into new `scene.cancel_pipe_placement()`; `node_start_pos` split DEFERRED (unblocked); smoke-tested 2026-09-02 (`refactor/model-space-pipe-slice`; design `docs/superpowers/specs/2026-09-02-pipe-network-slice-design.md`). (6 ✅) **Sprinkler/Design-Area/hydraulic domain slice** — extracted `SprinklerWorkflowController(scene)` (plain object) → new `sprinkler_workflow_controller.py`; ~13 methods (sprinkler CRUD, `auto_populate_room`, run/clear_hydraulics, set_coverage_overlay, design-area edit lifecycle, water-supply placement) + 4 design-area edit transient attrs relocated; pure relocation (`.fpd` byte-identical, undo untouched — `sprinkler_system`/`design_areas`/`water_supply_node`/`hydraulic_result`/`active_design_area`/`_supply_network_node` stay on scene; serialization+`scene_io`+`network_codec`+`main.py` zero-diff), back-compat delegation (6 public shells + `design_area_sprinklers` property + `_ensure_editing_da` shell + 3 dispatch forwarders; dispatch tables untouched); `set_mode` design-area teardown → idempotent `clear()` + `sync_design_area_z()`; `contextMenuEvent` confirm branch → `confirm_design_area()`; NO §5.1 reach-in cleanup needed (verified no-op); `test_design_area.py` white-box asserts repointed to `scene._spr_ctl._da_*`; per-batch parity reviews + whole-branch seam review APPROVED; full-suite gate CLEAN (0 new failures vs main); smoke-tested 2026-09-03 (`refactor/model-space-sprinkler-slice`; design `docs/superpowers/specs/2026-09-03-sprinkler-workflow-slice-design.md`). (7 ✅) **Placement-input slice** — extracted `PlacementInputCoordinator(scene)` (plain object) → new `placement_input_coordinator.py`; the Dynamic-Input HUD lifecycle + variant cycling + seed/publish/resolve + schema selection + template getters + placement-anchor accessors relocated (~45 methods + `dynamic_input`/`_pipe_hud_reference`/`_resolved_point`/`_variant_index`/`_PLACEMENT_VARIANTS`). **Key conclusion: ALIGN-tracking STAYS on the scene as irreducible core snap-plumbing** (read/driven by `get_effective_position`+`mouseMoveEvent`) — the coordinator owns placement *input*, not ALIGN; documented in `model-space-architecture.md §5.2`. The `"align"` MODE is the separate align-EDGES tool (SceneTools), untouched. Pure relocation, **zero serialization surface** (`.fpd`/undo trivially unchanged; gate = live parity + smoke), back-compat delegation (public shells + read/write `dynamic_input` property bridge + variant shells; dispatch tables untouched); `set_mode` HUD teardown → idempotent `clear()`; hasattr-trap fix on the `set_mode` variant-alias writes; §5.1 = scene `get_align_enabled()` + 4 repointed reads (flag stays scene-side). Caveat: coordinator methods a test monkeypatches at scene level (`active_schema`/`is_input_mode`) route through `self._scene.<name>()` to preserve the patch seam. Full-suite gate CLEAN (5 failures all pre-existing-on-main: the L48 underlay-modify pair + the L281 roundtrip; 0 new); smoke-tested 2026-09-03 (`refactor/model-space-placement-input-slice`; design `docs/superpowers/specs/2026-09-03-placement-input-slice-design.md`). (8 ✅) **2D-geometry drawing slice (simple primitives)** — extracted concern #6's Line/Rectangle/Circle/Polyline drawing methods to new `firepro3d/geometry_drawing_controller.py` (**`GeometryDrawingController(scene)`**, a *plain object* and a **behavior HOME — owns NO state**: all drawing state, persisted lists + transient anchors/previews/flags, STAYS scene-side because `PlacementInputCoordinator` reads it → §5.3). ~24 press/move/preview/applier/commit methods + rect ref-line helpers + polyline close-indicator helpers relocated; one idempotent `clear(new_mode)` absorbs the line/rect/circle/polyline `set_mode` teardown (polygon/arc/text stay inline). **STAYS scene-side (dual-concern/generic):** `_make_line_like` (builds GridlineItem → gridline concern #7 untouched), `_make_ref_line`/`_make_ref_circle` (shared by arc/polygon/wall/floor), `_delete_or_pop_polyline_vertex` (also handles floor). Pure relocation, **zero serialization surface** (`.fpd`/undo trivially unchanged; gate = live parity + smoke); back-compat via scene shells, dispatch tables + `_APPLIER_FOR_MODE`/`_SCHEMA_FOR_MODE` untouched, coordinator applier dispatch + its reads of scene-side state unchanged. `clear()` RED-demo proven. Chunked full-suite gate CLEAN (0 new failures vs main — 3 failing = documented pre-existing underlay-modify trio); smoke-tested green 2026-09-04 (`refactor/model-space-geometry-drawing-slice`; design `docs/superpowers/specs/2026-09-04-geometry-drawing-slice-design.md`). (9+) next concern-#6 sub-slice = **Arc + Polygon into `GeometryDrawingController`** (deferred from slice 8 — 3-step/step-aware-schema, ~18 methods); then arch-placement [concern #7, per-element `*PlacementController`s, wall must extract before room/opening]. `main.py` (#2 monolith, ~4,070 lines) = **sibling task, separate spec** (see follow-up below). **Why it matters:** the single biggest bug surface in the codebase (most seam bugs hide here — the file is too big to hold in context: two context menus, two view paths, focus loss, z-order, the ALIGN scope/first-point/angled-extension bugs). Target: thin core + composed collaborators. `model_space.py`, `scene_tools.py`, `scene_io.py`, `tool_geometry.py`, `constraints.py` [ref:model-space-architecture] [type:Task] [P1] [subject:Architecture]
  - **Follow-ups (filed 2026-08-28 from the decomposition map + slice-1 build):**
      - [ ] **Two newly-catalogued pre-existing full-suite failures** (found 2026-09-03 during the sprinkler-slice gate; **both fail on `main`**, NOT the slice — recorded so future gate baselines are accurate): (a) `tests/test_import_dialog_preview.py::TestPreviewSnapIndex::test_snap_engine_finds_midpoint_via_index` fails in isolation on `main` 3/3 (`assert None is not None` — the dialog snap engine returns no midpoint; the sibling `..._finds_endpoint_via_index` passes — likely a snap-aperture/index nondeterminism or a real midpoint-index gap); (b) `tests/test_underlay_modify_roundtrip.py::test_modify_noop_roundtrip_preserves_all_placement_params` fails on `main` (same family as the documented `test_underlay_modify_flow.py` levels-management pair). Neither is in the documented "L72 trio". `tests/` [type:Bug] [P3] [subject:Testing]
      - [ ] **Underlay slice — post-landing cleanups** (filed 2026-09-02 from the slice build): (a) `underlay_controller.py` imports `underlay_layer_pen`/`_pdf_width_to_px`/`_record_levels` **lazily inside methods** from `model_space` to dodge the model_space↔controller import cycle — promote those pure helpers to a shared module (e.g. `underlay.py`/a util) so the controller can import them at top-level; (b) **DWG-cleanup test gap** — no test exercises the DWG `load_from_file` temp-file-cleanup path (the seam bug where `scene_io` wrote `_dwg_cleanup_path` onto a deleted bridge property was caught by review, not tests); add a DWG-underlay load test (needs an ODA-convertible fixture or a stubbed converter); (c) the subagents' targeted underlay test set **missed** `test_append_geom_to_path.py`/`test_pdf_text_render.py`/`test_import_dialog_preview.py` — they call `Model_Space._append_geom_to_path` **statically**; the full-suite gate caught the resulting regression. Add those files to any future underlay-touching targeted set. `underlay_controller.py`, `tests/` [ref:model-space-architecture §5] [type:Task] [P3] [subject:Code Quality]
      - [ ] **Undo-perf bench: pipe restore via `add_pipe`** — 4b routes `_restore_network` pipes through `add_pipe` (update_geometry + per-pipe fitting DM colours + coalesced viewport update); pipe-connected nodes get fitting colours applied twice (idempotent). `load_from_file` already bears this per-pipe cost on open, so not a new cost class — bench a large-network undo before optimizing (`feedback_perf_theory_needs_minimal_bench`); if it lags, add a bulk-restore path. `model_space.py`, `network_codec.py` [type:Task] [P3] [subject:Architecture]
      - [ ] **Tier-2 deserialize loop boilerplate** — the `for entry: X.from_dict(); addItem; list.append` loops for walls/rooms/floors/roofs/gridlines/construction-geo are duplicated across `load_from_file`+`_restore_network` (per-entity logic already single-homed in each `from_dict`). Low value / higher churn; fold in only if it stops paying rent. `scene_io.py`, `model_space.py` [type:Task] [P3] [subject:Code Quality]
      - [ ] **Tier-3 paste-path (`paste_items`) unify + copy-but-no-paste bug** — `wall`/`room`/`floor_slab`/`roof` are captured by copy but silently dropped on paste (no branch); `block_item` pastes but isn't tracked in any list (orphaned from undo); gridline paste keys on a fragile structural heuristic (§4). Separate deserialize consumer + bug class → own slice. `model_space.py` (`paste_items`) [ref:model-space-architecture §4] [type:Bug] [P3] [subject:CAD]
      - [ ] **`Room.z_range_mm()` reads retired floor `.level`** — new two-boundary slabs don't reliably write `.level`, so ceiling-height can degrade. Not a deserialize-path issue; separate fix. `room.py` [ref:model-space-architecture §4] [type:Bug] [P3] [subject:CAD]
      - [ ] **Pre-existing sprinkler round-trip instability** (found during 4a) — a node's `sprinkler.get_properties()` derived rows (K-Factor/Coverage/Model options) are not byte-stable across save→load→save; unrelated to the codec. `sprinkler.py` [type:Bug] [P3] [subject:Sprinkler Design]
    - [ ] **`main.py` (MainWindow) decomposition — sibling task, own spec** — #2 monolith (~4,070 lines). Honor the cross-boundary dependency ledger (`model-space-architecture.md §5.1`): clean the bare-attr reach-ins (`_on_escape` → `scene.node_start_pos`/`_pipe_node_was_new`) into public scene methods as the pipe concern extracts. `main.py` [type:Task] [P2] [subject:Architecture]
    - [ ] **Drop the SceneTools thin wrappers once Slice B lands** — the `_offset_*`/`_compute_*`/`_get_item_segments`/`_point_to_segment_dist`/`extract_edges` wrappers delegate to `tool_geometry`; when callers move onto the composed `SceneTools`, retire the wrappers and point callers at `tool_geometry.*` directly. `scene_tools.py`, `model_space.py` [type:Task] [P3] [subject:Code Quality]

- [ ] PEP 8 class naming — `Model_Space`, `Model_View`, `CAD_Math` use underscores; should be `ModelSpace`, `ModelView`, `CADMath`. Requires renaming classes + updating all imports and string references. Low priority due to churn. [type:Backlog] [P3] [subject:Code Quality]

## Import

### PDF Import Polish (cluster) — 2026-08-28, branch `feat/pdf-import-polish` (SHIPPED, smoke-tested)
_PDF-specific import polish. Governing specs: `underlay-workflow.md` (import) · `snapping-engine.md` (item 4) · `icon-style-guide.md` (item 5). Grill-locked 2026-08-28; built TDD (~9 commits); all touched-area tests green (157)._

- [ ] DWG import: paper-space viewport compositing — Revit DWGs place building geometry and gridline/dimension annotations at separate model-space coordinates, composited via paper-space viewports. Current import shows raw model space where they're physically separated. Need to read viewport clip boundaries + transforms and remap block coordinates to overlay annotations with building geometry. `dxf_preview_dialog.py`, `dwg_converter.py` [type:Task] [P2] [subject:CAD]

- [ ] Multi-layout import extraction is O(all model space) per layout switch — selecting any layout re-walks all ~49,628 model-space entities and **explodes every INSERT/HATCH/DIMENSION regardless of viewport** (`_entity_in_viewport` returns True for them), producing ~388k explosion geoms that are then filtered down to ~7.5k for a single sheet. Two improvements: **(A) extract model space once, filter per layout** — cache the unfiltered geoms (incl. spatially-culled simple entities) and apply only the cheap viewport-bounds filter on each layout switch instead of re-extracting; **(B) bbox-prefilter blocks** — test INSERT/HATCH/DIMENSION extents via `ezdxf.bbox.extents([ent], fast=True)` (generous margin) and skip exploding those outside the viewport. B is the dominant per-layout win and is localized; A helps multi-layout switching. Spec-touching (governed by `docs/specs/underlay-workflow.md` + multi-layout import design). `dxf_import_worker.py:_entity_in_viewport`, `dxf_preview_dialog.py:_DialogExtractWorker._run_inner`, `dwg_converter.py` [type:Task] [P2] [subject:CAD]

## Paper Space Follow-Ups (from 2026-05-11 implementation)
- _↳ (done parent) Title block template editor_
    - [ ] **Font-properties widget dialog** for title block cell text styling (family/size/bold/italic in one picker, replacing the separate per-cell controls). `titleblock_editor.py` [type:Task] [P3] [subject:UX]
    - _↳ (done parent) "Fields" + "Arrangements" editor tabs_
        - [ ] **Surface solver/renderer warnings on real sheets** — unknown tokens, image-no-room, and strip overflow warn in the editor banner but are silently dropped on sheets (`TitleBlockTemplateItem.warnings` unread after `PaperScene._setup`); a template that overflows with real project values renders clipped with no message. Route through `titleblock_warning`/status bar like the mismatch case. `paper_space.py`, `main.py` [type:Task] [P3] [subject:CAD]
        - [ ] **Orientation-aware template-mismatch message** — the fallback warning names only the paper size; an orientation-only mismatch reads "Template (ANSI D) does not match sheet size ANSI D". `paper_space.py` [type:Bug] [P3] [subject:UX]
        - [ ] **"Effective sizing" hint on mixed pairs** — placement props edit the Slot; on a static|dynamic pair the row solves dynamic while the selected static member shows "Static" (per-slot storage, per-row solve). Add an "effective: Dynamic (partner)" note in the props group. `titleblock_arrange.py` [type:Task] [P3] [subject:UX]
        - [ ] **Extract a `FieldsTab` widget** from `titleblock_editor.py` mirroring `ArrangementsTab`'s shape (dialog owns working+snapshots; tabs own widgets) — deferred mid-branch to avoid churning ~400 test-covered lines. Also sweep the residual `variant` naming (`TitleBlockTemplateItem._variant`, editor locals) left by the solver's `layout` rename. `titleblock_editor.py`, `paper_space.py` [type:Task] [P3] [subject:Code Quality]
        - [ ] **Defensive hardening pair** (holistic review): `PaperScene._refresh_titleblock` should null `_title_tb` before rebuild (future-proofs a mid-swap dangle); editor preview sites should pass `dict(_SAMPLE_VALUES)` (shared-mutable-dict aliasing into `TitleBlockTemplateItem._values`). `paper_space.py`, `titleblock_editor.py` [type:Task] [P3] [subject:Code Quality]
        - [ ] **Side-by-side image+text composition** — `FieldDef.image_position` is reserved ("top" only today); add "left"/"right" letterhead layouts if stacked proves insufficient. Spec DD-12. `titleblock_template.py`, `paper_space.py` [type:Task] [P3] [subject:CAD]
        - [ ] **Insertion-band feel at low zoom** — the quarter-row cap can shrink the drop band to ~±3 px at fit zoom on 10 mm rows (memory: priority bands must not scale with tolerance); if drag-to-insert ever feels fiddly, floor the effective band at ~4 px equivalents while keeping it under half the adjacent row height. `titleblock_arrange.py` [type:Task] [P3] [subject:UX]
          - [ ] **uuid-gate `_do_save`'s result refresh** — Use template A → Save → select B → edit → Save B applies B to the project without an explicit "Use" (pre-existing via old Save; more visible with stay-open Save). Gate the `project_template_result` refresh on `working.uuid == project_template_result.uuid`. `titleblock_editor.py` [type:Bug] [P3] [subject:UX]
          - [ ] **`deleteLater()` the editor dialog per open** — dialogs (scenes, undo snapshots, connections) accumulate per MainWindow session; import dialog sets the precedent. `main.py` [type:Task] [P3] [subject:Code Quality]
          - [ ] **Canvas tooltip polish** — pass the cell's viewport rect to `QToolTip.showText` (native per-cell auto-hide) and `ignore()` instead of accept+hideText in the no-name branch (canonical Qt; matters if item-level tooltips are ever added). `titleblock_arrange.py` [type:Task] [P3] [subject:Code Quality]
          - [ ] **`_pick_field_fill` reads the swatch property, not the model** — migrate to the model-read pattern `_pick_text_color` uses (source of truth). `titleblock_editor.py` [type:Task] [P3] [subject:Code Quality]
    - [ ] **Strip position choice** (bottom/top for portrait sheets) — spec DD-6 reserved `strip_edge`; MVP is right-only. `titleblock_template.py`, `paper_space.py` [type:Task] [P3] [subject:CAD]

- _↳ (done parent) Gridline bubbles true-scale in paper space_
    - [ ] **Unreproduced crash: viewport delete → qFatal** (user, 2026-08-08 smoke, one occurrence) — select viewport + Delete (or context menu) died with `QWindow::setTransientParent` warning ("plan_view_Level 1Window must be a top level window") then 0xC0000409 abort in Qt6Widgets repaint machinery (WER dump `python.exe.20416.dmp` 8/8 10:52). NOT reproduced by user retry nor by scripted repro (real input + VTK-forced native windows + two viewports). Likely the pre-existing native-child-window class (VTK forces sibling tab pages native; no `AA_DontCreateNativeWidgetSiblings`). Monitoring: launch via `_debug_run.py` (repo root, untracked) to capture faulthandler stack + Qt messages in `_debug_crash.log` if it recurs. `paper_space.py`, `main.py` [type:Bug] [P2] [subject:Testing]
    - [ ] **Pipe labels adopt the §9.9 true-scale mechanism** — category-owned paper height + the same mutate/restore geometry stage; today pipe labels are model-unit sized (correct through viewports but not paper-height-driven). **Room labels DONE 2026-08-27** (paper-space bundle smoke follow-up: "Room" paper category `label_height_mm`=2.5mm, `apply_paper_overrides._apply_room_label_paper_height` sizes font to `label_height_mm/paper_scale` + restore; §9.9). **Pipe label COLOUR done 2026-09-03** (`fix/pipe-paper-line-weight`: `_apply_pipe` now forces the label to the paper category colour + save/restore — was white-on-white on the sheet; the pipe *line* weight paper-normalization landed in the same branch). **Remaining: pipe label SIZE** — still model-unit sized (not paper-height-driven); add a "Pipe" `label_height_mm` category key + an `_apply_pipe_label_paper_height` mirroring the room helper (font = `label_height_mm/paper_scale`, save/restore `Label Size`). `pipe.py`, `paper_display.py` [ref:paper-space§9.9] [type:Task] [P3] [subject:CAD]
    - [ ] **Thin-lines toggle** (model-space true-scale WYSIWYG preview of labels; deferred in the 2026-08-07 grill) — bubbles/labels render at paper size in model units when OFF. `gridline.py`, `model_view.py` [ref:paper-space§9.9] [type:Task] [P3] [subject:CAD]
    - [ ] **Undo/redo for Display Manager paper-category settings** (user, 2026-08-08 smoke) — no DM setting participates in any undo stack today; Label Ht follows that convention. Wants a design pass (which stack? settings-level command pattern). `display_manager.py` [type:Task] [P3] [subject:UX]
    - [ ] **Per-entry exception tolerance in `restore_model_display`** — one raising entry aborts the remaining restores (unreachable same-thread today; hardening if the pass ever grows async). `paper_display.py` [type:Task] [P3] [subject:Code Quality]
- _↳ (done parent) Multi-sheet management_
    - [ ] **Per-size title block template library** — one project template per paper size+orientation; unblocks per-sheet mixed paper sizes (spec §4.10, deferred in the 2026-08-06 grill). Needs its own grill (library keying, embed format, resolution order). `titleblock_template.py`, `paper_space.py` [type:Task] [P2] [subject:CAD]
    - [ ] **Mixed paper sizes per sheet** — re-enable §4.10 once the per-size template library lands; data model already per-sheet, only the uniform-size UI rule relaxes. `main.py`, `paper_space.py` [ref:paper-space§4.10,§19.1] [type:Task] [P3] [subject:CAD]
    - [ ] **Model-space empty-selection/Esc panel state** — Esc in plan views blanks the property panel (window `_on_escape` → `clearSelection`); user decision 2026-08-06: empty selection should show the **active view's properties** (level, view range…). Own design pass. `main.py`, `property_manager.py` [type:Task] [P3] [subject:UX]
    - [ ] **Gate placed-views recompute + browser push to relevant mutations** — `_on_paper_modified` rebuilds the sheet tree and recomputes italics on every paper edit (text edits included); bounded but per-edit; also the known double panel-rebuild on sheet-meta change (comment at `_on_sheet_meta_changed`). Consider a viewport-change signal + tree diffing. `main.py`, `project_browser.py` [type:Task] [P3] [subject:Architecture]
    - [ ] **Drag-cursor polish** — during an internal sheet drag the drop cursor shows over non-sheet rows (drop correctly rejected); scope `dragMoveEvent` acceptance to the sheet zone. `project_browser.py` [type:Task] [P3] [subject:UX]
    - [ ] **Intra-batch filename collision dedupe** — separate-files export: two numbers sanitizing to the same filename silently overwrite within one batch (overwrite confirm only checks disk). Suffix or warn. `main.py`, `paper_export.py` [type:Task] [P3] [subject:CAD]
    - [ ] **Stale `LEGACY_SHEET_KEYS` tuple** in `titleblock_template.py` still lists Title/Drawing No (docstring-only use) — trim to ("Rev","Date") on next touch. [type:Task] [P3] [subject:Code Quality]
    - [ ] **"Sheet X of Y" positional auto field** — only if wanted for AHJ sets; distinct from Sheet No (needs total-count invalidation). `paper_space.py` [type:Task] [P3] [subject:CAD]
    - [ ] **Per-sheet persistent undo stacks** — undo history currently clears on sheet switch (grilled MVP rule); revisit if switching becomes frequent. `paper_space.py` [type:Task] [P3] [subject:Architecture]
    - [ ] **MainWindow test mode without View3D/VTK** (user pain, 2026-08-07 smoke wrap) — MainWindow-heavy suites pop real not-responding windows on the desktop, and aborted runs leave zombie windows needing manual closes; `QT_QPA_PLATFORM=offscreen` is NOT viable (View3D's VTK stack native-crashes without a real GL surface — verified 2026-08-07). Add a test flag/fixture that stubs View3D at MainWindow construction so window-based tests run windowless and faster; keep real-View3D coverage in `test_view_3d.py`. Would also shrink the VTK cross-test crash surface (0xC0000409 item below). `main.py`, `view_3d.py`, `tests/conftest.py` [type:Task] [P2] [subject:Testing]

- [ ] Toolbar "Add View" button — secondary placement method alongside drag-from-browser. `paper_space.py` [type:Task] [P3] [subject:CAD]

- [ ] **Full-suite pytest dies with 0xC0000409 after ~600+ tests (partially root-caused 2026-07-13)** — was TWO contributors, not one. **(1) FIXED:** a closed-but-not-deleted MainWindow (module fixtures using `deleteLater()`) delivers its debounced `_view_refresh_timer` timeout at the next `processEvents()` anywhere in the process → `_refresh_all_views` → `view_3d.rebuild()` with `_plotter` already released by `cleanup()` → AttributeError in a Qt-invoked slot → silent fail-fast. Reproduced deterministically (`test_paper_single_widget` + `test_paper_space`; main crashed 3/4); fixed with a `rebuild()` guard (`feat/design-area-polish`). This resolves part of TODO item "Fix the two latent exceptions" (a-adjacent). Debug recipe that found it: a custom `sys.excepthook` pytest plugin + `-s` makes PyQt6 print-and-survive instead of qFatal. **(2) REMAINS (environmental):** the QPrinter real-driver SEH — faulthandler banners at `test_paper_export.py::test_print_routes_through_render`; SEH fires in the `QPrinter(HighResolution)` **constructor** (queries the default printer driver before output format applies); post-fix single-process run still died ~770 tests in. Fix direction: avoid constructing QPrinter against the real driver in tests (mock/stub, or QPdfWriter where only PDF output is needed). `tests/test_paper_export.py`, `tests/test_hydraulic_report.py`, `firepro3d/hydraulic_report.py:_export_pdf`, `firepro3d/paper_export.py` [type:Bug] [P2] [subject:Testing]

- [ ] **Full-suite native crash — third locus: underlay async DXF worker** (found 2026-09-02, pipe-slice full-suite gate; **pre-existing**, unrelated to that slice). Beyond the VTK-MainWindow and QPrinter-SEH loci above, `underlay_controller.py`'s async DXF-worker `<lambda>` slot access-violates in full-suite context — reached via `tests/test_underlay_slice_parity.py::test_underlays_changed_signal_fires_on_register` (~line 154) AND via the `shown_model_view` conftest fixture (`tests/conftest.py:153`). **Deterministic in-suite but PASSES in isolation** → cross-test contamination (a prior test leaves Qt/worker/GL state that makes the worker's signal-delivery fault). Different broad runs crash at different points (~1% vs ~95%) — the non-determinism is the tell. Likely same family as the "two latent exceptions" item (pyvistaqt `timerEvent` / dirty-item `boundingRect`) and `View3D` GL-context accumulation. Investigate: does the worker outlive its scene/thread across tests? Should `shown_model_view` (or the underlay worker) force-join/cleanup on teardown? Captured in the `project_model_space_fullsuite_gate_native_crash` memory. `firepro3d/underlay_controller.py`, `tests/conftest.py`, `tests/test_underlay_slice_parity.py` [type:Bug] [P2] [subject:Testing]

- [ ] Batch/multi-page export content-placement verification — when multi-sheet management + batch export UI land, add a render-content (not just page-dimension) assertion for mixed-size multi-page PDFs. The `paper_export` pipeline already loops `list[Sheet]` with the corrected per-page device rect (`_page_rect`), but page-2+ content placement is currently only structurally correct, not pixel-verified. `tests/test_paper_export.py` [type:Task] [P3] [subject:Testing]

## NFPA 13 Compliance Gaps (from 2026-04-29 gap analysis)

- [ ] Remote area selection mechanism — no way to designate the most-demanding remote area for hydraulic design. Currently uses all design sprinklers equally. Need UI to mark remote area + solver to validate it as most demanding. `hydraulic_solver.py`, `design_area.py` [type:Task] [P1] [subject:Hydraulic Calculator]

- [ ] Paper space Phase 2 implementation — annotations layer, label/thin-line scaling, layer overrides per viewport, DXF export. Phase 1 (sheet management, sheet views, PDF print) is partial. See `docs/specs/paper-space.md` Phase 2. `paper_space.py` [type:Task] [P2] [subject:Architecture]

## Documentation Gaps (from 2026-04-29 gap analysis)

- [ ] Update architecture docs for undocumented components — 8 components added since docs were written: DesignArea (`design_area.py`), DetailView (`detail_view.py`), GridLine (`gridline.py`), WaterSupply (`water_supply.py`), BlockItem (`block_item.py`), Theme (`theme.py`), UserLayerManager (`user_layer_manager.py`), Constraints (`constraints.py`). Add to `docs/architecture/entities.md` and `docs/architecture/overview.md` [type:Backlog] [P2] [subject:Documentation]

- [ ] Update stale metrics in architecture docs — `model_space.py` listed as 7,195 lines (actual: 7,677), `scene_io.py` as 639 (actual: 672), `wall.py` as 1,028 (actual: 1,061). Update `docs/architecture/overview.md`, `docs/architecture/io.md`, `docs/architecture/refactoring.md` [type:Backlog] [P3] [subject:Documentation]

- [ ] Update refactoring.md — all identified problems remain unfixed and codebase has grown. Add notes about model_space.py growth (+482 lines), wall.py growth, and newly identified decomposition targets (detail_view.py, design_area.py, gridline.py). `docs/architecture/refactoring.md` [type:Backlog] [P3] [subject:Documentation]
