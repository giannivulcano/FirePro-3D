# FirePro 3D - Python dependencies
# Install with: pip install -r requirements.txt

ezdxf>=1.4
fonttools>=4.50
freetype-py>=2.5
numpy>=2.0
PyMuPDF>=1.27
PyQt6>=6.8
PyQt6-Qt6>=6.8
PyQt6_sip>=13.8
requests>=2.32
scipy>=1.14

# 3D view (hard-imported at startup by firepro3d/view_3d.py — required to launch)
pyvista>=0.44
pyvistaqt>=0.11
vtk>=9.3

# Testing — per-test timeout so a hung test (e.g. an unpatched modal dialog)
# fails the run instead of silently stalling the whole suite forever.
pytest-timeout>=2.3
